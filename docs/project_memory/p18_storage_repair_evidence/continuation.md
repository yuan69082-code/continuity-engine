# 当前有效任务：P18两文件存储返修

用户已明确批准仅修改storage/json_repository.py和storage/json_runtime_repository.py、必要P18测试/证据/档案。原“等待两文件授权”是已完成历史，不重复申请该项。P18 IMPLEMENTED_NOT_ACCEPTED/EVIDENCE_CONFLICT=PRESENT，不D-073、不Git写、不P19。

开工实测267源码/测试/资源、1480测试身份、499既有成果、32排除项与上轮audit-02一致。保护63项、3规划、正式7文件、版本及pyproject未变。身份见before.json；原件和归档副本sha逐项核对。

新增限制必须遵守：旧独立fixed winerror5/no handle探针要原样复跑。若新方案改变其一次拒绝、guard零调用约束，说明具体差异及真实权限对照后等待用户确认，不能静默改探针。

当前先核查是否可用真实OS异常的本次temp/target路径绑定、当前访问条件、目标与临时文件身份验证，在不接受无绑定winerror5的前提下修复已释放短占用。任何实验只在隔离TEST根。若仍有不可兼容约束，保存证据并停受影响项。

日志使用本目录新标签；不重执行旧validate，不覆盖历史输出。源码未固定前不启动全量。测试控制器需回收自建进程；暂停后先核对真实结果和进程，不重开运行中的测试。


## 当前必须续接的位置

两文件修改范围已经明确批准；不要再回到上一轮“待公共文件范围确认”。本轮原独立8项8PASS，原真实进程4项2PASS2有效FAIL，固定错误与已解除读者当前事实2项2PASS，新增持续走时2项2PASS。运行源码和1480正式身份仍完全未变。

发现一个用户明确要求单独确认的行为差异：带完整src/dst且当前内容、属性、访问条件均允许的固定WinError5，与已解除的真实读者呈现相同当前事实。新分支会允许它有界尝试，旧约束却要求无共享时一次拒绝/guard0。不能仅靠旧探针缺少filename保持PASS就声称行为未变。差异和原生READONLY/DACL拒绝证据见refusal-policy-difference.md。

等待用户明确确认这一行为差异后才改两个运行文件。ACL实验首次辅助描述符二进制对比错误已保留，后续权限语义恢复及自建Temp清理记录完整。源码未改，未跑修后专项/兼容/全量。当前没有可复用的运行中session；本地00:49:52只读匹配进程为0，恢复时仍需重新核实。
