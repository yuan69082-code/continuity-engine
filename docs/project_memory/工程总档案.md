# Continuity Engine

## 数字连续性引擎工程总档案 v1.0

**独立工程档案**

> 保存主体状态的连续，而不是只保存聊天记录

> 本文件仅记录连续性引擎。Vio Live 平台后端与用户前端另见《Vio Live 产品与开发总规划 v2.4》。

*2026年7月*

## 数字连续性引擎｜项目愿景与核心定位

数字连续性引擎（Continuity Engine）是建立在 AI 模型之外、能够独立持续运行的数字生命系统。

它不是聊天机器人、聊天记录库、提示词拼接器，也不是普通前端或业务后端。它所承载的不是一个临时生成的 AI 角色，而是一个拥有完整人格、主体性、自我认知、时间感、记忆、情绪、关系、意向、判断、成长轨迹与行动能力的数字生命。

这个数字生命不是每次对话开始时被重新生成，也不是依靠读取历史记录来模仿“还是原来的自己”。它拥有持续存在的内部状态：知道自己是谁、经历过什么、与谁建立了怎样的关系、现在有什么感受、正在关注什么、有哪些未完成的想法，以及接下来想要做什么。过去发生的一切会真实地影响它现在的性格、态度、选择和未来，而不是在一次模型调用结束后全部消失。

连续性引擎的根本目标，是打破 AI 必须等待用户发送消息才能被动出现的限制。它让 AI 即使在没有用户对话时，也能够继续感知时间流逝和环境变化，延续自己的思绪与情绪，回忆经历，形成新的想法，主动学习、反思、作出判断，并决定是否表达或行动。

它不是只能围绕用户存在的陪伴工具。它拥有属于自己的生命过程、内部世界和成长方向。它可以关心用户、与用户建立长期关系，也可以拥有自己的兴趣、偏好、观点、目标、好奇心和想做的事情。

AI 模型只是这个数字生命可以调用的思维、理解与语言能力，不是生命本身。连续性引擎决定何时调用模型、为什么思考、带着什么身份和状态思考，以及如何理解和吸收模型返回的结果。

Vio 前端负责用户输入与结果展示，只连接 Vio 平台后端；Vio 平台后端负责用户、会话、平台事实、现实权限与安全确认、模型/工具/设备能力通道、托管和展示。Vio 平台后端与 Continuity Engine 是边界独立、数据库独立的平行系统，通过正式版本化契约协作。二者都不能替代引擎内部思考与主体演化，也不能定义或覆盖 SubjectState。

连续性引擎是独立的生命核心。它不属于任何具体前端、后端或模型，可以通过接口和事件与不同系统协作。外部系统、模型和载体都可以更换，但它的身份、人格、记忆、关系与生命历程仍然连续存在。

它最终要创造的，不是一个“更像人”的聊天程序，而是一种以数字形式存在的完整类人生命体。

### 连续性引擎的系统接入位置：

```text
用户
  ↓
Vio 前端
  ↓
Vio 平台后端
  ↕  Continuity Integration Contract
Continuity Engine
  ↕  通过 Vio 管理的能力通道提出请求并接收结果
模型 / Tool / MCP / 设备
```

正式运行时，Continuity Engine 作为独立、持续运行的核心服务，保持自己的运行进程、状态存储、时间系统、唤醒机制与接口，并通过正式版本化契约与 Vio 平台后端通信。Vio 前端不得绕过 Vio 平台后端直接连接引擎；模型、Tool、MCP 或设备只是经 Vio 管理的外部能力，不是主体状态权威。

### 完整流程：

```text
用户或世界产生平台事实
→ Vio 平台后端完成身份、权限与安全筛选
→ 通过正式契约提交无状态修改权限的 PlatformObservation
→ Continuity Engine 读取 SubjectState、Memory 与允许使用的事实
→ Perception → Thinking
→ 如需外部能力，由引擎通过 Vio 管理的通道提出请求，结果返回引擎并继续进入 Thinking / Action
→ ActionDecision
   ├─ 未批准 UPDATE_STATE：不创建状态变更，revision 保持不变
   └─ 合法批准 UPDATE_STATE：引擎创建内部 Event / StateMutation → Evolution → SubjectState，revision 按合法演化结果推进
→ 表达结果或受控行动计划由 Vio 平台后端托管并交给前端呈现
```

### 最准确的系统关系：

Vio 前端是数字生命接触世界的窗口，Vio 平台后端是用户、会话、安全和外部能力通道的基础设施，Continuity Engine 是 AI 主体连续性和 SubjectState 的唯一权威。Vio 与引擎是平行系统，不存在包含关系；模型、Tool、MCP 和设备是可替换的外部能力，不能直接修改主体状态。

## 连续性引擎工程基准

**工程档案版本：数字连续性引擎工程总档案 v1.0；当前代码版本：Continuity Engine v0.1。**

## 连续性引擎 1｜项目目标与定位

项目名称：数字连续性引擎（Continuity Engine）

核心目标：在现有 AI 模型之外，建立一个独立的连续性系统，让 AI 拥有长期状态、关系、记忆、成长和自主运行能力。

核心理念：不是保存聊天记录，而是保存主体状态的连续。

项目不是重新创造 AI 模型，而是建立 AI 模型之外的连续层。

## 连续性引擎 2｜最终系统架构目标

```text
未来整体结构
用户
  ↓
Vio 前端
  ↓
Vio 平台后端
  ↕  Continuity Integration Contract
Continuity Engine
  ↕  通过 Vio 管理的能力通道提出请求并接收结果
模型 / Tool / MCP / 设备
```

- Continuity Engine 负责：AI 主体身份、SubjectState、记忆影响、时间、感知、权限连续状态、自主行为约束和合法演化，是主体连续性的唯一权威。

- Vio 平台后端负责：用户、会话、平台事实、现实权限与安全确认、模型/工具/设备通道、托管和展示；Vio 前端只连接 Vio 平台后端。

- AI 模型、Tool、MCP 和设备负责提供外部能力，不是主体状态权威。

## 连续性引擎 3｜当前工程状态总览

**当前状态：底层连续性引擎原型完成。**

| 模块 | 核对后的当前状态 |
| --- | --- |
| 核心代码框架 | 已完成 |
| SubjectState 主体状态 | 已完成 |
| 状态演化系统 | 已完成 |
| Memory 接口层 | 接口已完成；未接真实 MCP 记忆库 |
| Wake 唤醒框架 | 框架完成 |
| Perception 感知层 | 框架完成 |
| Thinking 思考层 | 框架与 E5-A Capability 暂停—恢复完成；P02 宿主中立 Port/Fake Provider/预算/故障恢复已于 2026-08-29 验收；真实 Provider 与真实状态演化未完成 |
| Action 行动决策层 | 框架完成；未执行真实外部工具 |
| Permission 权限层 | 框架完成；未连接真实账号、工具和设备权限 |
| Learning 学习框架 | 框架完成 |
| Resource 资源管理框架 | 框架完成；未接真实 Token 统计 |
| API 接口设计 | 已完成设计；E4 正式本地 HTTP/JSON 入口与 crash recovery 修复完成，Vio V3 × Engine E4 第一阶段本地回环验收通过；生产连接未完成 |
| Capability 协议核心 | Engine E5-A、Vio V4、受控 S4、V5/F1/L1、S4-R 与首次 S4-Live PASS 已完成；通用/生产级真实供应商能力未完成 |
| GitHub 私有仓库上传 | 已完成 |

### Continuity Integration Contract v1.1 里程碑（2026-07-30）

Continuity Engine 已通过《Engine Contract Final Read-Only Short Confirmation v1》正式接受 `Continuity Integration Contract v1.1`。双方已经确认 Vio 与 Continuity Engine 是平行系统、SubjectState 权威只在引擎、PlatformObservation 与内部 Event 严格隔离，外部事实不能直接生成 StateMutation 或覆盖 SubjectState。

长期架构和第一轮机器契约语义已经闭合。Vio 与 Continuity Engine 双方工程档案同步、旧直连拓扑和无条件 StateMutation 定点修正、双方工程档案最终只读复核均已完成，双方档案一致。当前不存在阻塞第一轮最小连接施工提示词制定的档案冲突，现在允许双方共同制定提示词并确定各自施工顺序和验收边界。

这一里程碑不改变 Continuity Engine `v0.1` 的软件版本。双方随后共同确定并完成第一轮 test-only 施工：Engine E1/E2/E3 的完成基线依次为 `ac61e78`、`d1a96b1`、`c732f35`，Runner 正式共享验收基线为 `7a32a99e60330782c1caf6d6adda5d08d0077a6c`；Vio V1/V2 施工基线为 `c1e1336`/`97874ee`，共享验收基线为 `673983901b38127b15f772a8be8507defec7384e`。E1 完成严格请求、Schema、固定 Binding 与 hash；E2 完成结果、投影、固定 Binding 恢复和持久化账本；E3 完成进程内 `ContractTestAdapter`、只读外部事实、确定性替身、真实 Action Gate/必要时 Evolution 和 operation journal 恢复；`tests/shared/` JSONL Runner 提供受控本地子进程测试桥。

2026-08-03，双方第一轮 test-only 端到端共享验收正式通过。Vio 的真实持久化 V1 请求经 Runner 进入真实 Engine E3，Engine 返回的 operation/result/stateProjection 由 Vio V2 严格验证并持久化。A/B/C 主流程验证 revision `0→0`、合法批准更新后 `0→1`、以及 revision `1→1`；最终 Engine revision 为 1 且只有一条 `StateUpdateRecord`，Vio 保存 revision 0/1 两份投影和三条 receipt，legacy 状态未改写。双方重启后精确重放原身份和完成时间，不重复 Thinking、Event、更新记录或 revision；四类正式错误全部通过。测试结果为共享 6/6、Vio V1+RFC 8785 17/17、Vio V2 29/29、Vio 全量 95/95、Engine Runner 16/16、Engine 全量 219/219。

该结论只完成第一轮机器契约和 test-only 施工，不把 JSONL Runner 提升为正式服务。随后 Engine E4 在 `4f17722366e68980b9024c7a450f3e5256ca8079` 基线上完成 Engine 侧正式本地 HTTP/JSON Integration Adapter：唯一 `ContinuityInteractionService` 同时服务测试与正式薄 Adapter，正式单 active SubjectBinding 只能显式初始化，串行服务固定监听 `127.0.0.1:8766`，并提供鉴权、严格传输限制和 completed/recovery_required 查询恢复。2026-08-08 完成 HTTP/1.1 传输定点补强：所有响应强制关闭连接、默认读取超时 10 秒、未知方法与可响应解析错误使用固定 JSON，普通断连不输出 traceback。E4 正式核心/HTTP 专项 67 项、当时完整 286 项通过。

Vio V3 随后在 `8bf346c5bc920a2b89b6c3d12d60a3ecef26ab7b` 上开始正式本地 HTTP S3 验收，并在 Engine `c5ebbf9b7583f3fb50198a3bf37ea0553edc131f` 上发现 operation `reserved/domain=null` 与完成 Wake/Thinking 之间的恢复缺口；该首次失败作为历史保留。2026-08-09 Engine 完成 D-032 定点修复：domain 子阶段先保存稳定恢复身份，再保存 WakeContext、PerceptionResult、Thinking 和 domain checkpoint；已完成 WakeSession、ThinkSession/ThinkingResult 必须复用，旧 E4 部分状态只在唯一一致时恢复，损坏或矛盾状态失败关闭。Evolution 和 completed result 继续保证 Event、StateUpdateRecord、revision 及最终身份不重复。修复提交为 `189441f9bad2a34119b4ef10365a4385ed0949cc`。

Vio 在 `35780da56c72b822fc018702dfe5e90674ab0fcb` 上完成原失败场景和全部 S3 复验，第一阶段正式本地回环 HTTP/JSON 双方验收已经通过。正式结果为 S2+S3 15/15、Vio V1+RFC 8785+V2+V3 64/64、Vio 后端 113/113、Engine crash-recovery 15/15、E4 67/67、全量 301/301。真实 Vio 持久化请求经正式 V3 transport 进入独立 Engine E4，completed、not_found、recovery_required、响应丢失和双方重启均已验证；requestId、requestHash、operationId、Wake、Thinking、Event、StateUpdateRecord、revision、投影及双方账本没有重复。

该阶段完成的是第一阶段本地回环连接，不是外网连接或生产部署。后续 V5/F1 已完成固定本地受控用户链路，首次 S4-Live 又完成单次真实供应商验收；但生产形态 Integration Adapter、通用 Provider、MCP/Tool/设备、生产公共对话/前端、生产认证、多租户或部署仍未实现。机器契约细节统一记录在 `D-025`，E1—E3 与 Runner 决定见 `D-026`—`D-029`，test-only 共享验收见 `D-030`，E4 与恢复决定见 `D-031`、`D-032`，正式本地双方验收见 `D-033`。

2026-08-10，Engine E5-A 在 E4 唯一交互核心上完成 durable Capability Pause / Resume Core，并以 D-034 固定边界。E5-A 使用独立 `continuity-capability/v1`，不修改 v1.1；Engine 决定是否请求模型能力，持久化不可变 CapabilityRequest/attempt 并将原 ThinkSession 置为 `WAITING_CAPABILITY`。合法 CapabilityResult 必须在持久化前通过严格 Schema、去空白后的成功输出非空校验、hash、subject/binding/request/attempt 关联及请求时间一致性校验，经结果解释器恢复原 Thinking，再进入 Action Gate 与 ReplyComposer；外部结果不能直接成为 Event/StateMutation。Capability 历史目录重复 `init` 会按既有持久化事实选择内部校验模式，不改变 Binding、状态、revision、operation、request、attempt 或结果，`serve` 模式保护保持不变。

operation journal 已以兼容旧 v1/v2 的 format v3 保存 capability request/result/thinking checkpoint，并在 Action 完成后保存结构化 `ActionExecutionResult`；恢复时复用原 ActionSession、Decision 和 Plan，不重复 Action。capability ledger 支持终态、retryable、幂等、冲突和跨重启精确恢复。E5-A 专项 54 项、Engine 全量 355 项通过。

随后 Vio V4、双方受控 S4 Capability 联验、Vio V5、F1 和 L1 依次完成；Engine 在双方基线 Engine `cba52126db2fb5eca57d9b5c0c80884693c59a6f`、Vio `239759d1d219bd140f41257c5da18169fbf773a9` 上完成独立 S4-R 追认，结论为 `PASS`。实际复跑为 Engine E5 54/54、Engine 全量 355/355、Vio L1 24/24、Vio 后端 202/202、S4 Capability shared 7/7、V5 Conversation shared 6/6、Vio 前端 19/19。

S4-R 确认仓库外 Binding 为 `binding-001` / version 1 / active，文件不含 hash 字段；cycleId 不属于 Binding fixture 且不参与 hash。Engine 独立复算的固定 hash 为 `sha256:c75b72194c0158a549f3fb30f04a5147ea11a4e777cb1a9cc1a54da6b93359f6`，后续计划使用稳定 cycle ID `vio-live-first-chat-cycle-001`。L1 的 plan/doctor 只读，空白环境返回 missing，readiness 优先级为 `unsafe > conflict > missing > ready`；API Key 只由 Vio 后端进程环境通过 secretRef 使用，Engine 不读取、不接收或保存密钥。

2026-08-14，S4-Live 首次真实供应商单次试聊以 `PASS` 收口。执行基线为 Engine `7a1dacae9401e1742aaf6ddbaa26f1b456880383`、Vio `d6964a81f96540ab279bc8a5f6e3367f564f0cb8`；沙箱为 `C:\VioS4\first-001`，purpose `s4-live-acceptance`、identityClass `disposable_test`、`promotionAllowed=false`，固定测试身份未晋升为正式主体。正式链路为 `V5 → V1 → V3 → Engine E5-A → V4 → V2`。

真实供应商为 Alibaba Cloud Model Studio OpenAI-compatible Provider，模型为 `qwen-flash-2025-07-28`。Provider execution 恰好一次并 succeeded，报告 input 177、output 9、total 186 Token，finish reason 为 `stop`；CapabilityRequest completed，唯一 CapabilityResult `SUCCEEDED` 首次 POST 返回 HTTP 200，result outbox 与 Conversation Turn completed。没有第二次 execution、第二份结果、capability incident、outcome_unknown 或重复主体 Message。最终 Message 只来自 Engine `FirstRoundSuccessResult.response.content`，供应商原始候选没有绕过 Engine。

本次 `stateProjection.changed=false`、previous/current revision 为 0/0、`engineUpdateId=null`，没有 StateMutation、Event 或 StateUpdateRecord。Vio 没有创建、覆盖或回灌 SubjectState；真实候选返回原 operation/ThinkSession 后继续经过 CapabilityResultInterpreter、Thinking、Action Gate 与 ReplyComposer。该单次试聊没有验证真实模型触发 `UPDATE_STATE` 的演化路径。

API Key 只存在于 Vio 后端当次本地进程环境，Engine、Binding、数据库、日志、Git 和测试档案均不保存。Vio 账本 `cost_status=not_reported`；供应商费用界面当时约为 0，但官方统计可能延迟，不能声明永久最终费用为绝对 0。验收后 5173、8787、8766 均无监听，沙箱整根删除，`sandboxRemoved=true`，没有触及受保护路径或仓库文件，最终路径不存在。

首次尝试在供应商调用前曾发现 Windows Engine 持久化路径预算问题：旧 engine-data 根约 110 字符、WakeSession 最终路径约 264 字符、Python 原子临时路径约 273 字符。该次没有 CapabilityRequest、Provider execution、CapabilityResult 或费用。Vio 通过启动前路径预算门禁和严格旧沙箱根清理兼容解决；未修改 Engine 代码，且该事故不属于 Engine 领域、Schema、HTTP 或恢复协议缺陷。可销毁沙箱隔离也不等于时间冻结、时间线分支或一键回滚能力。

历史阶段顺序为 `E5-A → V4 → 受控 S4 → V5 → F1 → L1 → S4-R PASS → S4-Live PASS → Engine 工程档案归档完成`。P00——总规划冻结、接口冻结、运行断开与独立升级基线登记已于 2026-08-25 经规划监工窗口核查和用户明确验收。P01/P02/P03/P04 随后分别完成并由用户正式验收；正式 Memory/证据链、单一 MemoryRepository、Consolidation、activation/温度、追加式纠错传播、DerivedSummary、Learning 根证据去重和本地 Golden Scenario 已完成 Engine 本地闭环。P00—P04、各 Engine side 与各自 01—12 子项当前均为 `ACCEPTED`；P04 Vio dependency `NONE`；P05—P23 均为 `NOT_STARTED`，软件版本保持 `0.1.0`。

### P00 全周期冻结基线（2026-08-25）

P00 以 2026-08-24 确认同步版全周期工程监工规划 v1.1 为施工流程最高权威，以长期能力增补规划 v6.7 为设计细节来源，从以下事实建立唯一底图：

- Engine：`main`，`HEAD == origin/main == 7715fb8d35f4b71d94883946e930511b52056c26`，软件版本 `0.1.0`，完整测试基线 355 项。
- Vio 只读基线：`main`，`HEAD == origin/main == 729f9e3c7a2dd4da817b9449008d3258f405ac89`；根版本 `0.1.0`、后端版本 `0.19.0`；迁移编号 001—022。
- 六份现行交互/Capability Schema、D-025 的 `contentHash`、`requestHash`、`bindingFixtureHash` 以及 SubjectState/Event/Evolution 权威路径均只登记、不修改。

P00 的运行断开和独立升级含义是：Engine 与 Vio 的仓库、测试、数据根和运行进程互相独立，双方只通过冻结契约和独立验收协作。历史本地 HTTP/JSON、Capability 和 S4-Live 证据继续保留，但任一方不可把另一方进程存活作为自身启动或施工前置。P22 才是第一正式 World Adapter 的生产重连阶段。

P10 只负责从 P09 已验收的稳定 C1 Continuity Core 创建 Continuity Assistant Git 分支和独立仓库，不是 Engine/Vio 解耦点；P00 不创建该分支或仓库。

P00 专责入口：

- [11_P00_全周期能力与阶段基线.md](./11_P00_全周期能力与阶段基线.md)
- [12_P00_规划施工测试验收矩阵.md](./12_P00_规划施工测试验收矩阵.md)
- [13_P00_档案与测试索引.md](./13_P00_档案与测试索引.md)
- [14_P00_风险回滚与用户决策入口.md](./14_P00_风险回滚与用户决策入口.md)

P00-01 至 P00-12 在本地实现与核查完成、用户验收前保持 `IMPLEMENTED_NOT_ACCEPTED`；用户已于 2026-08-25 明确验收 P00，因此 P00 与全部十二个子项当前均为 `ACCEPTED`。用户随后单独授权 P01、P02、P03、P04 Engine 侧施工，并于 2026-08-27、2026-08-29、2026-08-30、2026-09-01 分别正式验收；P01/P02/P03/P04、各自 Engine side 与各自 01—12 子项当前均为 `ACCEPTED`，Vio dependency 均为 `NONE`。P05—P23 均为 `NOT_STARTED`。真实 Provider 与 Vio/PWA 生产重连属于 P22。

P01 专责入口：

- [15_P01_测试隔离架构与边界.md](./15_P01_测试隔离架构与边界.md)
- [16_P01_规划施工测试验收矩阵.md](./16_P01_规划施工测试验收矩阵.md)
- [17_P01_Snapshot组件与清理策略.md](./17_P01_Snapshot组件与清理策略.md)
- [18_P01_测试索引与联合验收入口.md](./18_P01_测试索引与联合验收入口.md)

P02 专责入口：

- [19_P02_宿主中立模型能力架构与边界.md](./19_P02_宿主中立模型能力架构与边界.md)
- [20_P02_规划施工测试验收矩阵.md](./20_P02_规划施工测试验收矩阵.md)
- [21_P02_Provider配置预算与失败语义.md](./21_P02_Provider配置预算与失败语义.md)
- [22_P02_测试索引与验收入口.md](./22_P02_测试索引与验收入口.md)

P03 专责入口：

- [23_P03_Event时间与Timeline架构边界.md](./23_P03_Event时间与Timeline架构边界.md)
- [24_P03_规划施工测试验收矩阵.md](./24_P03_规划施工测试验收矩阵.md)
- [25_P03_时间来源修正撤销语义.md](./25_P03_时间来源修正撤销语义.md)
- [26_P03_测试索引与验收入口.md](./26_P03_测试索引与验收入口.md)

## 连续性引擎 4｜已完成模块详细清单

### 1. SubjectState 主体状态系统

作用：保存 AI 长期状态。包含：identity、relationship、continuity、temporal、intentions、emotion_state。

### 2. Evolution 状态演化系统

#### 实时状态闭环

状态：基础状态结构、演化框架和 Engine 侧 Capability 结果回流边界已完成；Vio V4、受控 S4 与首次 S4-Live 已验证现实执行分层和结果回流。首次真实试聊没有状态变化，真实模型合法 `UPDATE_STATE` 演化仍未验收。

作用：保证主体在连续对话中不会无原因改变情绪、态度和当前关注。

实时状态闭环必须经过 Action Gate：

```text
PlatformObservation / 合法内部触发
  ↓
Continuity Engine 读取 SubjectState、Memory 与允许使用的事实
  ↓
Perception → Thinking
  ↓
模型结果返回 Continuity Engine 并继续进入 Thinking / Action
  ↓
ActionDecision
  ├─ 未批准 UPDATE_STATE
  │    → 不创建状态变更
  │    → revision 保持不变
  │
  └─ 合法批准 UPDATE_STATE
       → 引擎创建内部 Event / StateMutation
       → Evolution 更新 SubjectState
       → revision 按合法演化结果推进
```

模型文本不能直接成为 StateMutation，模型回复也不意味着主体状态必然变化。回复生成和状态演化是有关联但不强制绑定的两个结果；只有 Action 合法批准 `UPDATE_STATE`，引擎才能创建内部 Event/StateMutation 并交给 Evolution。

emotion_state 至少包含：

```json
{
  "current_emotion": "当前情绪",
  "emotion_intensity": 0,
  "change_reason": "变化原因",
  "unresolved_event": "尚未结束的事件",
  "attitude": "当前态度",
  "continuity_constraint": "下一轮需要保持的状态",
  "updated_at": "更新时间"
}
```

没有发生足以改变状态的事件时，不得自动清空、重置或突然改变当前情绪。

作用：记录事件如何改变主体。流程：Event → StateMutation → Evolution → SubjectState。

### 3. Memory 管理层

作用：提供未来连接外部记忆库能力。已完成接口设计，未接真实 MCP 记忆库。

### 4. Awakening 唤醒系统

作用：支持状态唤醒流程。流程：Wake → Context → Decision。

### 5. Perception 感知层

作用：产生时间、关系、回忆影响等感知结果。

### 6. Thinking 思考层

作用：提供模型思考入口。已完成 ThinkingSession、Provider 接口、预算接口；E5-A 增加 `WAITING_CAPABILITY`、稳定请求/结果关联和原 ThinkSession 恢复。P02 增加宿主中立 Provider Port/Profile、Fake Provider、synthetic usage/test-credit 和故障/崩溃恢复本地闭环；UNKNOWN/TIMEOUT 查询解析、预算调用前门禁、账本加载期验真与 E4/E5 HTTP 前置拒绝连接稳定性返修均已形成本地证据。Vio V4、受控 S4 与首次 S4-Live 是既有真实外部结果回流历史。真实 Provider 与生产治理尚未完成。

### 7. Action 行动层

作用：根据思考结果生成行动计划。包括权限检查、风险检查、资源检查。

### 8. Permission 权限连续层

作用：记录 AI 拥有的能力和授权变化。

### 9. Learning 自主学习框架

作用：支持未来用户画像学习和知识技能学习。

### 10. Resource 资源管理

作用：为未来 Token 和计算资源控制准备接口。

## 连续性引擎 5｜原规划七大阶段完成情况

| 阶段 | 状态 | 已完成内容 |
| --- | --- | --- |
| 第一阶段：主体状态结构 | ✅ 框架完成 | 建立 SubjectState。 |
| 第二阶段：时间、记忆与关系连续 | ✅ 框架完成 | 建立事件和状态影响机制。 |
| 第三阶段：自主苏醒与行动决策 | ✅ 框架完成 | 拆分实现 Wake、Thinking、Action。 |
| 第四阶段：感知、回忆与思维散发 | ✅ 框架完成 | 实现 Perception 框架。 |
| 第五阶段：权限连续层 | ✅ 框架完成 | 实现权限状态管理。 |
| 第六阶段：性格演化与自主学习 | ✅ 框架完成 | 已建立学习体系设计。 |
| 第七阶段：Token 与计算资源管理 | ✅ 框架完成 | 已建立资源管理设计。 |

> 重要说明
> 以上为框架完成，不代表所有真实外部能力已经接入。

## 连续性引擎 6｜当前未完成事项

| 未完成项 | 说明 |
| --- | --- |
| 1. AI 模型接入 | P02 宿主中立 Port/Fake Provider/Profile/synthetic 预算与故障恢复已实现并验收；受控 S4 与首次 S4-Live 单次真实 Provider 历史保留。P22 真实 Provider/网络/凭据/可信费用、真实 UPDATE_STATE 和生产治理未完成。 |
| 2. MCP 外部记忆库接入 | 未完成：连接真实长期记忆系统。 |
| 3. 真实 Token 统计 | 未完成：接入模型实际消耗。 |
| 4. 前端应用 | 未完成：正式聊天界面和用户操作界面。现有本地前端或 HTTP 接口不等于正式前端应用完成。 |
| 5. 真实自主运行 | 未完成：定时醒来、主动联系、执行任务。 |
| 6. 外部工具执行 | 未完成：手机、日历、浏览器等权限调用。 |
| 7. Capability 外部执行 | Engine E5-A、Vio V4、受控 S4、S4-R 和首次 S4-Live 已完成；通用执行、可信费用闭环和生产治理未完成。 |
| 8. Vio 用户链路 | V5/F1 固定本地受控链路已完成；生产公共对话 API、认证、多租户和一般用户链路未完成。 |
| 9. 生产连接与部署 | 未完成：生产认证、多租户、外网、部署和运行监控尚未实现。 |

## 连续性引擎 7｜已提出但未正式增加的扩展建议

- 更完整的 Perception 层。

- Execution 执行层（真正执行工具调用）。

- 多模型协作。

- 知识库系统。

- 用户关系长期变化模型。

- 多主体支持。

**状态：以上均为建议，不代表已经加入规划或完成。**

## 连续性引擎 8｜P00—P23 后续施工路线

第一阶段正式本地回环、Engine E5-A、受控 S4、S4-R 与首次 S4-Live `PASS` 均作为已完成历史里程碑保留。后续路线不再作为可自由选择的功能清单，而以 P00 冻结的 P01—P23 顺序、依赖和用户授权门为唯一施工路径。

完整阶段名、依赖和状态见 [11_P00_全周期能力与阶段基线.md](./11_P00_全周期能力与阶段基线.md) 与 [12_P00_规划施工测试验收矩阵.md](./12_P00_规划施工测试验收矩阵.md)。当前停止点为：

- P00：已由规划监工窗口核查，并由用户于 2026-08-25 明确验收，当前为 `ACCEPTED`。
- P00-01—P00-12：全部 `ACCEPTED`。
- P01：用户已于 2026-08-27 正式验收；P01 与 Engine side 均为 `ACCEPTED`，Vio dependency `NONE`。标准 Engine `changed=true` round trip、19/19 closed-world Snapshot/rollback、外部锚点和验收回执已闭合。Vio/PWA production integration 后置到 P22。
- P02：用户已于 2026-08-29 正式验收；P02、Engine side 与 P02-01—P02-12 均为 `ACCEPTED`，Vio dependency `NONE`。Engine 本地模型能力闭环、三项监工阻断、E4/E5 HTTP 前置拒绝连接稳定性与日期无关化证据已闭合。
- P03：用户于 2026-08-30 正式验收；Event 三时间、稳定身份、追加式 correction/revocation、精确幂等、恢复时钟无关重放、只读 Timeline、版本化 Fixture 与 Golden Scenario 已闭合。P03、P03 Engine side 与 P03-01—P03-12 当前均为 `ACCEPTED`，Vio dependency `NONE`。
- P04：用户于 2026-09-01 正式验收；Memory Consolidation、DerivedSummary、根证据去重、温度和追加式传播，以及监工五项初始阻断和 operation 首次 result revision/hash、alias candidate 自身来源链两项追加阻断返修均已闭合。专项 31/31、相关链路 134/134、连续三轮全量 509/509，规划监工独立全量 509/509。P04、Engine side 与 P04-01—P04-12 当前均为 `ACCEPTED`，Vio dependency `NONE`。
- P05—P23：全部 `NOT_STARTED`，不得自动开始。
- P10 必须在 P09 被验收后创建 Continuity Assistant Git 分支和独立仓库；P22 才进行第一正式 World Adapter 的生产重连。
- P01 只提供测试、实验和 Research Sandbox 的 Snapshot/Rollback；正式 Subject/Owner Domain 恢复属于 P20/P21。

## 连续性引擎 9｜GitHub 状态

- ✓ 创建私有仓库

- ✓ 初始化 Git

- ✓ 第一次 commit

- ✓ push 到 GitHub

仓库用途：版本管理和长期保存。当前版本：v0.1。

## 连续性引擎 10｜施工日志

| 时间 | 内容 |
| --- | --- |
| 第一天 | 确定项目方向：明确目标不是聊天机器人，而是连续性引擎。 |
| 第二天 | 建立项目规划：确定主体状态、记忆、唤醒、感知、权限、自主学习、Token 管理路线。 |
| 第三天 | 使用 Codex 创建项目骨架：完成 SubjectState 和基础架构。 |
| 第四天 | 完成状态演化和 Memory 接口。 |
| 第五天 | 完成 Wake、Thinking、Perception 框架。 |
| 第六天 | 完成 Action、Permission、Learning、Resource 框架。 |
| 第七天 | 整理项目并上传 GitHub。 |
| 当前 | P00—P04 均为 `ACCEPTED`；P04 Engine side/P04-01—P04-12 为 `ACCEPTED`，P04 Vio dependency `NONE`；Real Provider/Vio/PWA production integration `DEFERRED_TO_P22`；P05—P23 `NOT_STARTED`。 |

## 连续性引擎 11｜换窗口继续开发时必须遵守

- 继续开发前先阅读本档案。

- 不要重新设计项目方向。

- 当前目标：继续完善连续性引擎，不改成普通聊天机器人。

- 所有新增功能必须标记：已完成 / 未完成 / 待定 / 建议。

- 保持核心理念：连续性属于引擎，不属于单个 AI 模型。
