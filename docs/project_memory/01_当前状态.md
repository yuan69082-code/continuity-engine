# 当前状态

> 状态快照日期：2026-08-01

## 当前版本

- 包版本：`0.1.0`（来自 `pyproject.toml`）。
- 工程档案版本：`数字连续性引擎工程总档案 v1.0`。
- 项目定位：位于 AI 模型之外的数字连续性层；长期愿景是独立持续运行的数字生命系统，引擎承载连续性，模型与工具是未来可调用能力。
- 发布状态：已有 Git 初始提交并同步到 `origin/main`，但尚无版本标签，不能视为已完成正式版本发布。
- Python 要求：`>=3.11`。
- 发布前兼容检查范围：Python 3.11–3.14。
- 当前核对环境：Python 3.14.4。
- 第三方运行依赖：`jsonschema>=4.26,<5`（MIT，Draft 2020-12 校验与本地 registry）和 `rfc8785>=0.1.4,<1`（Apache-2.0，RFC 8785 规范化）。二者均为本地库，不需要网络服务、账户、密钥或遥测。

## Vio 连接契约与当前协作阶段

- Continuity Engine 已通过《Engine Contract Final Read-Only Short Confirmation v1》正式接受 `Continuity Integration Contract v1.1`。
- 长期架构和第一轮机器契约语义已经闭合；此前“暂不接受”属于相应审核时点的历史结论，不再是当前状态。
- Vio 与 Continuity Engine 双方工程档案同步、引擎旧拓扑和无条件 StateMutation 定点修正、双方工程档案最终只读复核均已完成，双方档案一致。
- 第一轮最小连接施工范围已经共同确定，Continuity Engine 于 2026-08-01 完成 Engine E1 机器契约基础。
- E1 已实现 PlatformObservation/PlatformFactPackage 类型化模型、固定 SubjectBinding fixture、三份本地 Draft 2020-12 Schema、严格 validator、跨字段验证、RFC 8785 以及 `contentHash`、`requestHash`、`bindingFixtureHash` 计算与验证。
- E1 已提交并同步至 `main`/`origin/main`，本轮施工基线为 `ac61e78`。
- Engine E2 已完成不可变成功结果与错误 envelope、最小 stateProjection、`stateHash`/`contentHash`、固定 Binding 本地持久化，以及 request/operation/result 账本和跨重启精确重放。
- Engine E2 已提交并同步至 `main`/`origin/main`，Engine E3 施工基线为 `d1a96b1`。
- Engine E3 已完成独立、test-only、进程内 `ContractTestAdapter`：按固定顺序验证请求、Binding、账本和 revision；把已验证平台事实作为只读感知输入；使用确定性 Memory/Thinking/Reply/Token 替身；经过真实 Action 权限/风险/资源/revision Gate 后，仅对批准的 `UPDATE_STATE` 创建内部 Event/StateMutation 并进入 Evolution；operation journal 支持跨重启中断恢复和完整结果重放。
- 2026-08-02，Engine E3 经项目统筹窗口独立验收正式通过；E1、E2、E3 均已完成并通过验收，E3 专项测试 50 项、完整测试 203 项全部通过。
- E3 不复用也未修改 `APIService.submit_message`、本地 HTTP 或 `UserInteractionService`。Vio 投影接收链路、双方共享测试、网络连接和生产 Integration Adapter 仍未实现。
- 当前未授权开始后续代码施工；下一步等待双方确定 Vio 侧施工、投影接收和共享验收顺序。
- 机器契约细节和三个固定 hash 集中记录在 `04_决策记录.md` 的 `D-025`；本文件只维护当前状态结论。

## 阶段完成度

本档案使用以下口径：

- **已完成：** 当前代码中存在完整的 v0.1 核心实现与对应测试。
- **部分完成：** 内部结构或适配接口已经存在，但统一入口、持久化或真实集成仍不完整。
- **未实现/未来规划：** 只有边界设计或计划，没有真实外部能力。

工程总档案的阶段结论是：**Continuity Engine v0.1 底层连续性引擎原型完成，原规划七阶段的内部框架已建立。** 该结论只表示领域模型、服务边界、仓储或可插拔接口已经形成，不表示真实外部能力已经接入。

工程总档案中的“数字生命系统”是长期项目愿景，不是 v0.1 的完成状态。当前版本没有独立常驻运行进程、完整时间与环境感知、真实模型思考、外部工具执行、自主表达或无需触发的后台生命循环。

按实际施工顺序归一化后，当前状态为：

1. SubjectState 最小持久化：**已完成**。
2. Event 与 Evolution：**已完成核心模块**。
3. Memory 管理层：**部分完成**；检索请求、相关性判断和影响记录接口已完成，外部长期记忆库未实现。
4. Awakening System：**部分完成**；周期、会话和单次唤醒流程已完成，后台定时器与统一资源入口未实现。
5. Thinking Engine：**部分完成**；直接接收 PerceptionResult，ThinkSession、Provider 和预算接口已完成，真实模型未接入。
6. Perception Engine：**已完成 v0.1 确定性感知层**。
7. Action Engine：**已完成决策规划层**；真实执行属于未来独立阶段，ActionSession 持久化尚未完成。
8. Permission Continuity Layer：**已完成内部权限连续状态层**；真实平台权限未接入。
9. Personality Evolution & Self Learning：**部分完成**；受控学习框架已完成，自动行为分析和模型学习未实现。
10. Token & Resource Management：**部分完成**；资源状态、管理器和检查入口已完成，但尚未强制所有路径使用统一资源入口。
11. API / MCP / Skill Adapter：**部分完成**；进程内 API 和适配器设计已完成，真实 MCP 与平台 Skill 未实现。
12. Frontend Integration：**部分完成**；最小本地前端可运行，不是生产前端。
13. First Round Minimal Connection — Engine E1：**已完成机器契约基础**；只负责类型、Schema、离线校验、规范化和 hash，不包含适配器、账本、重放、投影或实际连接。
14. First Round Minimal Connection — Engine E2：**已完成数据与持久化基础**；结果、错误、投影、固定 Binding 和本地账本可跨重启恢复，但不包含 `ContractTestAdapter`、完整流程、Vio 接收器或共享测试。
15. First Round Minimal Connection — Engine E3：**已完成引擎侧 test-only 确定性领域闭环**；包含独立 Adapter、确定性替身、真实 Action Gate/Evolution 和可恢复 operation journal，不包含 Vio 客户端、投影接收器、共享测试、网络或生产连接。

原规划在 Action 之后重新使用了“第五、六、七阶段”编号。档案统一采用上述施工序号，并在路线图保留原编号映射。阶段曾完成验收不等于所有最终外部能力均已实现。

## 当前核心闭环状态

工程总档案定义的目标实时闭环必须经过 Action Gate：

```text
PlatformObservation / 合法内部触发
→ Continuity Engine 读取 SubjectState、Memory 与允许使用的事实
→ Perception → Thinking
→ 模型结果返回 Continuity Engine 并继续进入 Thinking / Action
→ ActionDecision
   ├─ 未批准 UPDATE_STATE：不创建状态变更，revision 保持不变
   └─ 合法批准 UPDATE_STATE：内部 Event / StateMutation → Evolution → SubjectState，revision 按合法演化结果推进
```

模型文本不能直接成为 StateMutation，模型回复不意味着主体状态必然变化；回复生成和状态演化是有关联但不强制绑定的两个结果。当前已经具备状态读取/保存、Memory 检索管理接口、StateMutation/Event、Evolution、Action 决策边界、revision 校验和审计记录；ThinkingProvider、进程内 API、本地 HTTP 与调试前端提供了内部集成位置。真实模型尚未接入，因此当前不能声称真实模型参与的闭环已经完成。

## 当前代码结构

```text
continuity-engine/
├─ README.md
├─ pyproject.toml
├─ docs/project_memory/
│  ├─ 00_项目总览.md … 10_档案修订记录.md
│  └─ CHANGELOG.md
├─ src/continuity_engine/
│  ├─ __init__.py
│  ├─ __main__.py
│  ├─ cli.py
│  ├─ domain/
│  │  ├─ action.py
│  │  ├─ awakening.py
│  │  ├─ awakening_rules.py
│  │  ├─ errors.py
│  │  ├─ events.py
│  │  ├─ evolution.py
│  │  ├─ learning.py
│  │  ├─ integration_contract.py
│  │  ├─ integration_hashing.py
│  │  ├─ integration_results.py
│  │  ├─ memory.py
│  │  ├─ models.py
│  │  ├─ perception.py
│  │  ├─ perception_rules.py
│  │  ├─ permissions.py
│  │  ├─ resource_policy.py
│  │  ├─ resources.py
│  │  └─ thinking.py
│  ├─ services/
│  │  ├─ action_evolution_service.py
│  │  ├─ action_evaluators.py
│  │  ├─ action_permissions.py
│  │  ├─ action_ports.py
│  │  ├─ action_service.py
│  │  ├─ awakening_service.py
│  │  ├─ contract_test_bootstrap.py
│  │  ├─ contract_test_doubles.py
│  │  ├─ learning_service.py
│  │  ├─ integration_contract_hashing.py
│  │  ├─ integration_contract_validation.py
│  │  ├─ integration_result_factory.py
│  │  ├─ memory_ports.py
│  │  ├─ memory_service.py
│  │  ├─ perception_service.py
│  │  ├─ permission_service.py
│  │  ├─ resource_aware_wake_scheduler.py
│  │  ├─ resource_manager.py
│  │  ├─ subject_state_service.py
│  │  ├─ thinking_ports.py
│  │  ├─ thinking_service.py
│  │  ├─ user_interaction_service.py
│  │  ├─ wake_perception_thinking_action_service.py
│  │  └─ wake_perception_thinking_service.py
│  ├─ storage/
│  │  ├─ base.py
│  │  ├─ in_memory_action_repository.py
│  │  ├─ json_awakening_repository.py
│  │  ├─ json_learning_repository.py
│  │  ├─ json_permission_repository.py
│  │  ├─ json_repository.py
│  │  ├─ json_resource_repository.py
│  │  ├─ json_integration_repository.py
│  │  └─ json_thinking_repository.py
│  ├─ interfaces/
│  │  ├─ api_service.py
│  │  ├─ contract_test_adapter.py
│  │  ├─ core_views.py
│  │  ├─ http_server.py
│  │  ├─ integration_contract_schema.py
│  │  ├─ local_frontend_app.py
│  │  ├─ mcp_adapter.py
│  │  ├─ models.py
│  │  ├─ ports.py
│  │  ├─ security.py
│  │  ├─ skill_adapter.py
│  │  └─ schemas/（三份 E1 Draft 2020-12 Schema）
│  └─ frontend/
│     ├─ __main__.py
│     ├─ app.js
│     ├─ client.js
│     ├─ index.html
│     └─ styles.css
└─ tests/
   ├─ test_contract_test_adapter.py
   ├─ test_integration_contract.py
   ├─ test_integration_persistence.py
   ├─ test_action.py
   ├─ test_awakening.py
   ├─ test_event_persistence.py
   ├─ test_evolution.py
   ├─ test_frontend_integration.py
   ├─ test_interfaces.py
   ├─ test_learning.py
   ├─ test_memory_service.py
   ├─ test_models.py
   ├─ test_perception.py
   ├─ test_permissions.py
   ├─ test_repository.py
   ├─ test_resources.py
   ├─ test_service.py
   └─ test_thinking.py
```

每个包还包含必要的 `__init__.py`；树中省略了缓存文件。

## 当前运行状态

### 验证结果

- 2026-08-01 在项目根目录设置 `PYTHONPATH=src` 后执行 `python -m unittest discover -s tests -q`：203 项测试通过；E2 同步基线 153 项继续通过，E3 新增 50 项。
- 全部 Python 源码与测试通过 Python 3.11 语法版本解析检查；当前机器只安装 Python 3.14.4，尚无 3.11–3.14 多解释器 CI 矩阵。
- CLI 入口已建立：`continuity-engine`。
- 本地前端入口已建立：`continuity-frontend` 或 `python -m continuity_engine.frontend`。
- 本地前端首次启动需要显式初始化：`python -m continuity_engine.frontend --initialize`。
- 默认本地地址：`http://127.0.0.1:8765`。
- Node.js 语法检查曾用于前端 JavaScript；当前环境可用 Node v22.23.1。

### 数据状态

- 默认本地运行数据位于 `.continuity-data/`。
- 该目录已被 `.gitignore` 排除，不应上传仓库。
- SubjectState、事件/变化记录、唤醒、思考、权限、学习和资源均有 JSON 持久化实现。
- E2 的固定 SubjectBinding 与 completed result ledger 使用独立、带格式版本的 UTF-8 JSON 文件，并通过临时文件、flush/fsync 和原子替换保存。
- ActionSession 当前只有内存仓储，进程重启后不恢复。
- Memory 只管理检索请求、相关性和影响记录，不拥有长期记忆存储。

### 接口与前端状态

- `APIService` 已提供统一请求/响应模型及状态、记忆、感知、思考、行动、唤醒和聊天入口。
- E1 机器契约 validator 已能在进程内严格验证 v1.1 正式请求向量并复算三个固定 hash；它不是传输入口，也不运行完整交互。
- E2 已能构造并恢复第一轮成功结果、错误 envelope 和最小 stateProjection，并按 requestId、subject revision 与非空 engineUpdateId 约束本地账本；它仍不是摄取入口，也不向 Vio 发布投影。
- `ContinuityMCPAdapter` 与 `SkillAdapter` 是接口设计，不是已连接的平台集成。
- 本地 HTTP 服务和最小聊天界面可运行，但不是生产部署方案。
- 工程总档案所称“前端应用未完成”指正式产品前端；现有本地 HTTP 与调试界面不等于正式前端完成。
- 前端不保存 SubjectState、记忆或人格状态。
- 当前仓库不包含 Vio 平台后端。Vio 负责用户、会话、平台事实、现实权限与安全确认、模型/工具/设备能力通道、托管和展示；它与 Continuity Engine 是边界独立、数据库独立的平行系统。
- 正式未来拓扑固定为“Vio 前端 → Vio 平台后端 ↔ Continuity Integration Contract ↔ Continuity Engine”，外部模型、Tool、MCP 和设备由 Vio 管理能力通道。前端不得绕过 Vio 平台后端直连引擎，外部能力也不是 SubjectState 权威；正式契约通信、独立常驻服务和结果回流尚未实现。

## Git 状态

- 当前分支：`main`，跟踪 `origin/main`，无 ahead/behind 提示。
- E3 施工前当前提交：`d1a96b1`，与 `origin/main` 一致。
- 远程仓库：`origin` 指向 `https://github.com/yuan69082-code/continuity-engine.git`。
- 当前文件状态：本次 E3 包含 test-only Adapter、只读外部事实感知、确定性替身、Action→Evolution 授权快照与服务、operation journal、独立测试、README 与工程档案更新；不含 Vio 文件、网络/生产入口或现有调试入口修改。全部修改保持未暂存、未提交、未推送。
- `.gitignore` 已覆盖 Python 缓存、虚拟环境、构建产物和 `.continuity-data/`。
- 尚无版本标签或 CI 工作流。

## 已知一致性问题

- README 与 `pyproject.toml` 已在本次发布前整理中同步到 v0.1 实际范围。
- `storage/base.py` 已使用延迟求值类型标注，避免 Python 3.11–3.13 中 `PermissionRepository.list()` 遮蔽内置 `list` 后影响后续返回类型标注；协议方法名和接口保持不变。
- 当前只在 Python 3.14.4 上执行运行时测试；Python 3.11 通过语法版本检查，但仓库仍缺少 3.11–3.14 CI 矩阵。
- `wake_perception_thinking_service.py` 为兼容旧入口而引用内存 Action 仓储，是当前分层中的一个已知兼容性例外。
- 资源检查入口已经存在，但尚未强制所有内部路径统一经过 ResourceManager。
- 忽略目录中的 `__pycache__`、`.pyc` 和本地运行数据不属于应提交源码。

## 当前健康结论

v0.1 的底层连续性引擎原型和测试链路已经建立，适合作为原型发布候选；它还不是工程愿景中的独立数字生命运行体。Vio 与 Continuity Engine 的契约和档案已经对齐，第一轮 Engine E1 机器契约基础、E2 数据/持久化基础及 E3 引擎侧 test-only 确定性领域闭环现已完成并通过测试。E3 只证明引擎进程内边界、幂等与恢复语义可施工，不表示 Vio 客户端、共享测试、网络连接或生产集成已经完成。真实模型、MCP/外部记忆、正式 Vio 接入、ActionSession 持久化、统一资源入口及其他生产能力仍按长期路线分别立项。
