# P19前 A1/A2/A3 补修接续

当前有效授权：在既有R1—R4成果增量上补A1/A2/A3，不回退P18、不进入P19。批次IMPLEMENTED_NOT_ACCEPTED / EVIDENCE_CONFLICT=PRESENT，不验收、不执行Git写操作。

起点已核实main cb528d74884990915737b491ca6a9f2c35cc512a；274份源码/测试/资源、1551测试身份、154项原成果及32排除项均匹配独立审查。before.json固定原成果、保护项和当前源码；independent为逐字节副本，原规划侧不写入。

简短方案与候选运行文件：

- A1：expression_policy_service.py及必要C1/native接线。只分离表达专属拒绝，保留空表达、拒绝依据和原Action/Evolution当前检查；真实Context/来源/状态权限错误仍拒绝。
- A2：continuity_core_service.py、continuity_interaction_service.py、wake_perception_thinking_action_service.py。按绑定Choice与实际能力路径处理效果依赖，保留原模型提案；不扫描正文、不把无回执提案变为事实。
- A3：runtime_cognition.py。根据原Dynamics投影区分满足后的新需要、明确放弃及临时休息，保留间隔/预算/生命周期/STOP；不修改Scheduler或动力学政策。

验证矩阵：

| 项目 | 反例与正向/恢复边界 |
|---|---|
| A1 | native及普通C1表达开启；确认/现实拒绝、内部提交及零表达效果、已提交恢复不重复；Context/来源/状态权限拒绝 |
| A2 | contact/expression.emit/memory-tool实际路径；成功回执、拒绝、UNKNOWN；原始提案保留、独立内部提交、历史核验和不重复 |
| A3 | outcomes act后recur、无新需要不调用、明确放弃不强制复活、休息间隔、持久化正常链、PAUSE/STOP及资源 |
| 交叉 | 原独立八项不改；R1—R4正式组、Expression/C1/Mind/执行恢复/P18相关兼容 |
| 终局 | 源码固定后一次完整回归，原1551身份保留；保护/数据/规划/排除项与精确清单 |

先运行independent-before-01。任何中断先查结果JSON/相关进程，不重复启动全量。辅助runner导入和旧探针标量问题仅为历史辅助错误，不算Engine缺陷。历史F1/H1/F2仍UNKNOWN。

## 21:46 进度

修前原八项4PASS/4FAIL已保留。独立after-01八项全过属中间版；formal-02为23PASS。A1补存原Evolution metadata的空表达拒绝证据解决提交后中断时无执行请求的恢复，未新增存储格式/账本。formal-01新测试一次tick命中合法MAINTENANCE，受控诊断证明下一认知机会，测试据实补齐固定调度顺序。combined-01为61PASS。随后普通C1确认顺序与纯memory.lookup路由新增两反例各1FAIL，已在原授权范围修补。当前运行combined-02，session25976；共有29个补修正式测试，原1551身份不改，预期1580仅是发现计数，尚无最终全量结果。结束后原独立八项新标签、受影响兼容及最终固定源码全量；不要重跑旧组合脚本/覆盖标签。

## 21:48 交叉通过，兼容运行中

combined-02：63PASS，126.864秒；independent-final-01：8PASS，7.349秒。当前compatibility-01运行中，session82980，覆盖P09/P13/P14/P15/P17/P18/Action/Permission/Resources/Learning；请读唯一标签JSON和日志后接续，不并发启动全量，不修改运行中源码/测试。新测试29项，正式发现1580身份（1551原+29），最终总数以完整结果为准。完整回归尚未启动。

## 最终源码固定

compatibility-01完成538/538PASS、1152.693秒。其与combined-02（63PASS）及independent-final-01（8PASS）前后源码完全一致。frozen-source.json固定275源/测试/资源、1580测试身份（原1551+新增29），原测试文件字节未改。源码指纹sha256:37ca50a9b21595f1e31067d88fefe95d1b4ffcf02e9095ef3393d3afcd4c36f1。接下来仅一次full-final-01；未结束前不改源码/测试，不启动重复全量。

full-final-01已启动，当前工具session46642仅供本会话使用。中断接续先检查真实进程、结果JSON/退出码、原始stdout/stderr及前后hash，禁止盲目复用session或重复全量。完成后才运行finish.py（一次性写报告与七份文档）和audit.py（一次性审计/清单），随后核验每文件hash及原32排除项。无需重复已完成专项/兼容。

## 最终测试与档案已完成

所有选定组已完整结束，前后源码一致；报告/矩阵/索引与七份直接档案已同步。只剩final审计与清单核对，不重复测试，不修改源码。当前仍待独立复核，不验收、不Git写入、不进入P19。
