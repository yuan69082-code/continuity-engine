# P12 独立复核返修 R1—R5

2026-09-07。本轮仅修复规划监工确认的五类缺口；用户已授权返修，不是验收。P12 IMPLEMENTED_NOT_ACCEPTED，P00—P11 ACCEPTED，P13—P23 NOT_STARTED，Vio dependency=NONE。PLANNING_CONFLICT=NONE，EVIDENCE_CONFLICT=PRESENT，本轮验证已完成，等待独立复核。

## 来源与修前核对

原件位于规划工作区 `reviews/p12-independent-review-20260906/`：[报告](C:/Users/Administrator/Documents/Codex/2026-08-24/https-github-com-yuan69082-code-continuity/reviews/p12-independent-review-20260906/review-report.md)、[最终原探针](C:/Users/Administrator/Documents/Codex/2026-08-24/https-github-com-yuan69082-code-continuity/reviews/p12-independent-review-20260906/review_probes.py)。24 份原件的 hash 保存在[返修基线](p12_repair_evidence/before.json)；本轮不修改原件，也不向其他任务发送项目资料。

现场 main / HEAD / 本地 origin/main 为 `1b8020fbe687dafaaf83a629dbeee65d69a71d67`，0/0，暂存区空，25 个已跟踪修改、118 个未跟踪文件，包含原 31 个 P10 脚本。202 项源码/测试与原稳定全量清单一致，63 项保护文件一致，1088 个纳入核对的工作区文件已保存逐文件 hash。

监工实跑历史：原 P12 71/71 PASS，21.591 秒；最终探针 9 项，3 PASS、6 FAIL，1.121 秒。965 项全量（964 PASS、1 个 Windows symlink 1314 SKIP，513.166 秒）为监工核验引用的施工方历史结果，并非监工重跑或本轮返修结果。首轮降权探针曾用错查询参数且缺少命中断言，其无效通过不作为证据；最终具名参数与命中断言原样保留。

## 五项根因、修改与限制

| 项 | 根因 | 实际修复与责任边界 | 验证与限制 |
|---|---|---|---|
| R1 | 历史查找缓存正文，来源 Port 返回后未核实权限和目标自身；后查候选也可使前候选失效 | `memory_lifecycle_service.py` 在来源调用后重查权限、目标 canonical hash/状态及传递来源；返回前复核来源快照，再统一核对全部选中条目 | 撤权、删除、恢复造成版本变化、来源变更、后候选影响前候选均失败关闭；合法授权重启召回零写入。只读跨 Authority 快照重验，不宣称分布式原子读事务 |
| R2 | 缺省兼容规则没有区分真正旧记录和进入 P12 后的后继版本 | `json_memory_repository.py` 在 append/save 及加载验证中，禁止后继删除先前已存在的治理字段；既有可选空值仍保留 | 覆盖单字段/全字段丢弃、hash 自洽损坏文件加载零写入、合法归档普通后继与 RESTORE、旧数据 canonical hash；不迁移旧记录 |
| R3 | 可重入锁允许回调提交，外层仍整体写回先前快照 | `apply_lifecycle` 在 evaluate/replay 回调后重读同一文档并比较，变化则明确拒绝外层；成功嵌套版本和 lineage 保留 | 同目标、其他目标、来源/确认回调、显式重试、重启和幂等。无隐式重试，不替换 RLock 造成死锁，不建设分布式事务 |
| R4 | 新 alias 将旧 revision/hash 幂等命中误当成当前可消费授权 | 仓储事务对首次 operation 的当前目标版本、可消费性及根证据重验；Consolidation 保存后经原结果入口再次核实，再返回当前可消费结果或历史回执 | 保存前归档拒绝且不追加 alias；保存后归档不能返回活跃正文，已经合法提交的 operation 事实保留；合法旧精确回执保持 |
| R5 | 仅绑定父来源 hash，换 ID/scope 可恢复同根材料的权重 | 在同一 Memory 文档读取中按去重根计算已登记权重的保守上限；临时 `consumption_weight` 投影不序列化、不改 canonical hash；Memory、Summary、Router、Composer 复用该值；Router 的带权窗口指纹密封消费权重，未降权旧窗口保持旧指纹 | 同根直接 alias、父子及孙链、新旧 alias 和重启均保持降权；新独立根仍 weight=1。混合根材料采用最低上限，不用同根重复加权提升影响；明确 RESTORE 才能解除该根已登记的限制；不改变事实置信度或 R05/R06 学习公式 |

五项修复只改五份已有运行文件：`domain/memory.py`、`services/memory_lifecycle_service.py`、`services/memory_consolidation_service.py`、`storage/json_memory_repository.py`、`services/context_router_service.py`。正式新增回归 [test_p12_memory_review_regressions.py](../../tests/test_p12_memory_review_regressions.py) 保留最终原探针九项，再加入十六项交错、重启、加载及正向对照；另新增 [窄窗口正式回归](../../tests/test_p12_memory_weight_windows.py) 两项。既有原测试未改。

## 本轮真实运行与失败历史

| 标签 | 结果 | 原因与处理 |
|---|---|---|
| [original-probes-before](p12_repair_evidence/original-probes-before.stderr.log) | 9 项：3 PASS、6 FAIL，0 ERROR/SKIP，1.081 秒 | 本轮修改前直接执行规划原探针，六条反例全部真实复现 |
| [original-probes-after-01](p12_repair_evidence/original-probes-after-01.stderr.log) | 9/9 PASS，1.273 秒 | 初次修复后原探针通过；不是最终完整验证 |
| [extended-01](p12_repair_evidence/extended-01.stderr.log) | 25 个方法：20 PASS；1 FAIL、5 ERROR 条目，涉及 5 个测试方法；3.516 秒 | 首版 R2 误要求原本为空的权重字段也必须存在，拒绝合法归档后继及嵌套删除；已改为仅禁止丢弃原已存在字段。测试辅助中 pop 缺省字段、无效大写 summary_type 错误另行修正，不当作 Engine 缺陷 |
| [extended-02](p12_repair_evidence/extended-02.stderr.log) | 25/25 PASS，3.711 秒 | 保留拒绝、正文不可发放和字节不变断言，修正合法可选字段语义；retrieval request 显式补齐 required requested_at，未执行的构造笔误不编造成失败 |
| [p12-stable-01](p12_repair_evidence/p12-stable-01.tests.json) | 96/96 PASS，22.129 秒 | 原 71 项和新增 25 项全部通过，保存完整身份和源码 hash |
| [compatibility-stable-01](p12_repair_evidence/compatibility-stable-01.tests.json) | 428 项：427 PASS、1 既有 SKIP，0 FAIL/ERROR，439.023 秒 | P04—P09、P11、Learning、Memory、前审查修复兼容；原 R07 合并、R05/R06 置信度及 P09 长期场景保留 |
| [original-probes-final](p12_repair_evidence/original-probes-final.stderr.log) | 9/9 PASS，1.488 秒 | 窄窗口修复前代码直接运行规划原探针；输入脚本和 203 项源码/测试 hash 见同名 JSON，不能代替后续版本 |
| [full-stable-01](p12_repair_evidence/full-stable-01.tests.json) | 990 项：989 PASS、1 既有 SKIP，0 FAIL/ERROR，523.452 秒 | 窄窗口检查之前的本轮完整回归；后续存在相关修改，因此保留为中间版本实跑，不冒充最终版本 |
| [weight-window-before](p12_repair_evidence/weight-window-before.stderr.log) | 2 项，2 FAIL，0 ERROR/SKIP，0.305 秒 | R5 附属缺口：仅命中新 alias/摘要、ID/hash/顺序均不变时，旧满权重候选仍通过 revalidate；新检索已正确降权，但旧指纹未包含权重 |
| [weight-window-after](p12_repair_evidence/weight-window-after.stderr.log) | 2/2 PASS，0.304 秒 | 同一窄窗口断言保留；带权窗口指纹纳入消费权重，不重写 Memory/Summary canonical hash，未降权旧指纹保持兼容 |
| [p12-stable-02](p12_repair_evidence/p12-stable-02.tests.json) | 98/98 PASS，22.654 秒 | 新增两项已纳入正式 discovery，原 71 和第一批 25 项全部保留 |
| [original-probes-final-02](p12_repair_evidence/original-probes-final-02.stderr.log) | 9/9 PASS，1.237 秒 | 窄窗口修复后的当前代码再次直接运行原探针，保存 204 项源码/测试 hash |
| [compatibility-affected-02](p12_repair_evidence/compatibility-affected-02.tests.json) | 257/257 PASS，0 SKIP/FAIL/ERROR，55.715 秒 | 窄窗口修复后复跑 P04/P05/P06/P07、P09 边界和 Core、Memory/Learning、前审查修复；仅缩小到实际受影响链，未删原完整回归的测试 |
| [full-stable-02](p12_repair_evidence/full-stable-02.tests.json) | 992 项：991 PASS、1 既有 SKIP，0 FAIL/ERROR，523.914 秒 | 最终代码完整回归；原 965 项和本轮 27 项全部保留，SKIP 仍为原 Windows symlink 1314 |

上述秒数采用 unittest stderr；runner 的进程时间及外层 suite 计时保留在 JSON，边界可能有毫秒差异。早期直接运行日志保存全部具名测试输出；修前 source hash 在 before.json，稳定专项的源码清单在 tests.json。初次修后探针及 extended-01/02 没有单独逐次源码清单，不倒填；后续 runner 已补输入脚本与源码 hash，最终提交给独立复核的稳定结果以实际清单为准。

新增 25 项分别覆盖五类修复及正向对照；没有修改原探针九项断言，没有修改任何旧测试文件。990 个全量测试身份是实际 discovery 结果，等于原 965 加新增 25；该 990 项全量随后完成；追加 R5 窄窗口修复后它作为修复前历史保留，不能代替最终代码结果。原独立报告的 965 项及原 894 基线身份继续作为历史核验对象。

R5 窄窗口复现后才修改相邻 Router 的版本校验并增加两项正式测试，因此需要最终代码的完整回归补跑；这次追加由真实 FAIL 和相关代码变化触发，不是机械执行三轮。旧全量和旧兼容不会改写成最终代码结果。所有场景都在独立 Temp 内执行，两组测试不并发。

本轮 P12 专项 98/98 PASS（22.654 秒）。兼容 257 项：257 PASS、0 既有 SKIP，55.715 秒；稳定全量 992 项：991 PASS、1 既有 SKIP，0 FAIL/ERROR，523.914 秒。 [最终受影响兼容记录](p12_repair_evidence/compatibility-affected-02.tests.json)、[最终稳定全量记录](p12_repair_evidence/full-stable-02.tests.json)。全量 SKIP 为原 Windows symlink 1314，不计 PASS；Engine 无 Actions workflow，不报告远程 CI。所有原始 FAIL/ERROR/SKIP、历史 P09 segment 10 根因 UNKNOWN、旧 CI 失败及初版 P12 证据保留。

## 可复现入口

仅独立系统 Temp Fixture，不使用正式数据根，不接网络业务或生产 Adapter：

```powershell
$env:PYTHONPATH='src;tests'
$env:PYTHONDONTWRITEBYTECODE='1'
$env:PYTHONUTF8='1'
python 'C:\Users\Administrator\Documents\Codex\2026-08-24\https-github-com-yuan69082-code-continuity\reviews\p12-independent-review-20260906\review_probes.py'
python -m unittest discover -s tests -p 'test_p12_memory_review_regressions.py' -v
python -m unittest discover -s tests -p 'test_p12_memory_*.py' -v
python -m unittest discover -s tests -q
```

持久证据入口（每次使用新的 label，旧日志不能覆盖）：

```powershell
python docs/project_memory/p12_repair_evidence/run_tests.py review-p12-01 docs/project_memory/p12_repair_evidence/run_suite.py p12 review-p12-01
python docs/project_memory/p12_repair_evidence/run_tests.py review-compat-01 docs/project_memory/p12_repair_evidence/run_suite.py compat review-compat-01
python docs/project_memory/p12_repair_evidence/run_tests.py review-full-01 docs/project_memory/p12_repair_evidence/run_suite.py full review-full-01
```

最后一轮受影响兼容子集可将 scope `compat` 改为 `affected`，标签必须另取未使用的新值；`full` 始终发现并执行所有原有及新增测试，不按数量阈值放宽。

正式自动策略未配置；遗忘阈值、归档年限、正式删除确认方式仍由用户决定。逻辑删除不是物理擦除；生产删除、备份清除、物理擦除 NOT_READY。不改 Assistant、冻结边界、正式七文件、三规划源、版本或原 31 个 P10 脚本；无 Git 写操作，不进入 P13。

## 终局核对入口

[终局只读审计](p12_repair_evidence/final-audit.json) · [完整 P12 及返修待提交清单](p12_repair_evidence/pending-files.md) · [机器可读分类清单](p12_repair_evidence/pending-files.json)。这些收口报告在全量完成后生成；不以清单存在代替检查结果。初版 p12_evidence 全部保持原样。

本轮五项补修已实现，等待独立复核。P12 / Engine side / P12-01—12 = IMPLEMENTED_NOT_ACCEPTED；P00—P11 ACCEPTED，P13—P23 NOT_STARTED，Vio dependency=NONE。PLANNING_CONFLICT=NONE；EVIDENCE_CONFLICT=PRESENT 表示独立复核提出的阻断尚待监工确认关闭，不表示最终本地测试仍有 FAIL。没有阶段验收决定，没有 Git 写操作。
