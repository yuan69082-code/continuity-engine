# P17 R1/R2 返修独立复核通过

复核日期：2026-09-10。实际工作区：`C:/Users/Administrator/Documents/continuity-engine`。

结论：在本次独立复核覆盖的本地 TEST 范围内，R1/R2 已修复，未发现新增阻断，可进入用户正式验收与 Git 收尾确认。**本报告不是用户验收决定，也不授权提交、push 或 P18。** 没有修改 Engine 的实现、档案或 Git 状态；Engine 中 EVIDENCE_CONFLICT=PRESENT 等待正式归档更新，不把文件当前值冒称已经变更。

## 两项修复结论

### R1：最终资源检查期间撤权

`ExecutionService.current()` 已将资源准备和原 Context/Planner gate 工作放在当前许可读取之前，再检查世界就绪、主体生命周期、Broker/Reality 授权与恢复条件。最终读取仍发生于原 Outbox 投递事务内。

原三个最晚 capacity 检查撤销场景原样通过：Broker 撤权、Reality 撤权、恢复条件失效时没有新增 Fake 效果或实际扣费。新增正式测试还验证了世界文件不变、Adapter execute 零调用；恢复许可本身不自动重发，原请求通过既有显式 retry 才继续；已执行事实恢复不再次进行新执行或扣费。

额外独立检查：在事务内最终资源检查期间，让世界变为不可用或凭据恰好到期，两项均正确拒绝，零效果、零实际扣费。

适用边界：同步本地执行，授权读取端口无写副作用；Outbox 锁不等同跨系统原子撤权或任意生产 Adapter 的 exactly-once。生产恢复/Adapter 尚未开放，此限制已在施工报告写明，不构成本轮新增承诺。

### R2：预计消耗与实际 Fake 扣费

`FakeRealityBoundary.capacity()` 改为读取实际效果投影，再核对请求数、credits、effects 和精确序列化存储大小。世界实际写入的 credits 从对应回执累计，保持原“成功 Fake 效果计 1 TEST credit、失败计 0”语义。route.cost 仍是合法的原 Action Gate 估计，不替代实际 Fake 消耗；没有全局禁用 cost=0。

原 cost=0、credits 上限为 1 的反例原样通过：第一次执行用掉 1，下一次被拒绝，未超限。专项覆盖默认费用、恰好够用、重复恢复不重复扣费、消息/请求/存储限制及零额外消耗的失败回执。

额外独立检查：cost=2 的估计不会改变固定 1 credit 的实际 Fake 消耗，额度耗尽后仍拒绝下一次；纯效果投影不会修改传入文档或已落盘世界文件。两项通过。

## 本次独立实跑

| 独立运行 | 结果 | unittest 耗时 | 外层 runner 耗时 |
| --- | --- | --- | --- |
| 完整 P17 专项（原 62 + 新增正式 12） | 74 PASS，0 SKIP/FAIL/ERROR | 101.169 秒 | 102.077 秒 |
| 原独立 7 项 + 额外独立 4 项 | 11 PASS，0 SKIP/FAIL/ERROR | 9.246 秒 | 9.948 秒 |

原 7 项探针逐字节未改，与修前两次 3 PASS / 4 FAIL 可以直接对照。额外 4 项仅在规划侧，不虚增 Engine 正式测试数量。新 12 项已包含在 74 项专项中，不能重复相加为额外实跑。

证据：本目录 `repair-p17-01.*`、`repair-independent-01.*`，包括命令、原始 stdout/stderr、退出码、源码/文件前后快照。两轮各自 Engine 快照均为 **2158 / 2158 文件，changed=[]**。

## 独立来源与保护核对：24 / 24 通过

详细结果为本目录 `repair-identity-check.json`，验证器为 `verify_repair_evidence.py`。

- 当前 253 个源码、测试及资源文件与返修终局审计、五次引用运行的前后来源完全一致。独立解析 243 个 Python 文件通过。
- 修前 1348 项测试身份和原测试文件完整保留，新增正式 12 项。相对修前源码/测试增量恰好是 ExecutionService、P17 Fixture 两个修改和一个新测试文件。
- 63 项保护文件、三份规划、7 个正式文件及正式数据树、31 个 P10 脚本和 1 个旧接续报告不变。正式树指纹仍为 `sha256:f9c710c6085e430a251f74463d57133f8c5a4497fe8ffa2ccb96be2db1037bb2`。
- 102 个原 P17 证据文件与上次独立快照完全相同；21 份独立原件及归档副本逐字节匹配；113 个声明未改的原 P17 文件核对通过。
- 上次独立快照中的已有文件，仅声明的 23 个路径改变（2 个实现文件 + 21 个文档）；不存在另行覆盖历史或未说明的已有文件改动。
- P17 完整待提交清单 196 个路径，保留原 136 项；另有 32 个排除路径。与当前 21 个 tracked 修改、207 个 untracked 文件精确对应，195 个记录 hash 匹配（审计文件不自哈希）。
- main、HEAD/本地 origin/main 均为 `0c440b0476b07723abafe93777fe895b64fd8d0e`；本地 ahead/behind 0/0，暂存区为空。tracked `git diff --check` 通过。版本 0.1.0、pyproject 不变。

## 引用结果与限制

以下是施工方实跑、本次独立核对来源与原始记录的结果，不冒充监工重新执行：

- 返修正式定点 12 PASS，18.846 秒。
- 原独立探针由施工方执行 7 PASS，6.759 秒。
- 施工方 P17 专项 74 PASS，128.466 秒。
- 受影响兼容组合 224 PASS，169.793 秒。
- 最终全量 **1360 项：1359 PASS，1 既有 Windows symlink 1314 SKIP，0 FAIL/ERROR，1171.401 秒**。

全量未在本次机械重跑；其当前来源、测试数量、原始 OK 摘要与退出码已独立校验。SKIP 不算 PASS。修前 FAIL、新增测试早期 ERROR 及其修正过程继续保留；新测试修正了“恢复许可等于自动重试”的错误假设，没有更改原规划/原 ActionPlanningService 契约。

本次没有访问真实服务、扣真实费用、查询远端或运行远端 CI；不声明远端 CI PASS。不把同步 TEST 结果推广为生产恢复就绪。正常心理内容和沉默未被新增现实边界筛查。

辅助只读发现记录：规划工作区不存在 `.agents/workflow.md`，实际 Engine 的该文件已完整读取；广域 AGENTS 搜索遇到既有 `work/testdeps` 目录读取限制后，未扩大权限或访问依赖目录，继续读取实际相关源码与证据。这些是工具发现过程信息，不是 Engine 测试失败。

## 下一步

等待用户明确“验收并 push”后，由规划侧给出收尾提示词，用户转发 Engine。届时才能登记正式验收、关闭归档中的已知独立阻断、生成最新精确提交范围并普通提交/推送。仍不进入 P18；32 个排除项、失败原始证据和受保护文件保持原样。
