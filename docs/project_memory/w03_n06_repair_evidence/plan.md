# W03/N06 定点返修验证矩阵

当前状态：`IMPLEMENTED_NOT_ACCEPTED`；`EVIDENCE_CONFLICT=PRESENT`。本目录保存新反例和结果，不覆盖 W03 原证据。

| 项目 | 修前反例 | 修后正反与恢复 | 修改边界 |
| --- | --- | --- | --- |
| N06 终态下沉 | 连续完成/取消后当前 64 项集合耗尽 | 第 65 件可新增；终态由原 Evolution 历史只读查到；旧命令重放、重开与并发版本拒绝 | W03 事项服务，不改 SubjectState 容量或原 Event |
| N06 旧事项身份 | 同标题旧字符串在结构化事项完成后仍唤醒 | 必须显式绑定旧列表的准确位置及快照；无绑定同名不合并；重开与撤权拒绝 | W03 事项服务，单个原 Evolution 事务更新两个既有字段 |
| N06 来源有效性 | 不可用 Memory、外部派生材料撤回后事项仍 ready | 原 Memory 与 W02-C 当前根链重验；历史保留、零业务写入、重开与权限边界 | W03 事项服务，复用原存储和外部吸收服务 |
| W02 回忆时限诊断 | 旧 HEAD 与 W03 同用例触发 1000ms 限制 | 同条件各一次，记录实际耗时、停止位置及环境 | 仅诊断，不修改 W02 运行或测试 |

所有已验收 A/B/C 和受保护边界保持不变。正式测试先在修前实际运行并保留输出；修后专项、兼容、全量绑定最终源码。旧 `RECALL_TIMEOUT` 不因一次对照通过而关闭。
