# P02 宿主中立模型能力架构与边界

> P09 现行门：P00—P09 = `ACCEPTED`；P09 / Engine side / P09-01—P09-12 = `ACCEPTED`；P09 Vio dependency = `NONE`；P10—P23 = `NOT_STARTED`。PLANNING_CONFLICT = NONE；EVIDENCE_CONFLICT = NONE（仅表示现行验收阻断已闭合）。 D-054 已记录用户正式验收；历史 segment 10 stderr 缺失、根因 UNKNOWN 保留。成果尚未提交，稳定 C1 SHA 待用户提交并 push 后核定。见 [P09 验收档案](50_P09_测试索引与C1运行入口.md#p09-accepted)。
> 下文保留该阶段施工及验收时的范围、后续未授权状态与测试历史；P09 的本轮新增授权和接线以本页现行门及 47—50 号档案为准，不倒写既往决定。

> 日期：2026-08-28
> 阶段：P02——日常真实模型接入：Engine 独立模型能力闭环
> 当前状态：P02、P02 Engine side 与 P02-01—P02-12 均已于 2026-08-29 正式 `ACCEPTED`
> Vio dependency：`NONE`
> Real Provider integration：`DEFERRED_TO_P22`

## 1. Source of Truth 与阶段门

本阶段按以下优先级实施：用户确认的 P02 边界 → v1.1 P00—P23 阶段顺序与 2026-08-26 OVERRIDE → v6.7 详细架构 → Assistant 文件中的 P10 边界 → 当前代码、测试和历史档案事实。

三份运输附件的 DOCX 容器 hash 与规划窗口原容器不同，但按 python-docx 依次提取 paragraphs、tables/rows/cells，换行连接，Unicode NFKC、连续空白折叠、首尾裁剪并对 UTF-8 计算的 normalized body SHA-256 精确匹配：

- v1.1：container `aa0fdf897acd700cb58c6bc8d744afd6d277e4ae2ec5a3d426999f3fcb55a0ad`；body `68d17dfad6aeda413869d48e26bbda1e7408146ec1ccac9f6615eb4f0577cd6c`。
- v6.7：container `e183d22322e2254f27a380544a27a6d2f2812e38b2a1b1453ac3e43e2997f5e4`；body `bebf91a4313c5ccccbded8cf4b9af928339224ffcadec3e14e182ab523e47b35`。
- Assistant：container `1c7ea1d493ebeb67ed34453e0ab09b6c9726c0d8bb77d94c6539bff3bd55e439`；body `9febcf390b7916b4bf82ae871a47da29e1f620104533f7db7c39fe3883282ce3`。

结论为 `SOURCE_COPY_VERIFIED_BY_NORMALIZED_BODY`。P01—P21 Engine 独立施工、P22 首次 Vio/PWA 生产重连、`model.generate` 继续使用 E5-A 唯一 durable pause/resume、Provider 结果返回原 ThinkSession、模型结果不得直接成为 StateMutation，以及 P10 实际创建 Assistant 分支/独立仓库等语义锚点均命中。

## 2. 定位与权威边界

P02 建立 Engine 内部、宿主中立、可替换的模型执行能力层。模型是 Thinking 可请求的能力，不是 Subject；`SubjectState`、revision、Event、StateMutation、Evolution 和最终主体表达仍由 Engine 权威控制。

唯一结果链保持为：

```text
ContinuityInteractionService
  → CapabilityRequest(model.generate)
  → 原 ThinkSession = WAITING_CAPABILITY
  → Provider Profile / Budget Gate
  → host-neutral ModelProvider Port
  → ProviderExecutionFact
  → CapabilityResult
  → 原 ThinkSession 恢复
  → ThinkingResult
  → Action
  → ReplyComposer
  → FirstRoundSuccessResult
```

P02 没有建立第二套 CapabilityRequest、CapabilityResult、ThinkSession、operation journal 或 completed result ledger。`model.generate` 仍是 Thinking 的直接 Capability 路径，不进入 Planner。Provider 候选不能直接成为最终主体表达，不能写 SubjectState、推进 revision、创建 Event/StateMutation，也不能绕过 Action 或 ReplyComposer。默认表达路径为 `changed=false`。

## 3. 模块与职责

- `domain/model_provider.py`：Profile、执行请求、执行事实、查询结果、dispatch attempt、执行记录与 synthetic usage/test-credit 类型；不包含 SubjectState。
- `services/model_provider_ports.py`：宿主中立 `ModelProvider`、`ModelProfilePolicy` 和 `ModelExecutionRepository` Ports。
- `services/model_profile_policy.py`：显式选择唯一启用 Profile；未知、禁用或启用 fallback 的配置失败关闭。
- `services/model_capability_service.py`：把 Provider 执行事实驱动进现有 E5-A，并负责预算、查询优先、幂等、恢复与 delivery checkpoint。
- `storage/json_model_execution_repository.py`：P02 execution、usage 与 test-credit 的原子 JSON 账本；所有身份和历史只允许单调追加或补齐。
- `interfaces/local_model_capability_app.py`：Engine 本地、依赖注入式装配；不创建网络 transport。
- `testing/fake_model_provider.py`：完全本地、确定性、可脚本化且可查询的 Fake Provider/Fixture；只用于测试，不是真实 Provider Adapter。

`DeferredCapabilityThinkingProvider` 新增可选内部 provider ID，以便 P02 的 ThinkSession 使用 `engine-host-neutral-model-capability`。默认值仍保留旧 E5-A 标识，既有目录和测试兼容行为不变。

## 4. 持久化、幂等与恢复

P02 execution 以 `capabilityRequestId` 确定唯一 execution identity，并保留 `operationId`、`requestId`、`idempotencyKey`、`inputHash` 和 Profile 快照。每次被允许的 dispatch 在 Provider 调用前持久化稳定 attempt identity。Provider 返回后依次持久化执行事实、成功 usage/test-credit、CapabilityResult 和 delivery/completed checkpoint。

进程在 Provider 返回后、Engine execution ledger 落账前退出时，恢复必须先按原 attempt identity 查询 Provider；Fake Provider 可以返回原执行事实。响应已经由 Engine 完成但调用方或 P02 delivery checkpoint 未收到时，恢复使用 E5-A completed result ledger 精确重放，并补齐 P02 delivery/completed 状态。重复 request 不新增 Provider execution、usage、test-credit、Thinking、Action 或主体结果。

`UNKNOWN` 和 timeout 的 retry class 为 `query`，必须查询原 Provider execution；结果仍不确定时保持 UNKNOWN，不得盲目重试。`FAILED_RETRYABLE` 只允许通过显式 retry 入口创建下一 attempt；已发生的成功 execution 不得再次执行。

## 5. 数据、安全与兼容边界

P02 测试只使用临时、独立 Engine data root 和独立 Fake Provider fixture root。本阶段未启动或访问 Vio，未创建 socket/HTTP Provider transport，未读取环境 API Key，也未产生真实 Provider execution 或真实费用。

冻结 `CapabilityResult` 的 `vioLedgerEntryId` 继续存在。P02 使用稳定的 `engine-p02-test-ledger-*` 兼容引用满足冻结字段；它不表示 Vio 调用或 Vio 账本。synthetic usage 账本明确记录 `realProviderUsage=DEFERRED_TO_P22` 和 `realProviderCost=DEFERRED_TO_P22`。

P02 不持久化 Provider 原始隐藏 Chain-of-Thought，只保留结构化候选、Provider/Model 标识、Token usage、合成 test-credit、finish reason、错误/重试分类和审计关联。

## 6. 未完成边界

- 真实 Provider SDK、HTTP/网络、API Key、Credential Broker 和真实账单权威未实现。
- Vio 路由、Vio 预算/费用账本、Vio 进程/仓库/数据/网络和跨仓验收不属于 P02。
- P03 Event/Timeline 已经另获用户授权并于 2026-08-30 正式 `ACCEPTED`；这不改变 P02 的历史验收边界。
- P04、P05、P06 后续分别独立获权并于 2026-09-01、2026-09-03、2026-09-03 正式 `ACCEPTED`；P07 于 2026-09-04 经用户正式验收（D-050），当前 `ACCEPTED`；P09—P23 尚未开始。P10 Assistant、P17 Reality Execution、P20/P21 生产恢复和 P22 生产重连仍不得提前施工。
