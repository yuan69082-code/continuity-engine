# 全量期间的 R1 同项只读核查线索

当前 full-final-01 正在验证 frozen-source.json；源码/测试不动，不重复启动或中断它。

读取 input_outcome 与原 ledger 发现还应定点核实：部分 operation 已有可信准备材料，但完成结果表出现不匹配结果时，是否由于 `operation.domain is None` 跳过 `_verify_core_completed`，只凭完成表存在就返回 COMPLETED。

这是静态线索，尚未实跑，不登记为已复现缺陷。属于本轮 R1 已授权的“身份不匹配、损坏记录、未完成不能冒充完成”边界，不新增业务能力或扩展权限。

待本轮正在运行的全量结束后：保留其完整结果与旧固定源码；在隔离 TEST 中，用真实入站产生部分进度，再构造来自另一合法请求、但与此 operation 不匹配的完成结果，调用正式 input_outcome。检查拒绝且零写入/调用/效果/费用。若复现，在原查询出口做最小拒绝修补，补正式反例；用新的冻结清单与标签验证，不能将旧版本通过当作最终覆盖。

如果原数据读取已经拒绝该矛盾，则如实记录正向防护结果，不凭静态猜想改代码。不要修改正在运行全量对应的源码，也不要覆盖任何已有结果标签。
