# P11 测试索引与独立复核入口

> 状态：P00—P11 = ACCEPTED；P11 / P11 Engine side / P11-01—P11-12 = ACCEPTED；P11 Vio dependency = NONE；P12—P23 = NOT_STARTED；PLANNING_CONFLICT = NONE；EVIDENCE_CONFLICT = NONE（仅表示现行验收阻断已闭合）。D-058 已登记用户正式验收。

> 最新验收：监工独立原两个反例 2/2 PASS、完整 P11 45/45 PASS（8.371 秒），范围与受保护文件 hash 核对通过；用户已确认验收，见[第 9 节](#p11-accepted)。第 2—8 节保留施工和返修历史；修复前全量仍为历史结果。

## 1. 可运行入口

环境：

```powershell
$env:PYTHONPATH='src'
$env:PYTHONDONTWRITEBYTECODE='1'
$env:PYTHONUTF8='1'
```

P11 专项：

```powershell
python -m unittest tests.test_p11_scheduler tests.test_p11_scheduler_recovery -q
```

完整 Engine：

```powershell
python -m unittest discover -s tests -q
```

版本化 Golden 入口：

```python
from continuity_engine.testing import run_p11_golden_scenario

result = run_p11_golden_scenario(independent_temp_root)
```

Golden 使用两项任务、101 秒冻结逻辑推进、两个 WakeSession、零 model/Action 调用，并对稳定结果生成 canonical hash。调用方必须提供独立 Temp 根，不能指向仓库或正式数据。

## 2. 修改前基线

- `main` / HEAD / 本地 `origin/main`：`a905853ca05edd56d9a8b0164818b4388a90b85f`；ahead/behind 0/0；暂存与 tracked diff 为空。
- 完整回归：794 tests，793 PASS、1 SKIP、0 FAIL；unittest 410.432 秒，墙钟 411.338 秒。
- 原 794 项身份与新增 39 项身份精确拆分，loader errors 为 0，见 [test-identities.json](p11_evidence/stable-20260905/test-identities.json)。
- 25 项冻结边界、正式 7 文件、正式树 hash、188 个开工源码/测试文件及 31 个 P10 本地辅助脚本见 [before.json](p11_evidence/start-20260905/before.json)。规划源 hash 见 [planning-sources.json](p11_evidence/start-20260905/planning-sources.json)。

## 3. 首次失败与修复历史

| 阶段 | 真实结果 | 结论与修复 |
| --- | --- | --- |
| 实现前红灯 | 2 ERROR / 0.000 秒 | 两个 P11 测试模块因 `domain.scheduling` 尚不存在而导入失败，证明先测后做；见 `red-20260905/p11-red-first.*` |
| 首版实现 | 30 tests：29 PASS、1 FAIL / 1.078 秒 | TEST crash sentinel 继承普通 RuntimeError，被 Adapter 异常处理捕获；改为 TEST-only hard interruption，不改生产普通异常语义 |
| crash sentinel 第二次 | 1 FAIL / 0.047 秒 | 用例使用未启用 Awakening 的 Fixture 却断言 WakeSession；修正 Fixture 配置，不归因于 Engine |
| ResourceAware 边界红灯 | 1 FAIL / 0.069 秒 | 成功机会只 preview、未经过既有资源分配；接入 `ResourceAwareWakeScheduler`，随后资源 revision/usage/稳定 session 全部通过 |
| P09 组合首次 | 1 FAIL / 0.682 秒 | 断言把 P09 已有 WakeSession 计为 P11 输出；改为验证 P11 增量恰为 1，Engine 实际语义当时已正确 |
| Scheduled reason 红灯 | 1 FAIL / 0.087 秒 | TEST adapter 把 scheduled reason 交给 manual cycle；新增固定 scheduled cycle 并走既有 scheduled wake 边界 |
| UNKNOWN→DELIVERED 红灯 | 1 FAIL / 0.098 秒 | Fake query 从 UNKNOWN 变为 DELIVERED 时未实现其 synthetic wake effect；补齐可信查询结果对应的单次本地效果 |
| UNKNOWN 公平性红灯 | 1 FAIL / 0.054 秒 | 一个长期 UNKNOWN 占住每次 tick；加入确定性 verification backoff，使其他 due task 获得机会，原 attempt 仍只查询不盲重试 |
| 身份清单辅助工具 | 两次 exit 1 | 首次错误使用 `top_level_dir`；第二次漏加仓库根导致 `tests.*` import error。均为证据辅助工具错误，不是 Engine 测试失败；随后以原 discover 上下文得到 0 loader error、794+39 精确身份。原错误 JSON 保留 |

上述日志均在 [implementation-20260905](p11_evidence/implementation-20260905/)；后续 PASS 不倒写这些事实。

## 4. 最终代码验证

| 集合 | 真实结果 | unittest 时间 | 证据 |
| --- | --- | --- | --- |
| P11 专项 | 39 PASS、0 SKIP、0 FAIL | 7.704 秒 | `stable-20260905/p11-specialized-final-code.*` |
| 直接相关：Awakening/Resources/P03/P08/P09/P11 | 238 tests：237 PASS、1 SKIP、0 FAIL | 317.671 秒 | `direct-related-final-code.*` |
| P01—P11 编号阶段综合矩阵 | 475 tests：474 PASS、1 SKIP、0 FAIL | 371.445 秒 | `p00-p11-matrix-final-code.*` |
| Engine 完整稳定轮次 1 | 833 tests：832 PASS、1 SKIP、0 FAIL | 415.083 秒 | `full-round-1.*` |
| Engine 完整稳定轮次 2 | 833 tests：832 PASS、1 SKIP、0 FAIL | 419.188 秒 | `full-round-2.*` |
| Engine 完整稳定轮次 3 | 833 tests：832 PASS、1 SKIP、0 FAIL | 422.227 秒 | `full-round-3.*` |

唯一 SKIP 为既有 `tests.test_p08_fixture_paths.P08FixturePathTests.test_symlink_component_rejected_without_writes`，原因是当前 Windows 环境不授予 symlink 创建权限（WinError 1314）。真实 junction 和 reparse-point 用例均执行通过。SKIP 从未计入 PASS。

过程中的 38/38、237 tests 和 474 tests 结果也原样保存在相同目录；它们属于最后两项公平性/完整性修复之前的过程证据，不冒充最终代码结果。

## 5. 覆盖映射

- `tests/test_p11_scheduler.py`：领域验证、identity/hash、UTC/due、排序、背压、静默、资源零写、防饥饿、损坏仓储、revision、跨绑定和 Fixture 路径隔离。
- `tests/test_p11_scheduler_recovery.py`：稳定 attempt/receipt、UNKNOWN 查询、事实恢复、普通异常、回执冲突、重试上限、取消、重复 tick、Awakening/Resource/Timeline/P09 组合和 Golden。
- `src/continuity_engine/testing/p11_scheduler_fixture.py`：P01 Frozen Clock、受保护 Temp 根、持久 Fake Notification receipt、可选真实 Awakening 组合；零网络、零 Vio、零真实 Provider/Adapter。

## 6. 独立复核重点（验收前历史）

1. Scheduler 记录是否只拥有调度权威，且没有心理/主体/Action/Capability 写权。
2. UNKNOWN、取消、回执冲突和投递后中断是否始终先查原 attempt，且不重复 Wake/资源分配。
3. verification backoff 是否既不盲重试原 UNKNOWN，又不让它饿死其他任务。
4. `ResourceAwareWakeScheduler` 新增可选稳定 sessionId 与 event wake 是否保持原调用兼容。
5. 三轮全量后源码/测试未再改变；档案后终局专项和全量结果应追加在本文件，而不覆盖本节结果。
6. D-058 仍不存在；独立复核通过也不等于用户正式验收。

## 7. 档案后终局复跑

在 P11 实现、四份专责档案及汇总档案完成后，按顺序只运行一次专项和一次全量：

| 集合 | 真实结果 | unittest 时间 | 墙钟时间 | 证据 |
| --- | --- | --- | --- | --- |
| P11 专项终局 | 39 PASS、0 SKIP、0 FAIL | 7.673 秒 | 8.266 秒 | `stable-20260905/p11-specialized-final-archive.*` |
| Engine 全量终局 | 833 tests：832 PASS、1 既有环境 SKIP、0 FAIL | 438.796 秒 | 439.525 秒 | `stable-20260905/full-final-archive.*` |

本次没有失败后重跑。唯一 SKIP 与前三轮相同，仍是既有 P08 symlink 创建权限 `WinError 1314`，未计作 PASS；P11 的真实 junction/reparse、Fake Notification、Golden 和全部 39 项均实际执行通过。

终局只读审计第一次因 Assistant 仓库 ownership/safe-directory 限制产生四个读取辅助错误；第二次又把提示中的“已验证代码提交 `90f112b…`”误当成当前 Assistant HEAD。两份失败 JSON 均保留。最终只读核实确认 `90f112b…` 是当前干净 HEAD `a4d1dd2…` 的真实祖先，后续仅有三个 P10 验收文档变化；未修改全局 Git 配置。最终审计为 PASS，见 `stable-20260905/post-final-audit.json`。

<a id="p11-admission-repair"></a>

## 8. 首次 admission 生命周期定点返修（2026-09-05）

独立复核确认：QUEUED 且 sequence/revision=0 的对象可以预填 attempt、receipt、cancel、reason 等历史后入队；attempt_count=max_attempts=1 的毒化对象随后 tick 会重复抛出 `attempt_count exceeds max_attempts`。上述为用户提供的独立反例；本次先加入正式回归并保存修复前实跑，再修改入口。

代码仅修改 `SchedulerService.submit()` 的现有前置判断：要求 QUEUED、sequence/revision/attempt_count=0、所有运行时间/attempt/receipt/cancel/completed 字段为 None、reason_codes=()。判断位于 load_queue 之前，不修改领域类型、仓储加载、恢复路径、Resource 或 Awakening。

新增 `P11SchedulerAdmissionTests` 六个方法，包含无队列/有队列下的九个历史字段、组合伪造、attempt 上限毒化、原 state/sequence/revision 门，以及合法创建、重复入队、跨重启 RETRY_WAIT→COMPLETED 对照。负向回归同时断言明确 ValidationError、队列零读取/零写入、通知零调用、资源零调用、整个隔离 Fixture 文件清单和字节不变。

| 阶段 | 实际结果 | unittest 秒 | 墙钟秒 | 原始日志 |
| --- | --- | --- | --- | --- |
| 修复前定点 | 6 个方法：4 FAIL、2 PASS；20 条失败记录（含 subTest），0 ERROR、0 SKIP | 0.406 | 0.894 | [red stderr](p11_evidence/admission-repair-20260905/targeted-red.stderr.log) |
| 修复后定点 | 6 PASS、0 FAIL、0 SKIP | 0.335 | 0.831 | [green stderr](p11_evidence/admission-repair-20260905/targeted-green.stderr.log) |
| 完整 P11 专项 | 45 PASS、0 FAIL、0 SKIP（原 39 + 新增 6） | 7.529 | 8.024 | [P11 stderr](p11_evidence/admission-repair-20260905/p11-specialized.stderr.log) |

命令分别为 `python -m unittest tests.test_p11_scheduler.P11SchedulerAdmissionTests -v` 和 `python -m unittest tests.test_p11_scheduler tests.test_p11_scheduler_recovery -v`；环境仍为 PYTHONPATH=src、PYTHONDONTWRITEBYTECODE=1。命令、时间、退出码和独立 stdout/stderr 保存在同目录 JSON/日志，原两文件修复前副本亦保留。没有隐式重试、没有全量复跑、没有覆盖首版 39/833 或其他失败历史。

缺口在独立发现及红灯时构成 EVIDENCE_CONFLICT=PRESENT；本地修复和专项通过后现行 EVIDENCE_CONFLICT=NONE，只表示当前本地反例闭合，未取得返修独立复核通过或用户验收。PLANNING_CONFLICT=NONE；P11 保持 IMPLEMENTED_NOT_ACCEPTED，D-058 未创建、未使用。

[本次范围与边界审计](p11_evidence/admission-repair-20260905/audit.json)：源码/测试仅两文件变化，其余 189 文件 hash 不变，原 39 项测试保留；25 项冻结边界、正式 7 文件及 31 个既有 P10 辅助脚本的 63 项 hash 全同。版本 0.1.0，AST、敏感内容与 git diff --check 通过。Engine main/HEAD/本地 origin/main 仍为 a905853ca05edd56d9a8b0164818b4388a90b85f，ahead/behind=0/0、暂存区空；本次未访问 Assistant、未执行 Git 写操作。

<a id="p11-accepted"></a>

## 9. 用户正式验收及档案归档（2026-09-05，D-058）

用户明确确认 P11 本轮独立复核通过并正式验收。监工独立验证为：原两个反例 2/2 PASS；完整 P11 45/45 PASS，8.371 秒；修改范围及受保护文件 hash 核对通过。这些是用户提供的独立复核结果，本次归档没有运行测试，未给原两项补造耗时。

P00—P11 = ACCEPTED；P11 / P11 Engine side / P11-01—P11-12 = ACCEPTED；P11 Vio dependency = NONE；P12—P23 = NOT_STARTED；PLANNING_CONFLICT = NONE；EVIDENCE_CONFLICT = NONE（仅表示现行验收阻断已闭合）。首次 admission 入队阻断已关闭。D-057 保留开工/返修事实并引用 D-058；十二项矩阵与现行档案同步 ACCEPTED。全部历史失败、修复、辅助错误、SKIP 和 P09 segment 10 根因 UNKNOWN 保留。第 4/7 节及其他旧文中的 39/833 全量结果属于 admission 修复前历史，不作为当前新全量证据。

归档前 191 个源码/测试文件与已复核返修清单精确一致，63 项受保护文件 hash 一致。本次仅修改档案，不修改源码/测试、Assistant、冻结契约、正式数据或 0.1.0，不进入 P12，不执行 Git 写操作。P11 已验收成果仍未提交；HEAD a905853ca05edd56d9a8b0164818b4388a90b85f 仅为开工基线，不是最终 P11 SHA。

[归档核查](p11_evidence/acceptance-20260905/archive-audit.json)包含文档、来源和保护边界结果；[准确待提交与排除清单](p11_evidence/acceptance-20260905/pending-files.md)逐项区分 P11 成果、历史证据和31个应保留但排除的 P10 本地辅助脚本。
