# 待确认事项

> P08 现行门：P00—P08 = `ACCEPTED`；P08 / Engine side / P08-01—P08-12 = `ACCEPTED`；P08 Vio dependency = `NONE`；P09—P23 = `NOT_STARTED`。D-051 保留施工决定，D-052 已登记用户正式验收；不授权 P09 或 Git 操作。见 [P08 矩阵](44_P08_规划施工测试验收矩阵.md)。

> 本文件记录已在路线或接口中出现、但尚未获准作为当前实现范围的能力。它们不是“已经完成”，也不是“永久拒绝”；开发前必须取得明确批准并补充架构决定。

## P00—P08 已验收、后续授权门

- 用户已于 2026-08-25 正式验收 P00 的 Source of Truth、P00—P23 顺序、D-035、冻结接口、恢复边界和验收矩阵；P00 当前为 `ACCEPTED`。
- 用户已于 2026-08-27 正式验收 P01，确认 P01、P01 Engine side 与 P01-01—P01-12 均为 `ACCEPTED`；成功自动清理、失败保留 24 小时、显式调试最长 7 天、支持手动清理且不建设后台 Scheduler 的边界继续有效。
- P01 已从当前待确认范围移出。P01 Vio dependency = `NONE`；Vio/PWA production integration 的 Stage Brief 与跨仓验收统一留到 P22。
- 用户于 2026-08-29 正式验收 P02；P02、P02 Engine side 与 P02-01—P02-12 当前均为 `ACCEPTED`。P02 Vio dependency = `NONE`，真实 Provider integration = `DEFERRED_TO_P22`。P02 已从当前待确认范围移出。
- P03 已于 2026-08-30 由用户正式验收，P03、Engine side 与 P03-01—P03-12 当前均为 `ACCEPTED`。
- 用户于 2026-09-01 正式验收 P04；P04、Engine side 与 P04-01—P04-12 当前均为 `ACCEPTED`，P04 Vio dependency = `NONE`，D-044 已记录验收决定。P04 已从当前待确认范围移出。
- 用户于 2026-09-03 正式验收 P05，D-046 已记录该决定；P05、Engine side 与 P05-01—P05-12 当前为 `ACCEPTED`，Vio dependency = `NONE`，不再属于待确认事项。
- 用户已于 2026-09-03 正式验收 P06，D-048 记录该决定；P06、Engine side 与 P06-01—P06-12 当前均为 `ACCEPTED`。
- 用户于 2026-09-04 正式验收 P07，D-050 已创建并使用；P07、Engine side 与 P07-01—P07-12 当前均为 `ACCEPTED`，不再列为待验收项。未提交、未推送成果由用户自行决定 Git 操作。P09—P23 的用户决定入口仍集中登记在 [12_P00_规划施工测试验收矩阵.md](12_P00_规划施工测试验收矩阵.md) 与 [14_P00_风险回滚与用户决策入口.md](14_P00_风险回滚与用户决策入口.md)，P07 验收不自动授权 P08。 P08 随后通过独立授权施工并已正式验收（D-052），不再待验收。

P08 已经规划监工独立复核并由用户于 2026-09-04 正式验收（D-052），不再列为待验收事项。方案 A 仅限内部同通道泛化，不扩大为生产 exactly-once、生产 Adapter 或 P17 Execution Engine。本次接续补齐的验收档案不构成新的待确认或待验收项；当前仅等待用户另行决定 Git 操作及后续阶段授权，P09—P23 均未开始，本次验收不授权 P09。

## 已由 v1.1 确认、不再作为契约疑问的事项

截至 2026-07-30，以下内容已经通过 `Continuity Integration Contract v1.1` 和 Continuity Engine 最终短确认闭合，应以 `04_决策记录.md` 的 `D-025` 为准，不再作为开放契约问题：

- Vio 与 Continuity Engine 的平行系统关系及 SubjectState 唯一权威。
- PlatformObservation、Vio Event、摄取回执与内部 Event/StateMutation 的隔离。
- 第一轮严格 Schema registry、固定 SubjectBinding、幂等错误码、revision 冲突和最小投影语义。
- 第一轮使用独立 `ContractTestAdapter`，不经过 Capability 协议、`APIService.submit_message`、本地 HTTP 或 `UserInteractionService`。
- 双方工程档案同步和引擎定点文档修正已经完成，最终只读复核已经通过，双方档案一致。
- 当前不存在阻塞第一轮最小连接施工提示词制定的档案冲突。
- Engine E1/E2/E3、test-only JSONL Runner 和第一轮双方 test-only 端到端共享验收已经完成；真实请求、结果、投影、幂等、revision、四类错误及双方重启恢复均已闭合。

上述“已确认”既包括契约决定，也包括第一轮 test-only 及正式本地回环运行时验收：E1、E2、E3 与 Runner 已由 Engine 实现，Vio 测试端已实际调用并持久化结果和投影；Engine E4 完成正式本地服务和 crash-recovery 定点修复，Vio V3 × Engine E4 的 S2/S3 已通过。这些完成状态仍不表示生产连接已经实现。

## S4-Live 后续待决定事项（P02 已闭合本地 Fake 范围；真实接入归入 P22）

- 是否以及何时把首次可销毁测试身份的单次验收扩展为本机个人日常使用；不得复用或晋升本次测试主体。
- 通用正式身份/Binding、真实 Provider、API Key secretRef、可信预算和停止条件的生产配置；Engine 不接收密钥。P02 的 Fake Profile/预算已经固定，不替代本项。
- 更多真实网络、认证、返回格式、usage、故障恢复、真实 `UPDATE_STATE` 与可信费用的现场验收边界。
- 固定本地受控 V5/F1 如何演进为生产公共对话 API 与前端链路。
- 生产 Integration Adapter、传输、认证、多租户和部署参数。

Engine E1—E5-A、两轮第一阶段验收、Vio V4、受控 S4 Capability 联验、Vio V5、F1、L1、Engine S4-R `PASS` 和首次 S4-Live 单次验收均已完成，不再属于待确认事项。首次 S4-Live 只确认不可晋升身份和独立沙箱中的一次真实 Provider 成功，不自动授权重复调用、日常正式使用、生产形态 Integration Adapter、生产认证、多租户或部署。

## 1. Execution Engine

**已提出内容：** 执行 ActionPlan，调用工具、联系用户或处理外部副作用。

**当前状态：** 未批准进入当前版本；Action 阶段明确禁止真实执行。

**待确认：** 执行权限模型、确认流程、幂等、失败/补偿、审计、沙箱和回滚边界。

## 2. 真实 AI API

**已提出内容：** 让 GPT、Claude 或本地模型实现 ThinkingProvider，未来也可能辅助 Perception 或 Learning。

**当前状态：** 可插拔接口、E5-A、Vio V4、受控 S4、V5/F1/L1、S4-R 和首次 S4-Live 均已完成各自范围；P02 Engine 内宿主中立 Port、可替换 Profile、Fake Provider、synthetic 预算/usage/test-credit 与故障/恢复矩阵已由用户正式验收。真实 SDK/网络/凭据/可信费用、更多模型现场验证、真实 `UPDATE_STATE` 和生产治理仍后置 P22。

**待确认：** 首选 Provider、数据出境、隐私、超时/重试、输出校验、模型降级和成本策略。

## 3. 真实 MCP 连接

**已提出内容：** 通过 MCP 读取状态、查询记忆、感知、思考和行动计划。

**当前状态：** 只有 `ContinuityMCPAdapter` 接口设计。

**待确认：** MCP server/client 角色、工具暴露范围、鉴权、权限映射、资源计费和错误协议。

## 4. 外部长期记忆库

**已提出内容：** 通过 MCP、向量库或其他存储提供长期记忆检索。

**当前状态：** Memory 管理层只有检索和影响接口，不保存长期记忆。

**待确认：** 数据所有权、删除/更正、召回策略、相关性阈值、敏感信息、去重和迁移。

## 5. 主动联系用户

**已提出内容：** CONTACT_USER 计划最终转为真实消息。

**当前状态：** 只能生成计划；不能发送。

**待确认：** 渠道、频率、安静时段、用户确认、撤销、内容审核和失败处理。

## 6. 真实外部工具调用

**已提出内容：** USE_TOOL 计划最终调用浏览器、邮箱、日历、设备或其他工具。

**当前状态：** 只能生成计划；不能调用。

**待确认：** 工具白名单、参数校验、最小权限、沙箱、确认、结果可信度和副作用恢复。

## 7. 常驻自主运行与自主唤醒

**已提出内容：** 定时或持续后台唤醒，甚至根据事件自动运行。

**当前状态：** 有 WakeCycle/资源感知调度框架，没有常驻守护进程。

**待确认：** 生命周期、频率上限、关停开关、资源配额、异常熔断、时区和用户可见性。

## 8. 情绪模拟

**已提出内容：** 让主体拥有模拟情绪或情绪驱动行为。

**当前状态：** 当前阶段多次明确禁止；`emotion_state` 字段不等于情绪模拟系统。

**待确认：** 是否符合项目目标、与 Perception/Personality 的边界、用户误解风险和安全约束。

## 9. 自动永久人格改变

**已提出内容：** 系统自行学习并永久改变 PersonalityTrait。

**当前状态：** 未批准。当前学习必须多证据、验证、明确确认，并可回滚。

**待确认：** 人工确认责任、可解释性、错误恢复、冻结特征和策略版本。

## 10. 模型训练或参数更新

**已提出内容：** 通过经历训练模型或修改权重。

**当前状态：** 不属于 continuity-engine 当前定义，明确未实现。

**待确认：** 若未来提出，应作为独立项目评估，而不是 LearningService 的自然延伸。

## 11. 真实平台权限

**已提出内容：** 手机、浏览器、邮箱、日历、MCP 和工具权限接入。

**当前状态：** PermissionState 与 PermissionProvider 只处理内部状态/测试判断。

**待确认：** 平台授权协议、过期/撤销同步、scope 映射、凭据存储和重新确认机制。

## 12. 用户认证、多租户与支付

**已提出内容：** 登录用户 ID、多主体、资源购买或外部支付。

**当前状态：** 仅预留 subject_id；没有认证、租户或支付系统。

**待确认：** 身份模型、主体与用户映射、数据隔离、账单归属和合规要求。

## 13. SSE / WebSocket

**已提出内容：** 前端实时事件和状态推送。

**当前状态：** 仅作为未来扩展预留，未实现。

**待确认：** 事件类型、重连、顺序、鉴权、资源限制和是否确有产品需求。

## 14. 更完整的 Perception 层

**已提出内容：** 在当前确定性感知框架上增加更丰富的环境、关系、时间与冲突理解。

**当前状态：** v0.1 Perception 框架已完成；扩展范围未批准，也没有真实外部感知输入。

**待确认：** 新感知来源、隐私、可信度、与情绪模拟的边界，以及是否需要模型参与。

## 15. 多模型协作

**已提出内容：** 让多个 GPT、Claude、本地模型或专用模型共同提供思考、审查或降级能力。

**当前状态：** 未实现；现有 ThinkingProvider 只提供模型可替换边界，不等于多模型协作。

**待确认：** 路由策略、角色分工、冲突裁决、成本、隐私、失败降级和可重复性。

## 16. 知识库系统

**已提出内容：** 建立区别于主体记忆的知识检索与管理能力。

**当前状态：** 未实现；Memory 管理层接口不等于知识库系统。

**待确认：** 知识与记忆的边界、数据来源、更新、版本、引用、纠错和删除机制。

## 17. 用户关系长期变化模型

**已提出内容：** 对关系距离、长期关注、未完成交流和互动模式进行更完整的长期建模。

**当前状态：** SubjectState 和 Perception 已包含关系相关字段/观察，但独立的长期关系变化模型未实现。

**待确认：** 可解释规则、证据周期、隐私、用户控制、回滚和与 Learning/Evolution 的边界。

## 18. 完整多主体支持

**已提出内容：** 在同一系统中注册、隔离、运行和管理多个 subject_id。

**当前状态：** 多处模型和接口预留 subject_id，但没有完整主体注册、隔离和生命周期系统；也不等于多租户能力已经完成。

**待确认：** 主体与用户映射、权限继承、数据隔离、资源归属、删除和跨主体交互规则。
