# 架构决策记录

> 本文件记录由当前实现可确认的重要决定。未来修改决定时，应追加新记录并注明替代关系，不应静默覆盖历史。

## D-001：采用 domain / services / storage 分层

**决定：** 领域模型和规则放在 `domain`，业务编排放在 `services`，持久化实现放在 `storage`；外部连接另置于 `interfaces` 和 `frontend`。

**原因：** 保持核心规则不依赖具体存储、平台或 UI，便于测试和替换。

**影响：** 新功能必须先判断属于规则、用例、存储还是入口，不允许跨层偷写状态。

**未来是否可修改：** 可演进目录组织，但依赖方向和职责隔离不应在无架构评审时改变。

## D-002：SubjectState 是连续性的中心状态对象

**决定：** 主体当前状态由 SubjectState 及其 revision 表达，包含 identity、relationship、continuity、temporal、intentions 和 emotion_state 六部分。

**原因：** 需要稳定、可序列化、可比较的当前状态快照。

**影响：** 其他模块观察或提出变更，但不各自维护一份竞争性的主体状态。

**未来是否可修改：** 字段可通过兼容迁移扩展；不能无迁移地改变已有序列化语义。

## D-003：正式状态变化只能经过 Event → Evolution

**决定：** Permission、Learning、Thinking 或 Action 均不能直接修改 SubjectState；批准的变化先生成 Event/StateMutation，再由 Evolution 应用。

**原因：** 保留原因、来源、前后差异和可审计历史。

**影响：** 看似简单的字段更新也必须遵守演化入口。

**未来是否可修改：** 不建议修改；这是核心不变量。

## D-004：使用 revision 和 expected_revision 防止覆盖

**决定：** 状态更新和行动评估校验输入 revision。

**原因：** 防止基于旧状态生成的变化覆盖新状态。

**影响：** 调用方必须携带自己看到的 revision，并处理冲突。

**未来是否可修改：** 可替换为更强的并发机制，但不能取消并发保护。

## D-005：Memory 管理层不拥有长期记忆存储

**决定：** Memory 负责检索请求、相关性判断和影响记录，长期保存通过可插拔 Retriever/Recorder 或未来 MCP 完成。

**原因：** 避免核心绑定某个向量库、平台或记忆产品。

**影响：** 当前不能把 MemoryRetrievalResult 等同于已存在的长期记忆数据库。

**未来是否可修改：** 可增加适配器，不建议把具体外部存储实现放入领域核心。

## D-006：每次唤醒都形成 WakeSession

**决定：** 手动、定时或事件唤醒均记录原因、时间、输入 revision、所见上下文、最终决策和成功状态。

**原因：** 自主运行必须可追溯，而不是无记录的后台行为。

**影响：** Wake 是后续 Perception/Thinking/Action 会话的追踪起点。

**未来是否可修改：** 可增加原因类型和指标；不可取消基础审计。

## D-007：Perception 是 Thinking 唯一直接输入层

**决定：** 主流程固定为 Wake → Perception → Thinking，Thinking 不再直接读取 WakeContext 或数据库。

**原因：** 分离“观察到什么”和“如何判断”，减少耦合。

**影响：** 新的感知信息应先进入 PerceptionResult，再供 Thinking 使用。

**未来是否可修改：** 若需旁路输入必须架构评审；默认保持。

## D-008：Perception 只读

**决定：** Perception 只能观察、理解和生成感知，不能修改 SubjectState。

**原因：** 感知不是行动，也不应暗中产生状态副作用。

**影响：** 感知结果导致的变化仍需 Thinking → Action → Evolution。

**未来是否可修改：** 不建议修改；这是核心边界。

## D-009：ThinkingProvider 可插拔且不保存完整思维链

**决定：** Thinking Engine 通过 Provider 协议调用未来模型，只保存思考过程摘要、输入概况和结果。

**原因：** 避免绑定 GPT、Claude 或本地模型，并控制隐私与日志风险。

**影响：** 模型接入必须实现标准 Provider；日志不得写入模型完整内部推理。

**未来是否可修改：** Provider 可扩展；摘要审计原则应保持。

## D-010：Action 只决定和规划，不执行

**决定：** ActionIntent 不是最终批准，ActionDecision 和 ActionPlan 也不是执行结果；计划状态不包含 EXECUTED。

**原因：** 决策与真实外部副作用必须分离。

**影响：** CONTACT_USER、USE_TOOL、REQUEST_MEMORY 当前只生成计划，不得伪造成功。

**未来是否可修改：** Execution Engine 可作为独立阶段新增，但不得混入 ActionService。

## D-011：Action 必须经过权限、风险和资源评估

**决定：** 缺少/撤销/过期/越界权限会阻止或拒绝；HIGH/CRITICAL 风险不能自动批准；资源使用确定性估算。

**原因：** 让行动决定受约束、可复现、可测试。

**影响：** 同一输入应产生确定性结果，所有拒绝、阻止和延后必须有原因。

**未来是否可修改：** 规则可版本化增强，最低安全规则不能静默弱化。

## D-012：权限连续层与行动时 PermissionCheck 分离

**决定：** PermissionState/History 表达“长期拥有什么权限以及如何变化”；Action PermissionProvider 表达“本次行动是否可获准”。

**原因：** 一次判断不能替代权限生命周期和来源审计。

**影响：** Action 通过 PermissionContext 获取当前权限视图，PermissionService 不执行行动或修改 SubjectState。

**未来是否可修改：** 可统一适配接口，但不能丢失历史层与评估层的语义区别。

## D-013：学习必须经证据、验证、确认和 Evolution

**决定：** 单次事件不能改变长期特征；候选需多次独立一致证据、置信度阈值和验证，固化/回滚通过 Event → Evolution。

**原因：** 防止偶然事件或错误判断永久改变人格。

**影响：** LearningService 只管理候选、证据和建议，不训练模型，也不直接写状态。

**未来是否可修改：** 阈值和验证策略可版本化；禁止单次自动永久固化的原则应保持。

## D-014：主动运行最终必须收口到资源检查

**决定：** Wake、Thinking 和 Learning 在启动高成本工作前应向 ResourceManager 申请预算；新增主动运行入口不得继续绕过资源检查。

**原因：** 防止无限自主运行，并为未来模型调用提供统一约束。

**影响：** 当前已经存在 ResourceManager、Thinking 预算连接、Learning 可选连接和 ResourceAwareWakeScheduler，但 AwakeningService 仍可被直接调用，Learning 的资源管理器也可不配置。因此统一强制入口属于尚未完成的工程约束。资源不足时采用降低深度、延迟或等待，而不是无限透支。

**未来是否可修改：** 可接真实计量，但保留预算与追踪机制。

## D-015：外部入口不得绕过 PermissionContext 和 ResourceManager

**决定：** API、MCP、Skill 和前端调用通过接口安全门进入核心。

**原因：** 适配器不应成为绕过权限或资源规则的后门。

**影响：** 新入口必须复用统一访问检查，不得直接访问仓储修改状态。

**未来是否可修改：** 安全实现可增强；检查原则不可取消。

## D-016：前端只负责输入、展示和通信

**决定：** 浏览器端不保存 SubjectState、记忆、人格，不执行权限判断或自主逻辑。

**原因：** 保持核心唯一事实源，并允许前端替换。

**影响：** 前端调试视图是服务端状态投影，不是本地状态副本。

**未来是否可修改：** 可增加 UI 与缓存，但核心状态和授权决定不能下放到不可信前端。

## D-017：本地演示首次初始化必须显式进行

**决定：** 本地前端通过 `--initialize` 明确创建初始数据，后续正常启动不隐式重置。

**原因：** 避免演示入口在不知情时覆盖连续状态。

**影响：** 首次使用命令与日常启动命令不同。

**未来是否可修改：** 可改为引导式初始化，但不得静默重建已有状态。

## D-018：兼容旧编排入口

**决定：** 保留 `wake_perception_thinking_service.py`，同时提供完整 Action 编排入口。

**原因：** 避免无理由删除已有公共接口。

**影响：** 当前兼容服务引用内存 Action 仓储，是已知分层例外；未来需明确弃用周期后再清理。

**未来是否可修改：** 可以，在提供迁移说明、弃用版本和测试后删除。

## D-019：Continuity Engine 位于 AI 模型之外

**决定：** Continuity Engine 是独立于 GPT、Claude、本地模型等具体模型的连续性层。引擎负责身份、主体状态、记忆影响、时间、感知、权限、演化与自主行为约束；模型未来负责语言理解与生成。

**原因：** 主体连续性不能绑定某个模型、一次对话或某个平台，否则更换模型或入口会破坏长期状态。

**影响：** 模型必须通过可插拔 Provider/Adapter 接入；SubjectState、演化记录和权限资源规则仍由引擎持有。Vio 平台后端与 Continuity Engine 是边界独立、数据库独立的平行系统，通过正式版本化契约协作；Vio 前端只连接 Vio 平台后端，模型、Tool、MCP 和设备也不是主体状态权威。

**未来是否可修改：** 不建议修改；这是工程总档案 v1.0 的项目定位。

## D-020：以实时状态闭环作为模型接入目标

**决定：** 真实模型接入后的标准交流路径必须经过 Action Gate：`PlatformObservation / 合法内部触发 → 读取 SubjectState、Memory 与允许使用的事实 → Perception → Thinking → 模型结果返回引擎并继续进入 Thinking / Action → ActionDecision`。未批准 `UPDATE_STATE` 时不创建状态变更且 revision 保持不变；只有 Action 合法批准 `UPDATE_STATE`，引擎才能创建内部 Event/StateMutation，并通过 Evolution 更新 SubjectState 与推进 revision。

**历史背景：** 本决定最初以“模型生成回复 → 生成 StateMutation → Evolution”概括实时闭环。该表述在 Action Gate 尚未明确时用于说明状态需要在交互之间连续，但可能被误读为模型回复必然改变主体状态；此旧语义现已被 `Continuity Integration Contract v1.1`、D-025 和本次定点修正取代。

**原因：** 每次交互都应继承连续状态，但回复生成和状态演化是有关联而不强制绑定的两个结果。模型文本不是受信状态命令；只有经过 Thinking、Action、权限/风险/资源约束和 revision 校验的合法更新，才能进入正式演化。

**影响：** 模型文本不能直接成为 StateMutation，模型回复不意味着主体状态必然变化。当前 v0.1 已完成状态、Memory 接口、Mutation/Event、Evolution、Action Gate 和持久化框架；Vio V4、受控 S4 与首次 S4-Live 已验证模型候选作为 CapabilityResult 回流原 Thinking/Action 的能力通道。首次真实试聊没有获批 `UPDATE_STATE`，故 `changed=false`、revision 保持 0；真实模型触发合法状态演化的路径仍未验收。

**未来是否可修改：** 可扩展上下文来源和编排细节，但 Action Gate 以及 StateMutation/Evolution 的正式写入边界不可绕过。

## D-021：框架完成与真实能力接入分别记录

**决定：** 工程档案中的“框架完成”只表示模型、规则、服务、仓储或适配接口已经存在并经过当前测试；真实模型、MCP、外部记忆、外部执行、后台运行、平台权限和真实计费必须单独标记为未完成，直到实际接入并验收。

**原因：** Provider、Adapter 或接口预留容易被误读为外部能力已经可用。

**影响：** 路线图、模块清单、CHANGELOG 和交接说明必须同时写明内部完成范围与外部缺口。

**未来是否可修改：** 状态可在真实能力完成验收后更新，但不得提前声明完成。

## D-022：数字生命系统是长期愿景，不是 v0.1 能力声明

**决定：** 将 Continuity Engine 的长期方向定义为位于 AI 模型之外、能够独立持续运行的数字生命系统；该愿景用于指导身份、人格、记忆、关系、时间、意向、判断、成长和行动能力的长期架构，不作为当前 v0.1 已完成能力或发布验收结论。

**原因：** 项目的目标不是只保存聊天历史或在每次对话中重新生成角色，而是让过去经历能够持续影响当前状态和未来选择；同时必须避免把愿景误写为已经存在的自主运行能力。

**影响：** 所有工程档案必须同时标注长期愿景与当前边界。独立常驻运行、完整时间/环境感知、自主内部思考、主动表达和真实行动在完成接入与验收前均属于未来规划。

**未来是否可修改：** 愿景可由项目所有者重新确认或细化；任何改变不得静默改写当前能力事实。

## D-023：引擎、前端、业务后端与模型工具职责分离

**决定：** Vio 前端是数字生命接触外界和表达结果的窗口，只连接 Vio 平台后端；Vio 平台后端负责用户、会话、平台事实、现实权限与安全确认、模型/工具/设备能力通道、托管和展示。Vio 平台后端与 Continuity Engine 是边界独立、数据库独立的平行系统，通过正式版本化契约协作。Continuity Engine 持有 AI 主体身份、连续状态、记忆影响和主体演化，是 SubjectState 的唯一权威；AI 模型、Tool、MCP 和设备只是可调用的外部能力。

**原因：** 连续性不能由某个界面、业务平台或模型会话定义，否则替换载体或能力提供方会破坏主体连续。

**影响：** Vio 前端不得绕过 Vio 平台后端直接连接引擎，Vio 也不得把状态投影回灌为权威状态；外部能力结果不能直接定义或修改 SubjectState。当前本地 HTTP 和调试前端不等于正式产品前端，仓库也不包含 Vio 平台后端或正式连接实现。

**未来是否可修改：** 外部系统的实现与通信协议可以替换；引擎作为连续性中心的职责不应在无架构评审时改变。

## D-024：正式持续运行采用独立核心服务形态

**决定：** 未来正式运行时，Continuity Engine 应保持独立进程、状态存储、时间系统、唤醒机制和接口，并通过正式版本化契约与 Vio 平台后端通信。Vio 将通过身份、权限和安全筛选的平台事实作为无状态修改权限的 PlatformObservation 交给引擎；引擎通过 Vio 管理的通道请求模型、Tool、MCP 或设备能力，结果必须返回引擎继续进入 Thinking / Action。只有 Action 合法批准 `UPDATE_STATE` 时，引擎才创建内部 Event/StateMutation 并经 Evolution 更新 SubjectState；否则不发生状态变化且 revision 保持不变。表达结果或受控行动计划再由 Vio 托管并交给前端呈现。

**原因：** 只有让外部结果回到连续性中心并经过认知与 Action Gate，才能使模型、工具和载体变化时仍保持身份与生命历程连续，同时避免把模型文本或执行结果误当成必然状态变化。

**影响：** 当前进程内 API、本地 HTTP、适配器接口和单次编排只是未来部署的基础，不代表独立常驻服务、Vio 契约通信、真实模型/工具调用或自主运行已经完成。

**未来是否可修改：** 具体传输技术可按规模调整，但 Vio 与引擎的平行系统关系、正式版本化契约、结果回流引擎、Action Gate 和正式状态变化经 Evolution 的边界必须保持。

## D-025：正式接受 Continuity Integration Contract v1.1

**决定：** Continuity Engine 于 2026-07-30 通过《Engine Contract Final Read-Only Short Confirmation v1》正式接受 `Continuity Integration Contract v1.1`。长期架构和第一轮机器契约语义已经闭合；双方工程档案同步、引擎定点文档修正和双方最终只读复核均已完成，双方档案一致。该次接受本身只授权双方共同制定施工提示词，不曾把契约闭合误写为代码能力；后续 Engine E1 的施工授权和完成状态由 D-026 单独记录。

**审核历史：**

1. 《Engine Integration Contract Response v1》完成首次边界审核并提出修订要求。
2. 《Engine Contract Second Review Response v1》因七个第一轮契约阻塞项给出“暂不接受”的阶段结论。
3. 《Engine Contract Final Read-Only Confirmation v1》确认长期架构一致，但因四类机器契约精度问题继续给出“暂不接受”的阶段结论。
4. Vio 完成最后文档校准后，《Engine Contract Final Read-Only Short Confirmation v1》独立复核通过；此前“暂不接受”继续作为真实历史快照保留，当前唯一有效状态为正式接受 v1.1。

### 长期权威与系统边界

- Vio 与 Continuity Engine 是两个平行系统，数据库保持独立。
- Vio 前端只连接 Vio 平台后端，不得绕过平台后端直接连接 Continuity Engine。
- Continuity Engine 是连续性主体和 SubjectState 的唯一权威；Vio 只保存投影、缓存和审计材料。
- Vio 负责用户、助手、会话、平台事实、现实权限与安全确认、外部能力通道、托管和展示。
- PlatformObservation、Vio Event 和摄取回执均不是引擎内部 Event，不得携带或生成 StateMutation，也不能推进 SubjectState revision。
- 模型、Tool、MCP 或设备结果必须返回引擎继续进入 Thinking / Action；只有 Action 合法批准 `UPDATE_STATE`，正式状态变化才可经过引擎内部 `Event / StateMutation → Evolution → SubjectState`。没有获批状态更新时 revision 保持不变。
- 三层数据空间边界已确认，但 User Private Data、AI Private Space 和 Shared Space 的实际跨系统读写不参加第一轮测试。

### 第一轮 Schema registry

第一轮 registry 只注册三份 JSON Schema Draft 2020-12：

1. ContinuityInteractionRequest：`urn:vio-live:continuity-integration:schema:request:first-round-v1`
2. message_created PlatformObservation：`urn:vio-live:continuity-integration:schema:platform-observation:message-created:first-round-v1`
3. message_version fact：`urn:vio-live:continuity-integration:schema:platform-fact:message-version:first-round-v1`

顶层请求中的两个 `$ref` 必须分别精确解析到第 3 和第 2 个 URN。禁止文件相对路径、网络回退或双方自行定义别名。所有对象使用 `additionalProperties: false`；验证器必须启用 `format: date-time` 断言，第一轮时间只接受以大写 `Z` 结尾的 RFC 3339 UTC。`facts` 与 `observations` 均固定为恰好一项，消息正文只允许出现在唯一 `message_version` fact 的 `content` 中。

正式 conformance vector、三处 identity、conversation/message/version 引用、唯一 observationRef 和正文位置必须同时通过 Schema 与交叉字段校验。

### 固定哈希与 SubjectBinding fixture

三项最终一致性哈希为：

- `contentHash`：`sha256:2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824`
- `requestHash`：`sha256:ec07ad9ba66d1ffcdfa9177cd61bec1b880ad6ee99a6ec6449e732c1b86002d0`
- `bindingFixtureHash`：`sha256:c75b72194c0158a549f3fb30f04a5147ea11a4e777cb1a9cc1a54da6b93359f6`

`requestHash` 只排除顶层 `requestHash` 字段本身，其余允许的逻辑请求字段全部参与 RFC 8785 规范化、UTF-8 编码和 SHA-256。固定 SubjectBinding fixture 使用 `bindingId=binding-001`、`userId=user-001`、`assistantId=assistant-001`、`subjectId=subject-001`、`bindingVersion=1`、`status=active`、`createdAt=effectiveAt=2026-07-30T00:00:00Z`、`replacedBindingId=null`；双方必须装载相同值，不得分别重新生成。`bindingFixtureHash` 排除自身及所有 hash 字段后按同一算法计算，比较规范化结果而不是原始 JSON 字节、字段顺序、缩进或换行。

### 幂等、revision 与投影

- Schema、requestHash 和完整 SubjectBinding 验证通过后，相同 `requestId` 对应不同 `requestHash` 时，第一轮唯一错误是 `IDEMPOTENCY_KEY_REUSED / never`；`IDEMPOTENCY_CONFLICT` 不得作为别名。
- 完整 SubjectBinding 验证通过后，只要 `expectedEngineRevision != currentEngineRevision`，无论较大或较小，都返回 `REVISION_CONFLICT / reassemble`；绑定验证失败时不得泄露当前 revision。
- 投影内容唯一键为 `subjectId + currentRevision`；请求与投影关联键为 `requestId`；`engineUpdateId` 仅在非 `null` 时唯一。
- `changed=false` 时 revision 不变且 `engineUpdateId=null`；多个无变化请求可以关联同一 revision 的同一投影。
- `changed=true` 时 revision 只增加一次，`engineUpdateId=StateUpdateRecord.update_id`。
- 第一轮只使用最小 snapshot，不使用 delta；同一 `subjectId + currentRevision` 不得存在内容、`contentHash` 或 `stateHash` 不同的第二份投影。

### 第一轮运行边界

- `DeterministicThinkingProvider`、`MemoryRetriever` 和 `ReplyComposer` 三个 test double 全部位于 Continuity Engine 进程内。
- 第一轮不产生 `CapabilityRequest` 或 `CapabilityResult`，Capability 协议不参加第一轮；不调用真实模型、Tool、MCP 或设备。
- 当前 `APIService.submit_message`、本地 HTTP 和 `UserInteractionService` 只能作为历史能力证据，不能成为第一轮 PlatformObservation 摄取入口；`APIService` 只可复用只读查询或内部核心辅助能力。
- 第一轮必须新增独立、test-only、进程内 `ContractTestAdapter`；它不得把 PlatformObservation、Vio Event 或摄取回执直接转换为内部 Event。

**原因：** 三轮审核及最终短确认已经消除了会导致双方写出不同第一轮实现的 Schema、Binding、幂等、revision、投影、Provider 和 Adapter 入口歧义，同时保持主体性、连续性和状态权威不被平台连接削弱。

**影响：** 契约设计、机器语义、正式接受、双方工程档案同步、定点文档修正和最终只读复核已经完成。此后 Engine E1 已实现 PlatformObservation/PlatformFactPackage 类型、严格 validator、固定 SubjectBinding fixture 和运行时 hash 校验；Engine E2 已实现成功结果/错误 envelope、最小 stateProjection、固定 Binding 持久化、不可变结果账本和跨重启重放；Engine E3 已实现独立 test-only Adapter、确定性领域闭环、真实 Action Gate/Evolution 和 operation journal 恢复。D-029 的 JSONL 桥与 Vio 测试端随后完成 D-030 所记录的第一轮 test-only 共享验收；D-031 又完成 Engine 侧正式本地 HTTP 服务，D-032 补齐 domain 子阶段 durable recovery，D-033 记录双方第一阶段正式本地回环 HTTP/JSON 验收通过。完整生产连接仍未实现。上述实现均不改变 SubjectState 权威、Action Gate 或外部能力边界。

**未来是否可修改：** 第一轮 Profile 的固定值和语义只能通过双方明确评审的版本化契约修订改变，不能由任一实现自行解释或设置别名。第一轮之外的生产传输、认证、多租户、投影扩展和 Capability 协议仍按后续共同决定推进。

## D-026：Engine E1 采用封闭、本地、先校验后建模的机器契约基础

**决定：** Engine E1 将 v1.1 第 19 节的三份 Draft 2020-12 JSON Schema 作为唯一机器 Schema 来源，以三个固定绝对 URN 注册在进程内封闭 registry；只允许两个契约规定的 `$ref` 通过该 registry 解析，不允许网络、文件相对路径或别名回退。所有不受信请求先经过禁止状态写字段检查、严格 Schema、交叉引用及 hash 校验，通过后才创建不可变类型化领域对象。

**原因：** 第一轮双方必须对同一输入得到同一验证结果，且 PlatformObservation 不能夹带状态修改。Schema 多份手写副本、普通键排序代替 RFC 8785、外部 `$ref` 回退或先建模后校验都会造成规则漂移、安全旁路或不一致幂等判断。

**实现选择：** 使用 `jsonschema>=4.26,<5`（MIT）执行 Draft 2020-12 与本地 registry 校验，使用 `rfc8785>=0.1.4,<1`（Apache-2.0）执行 RFC 8785；两者均为本地依赖，不使用网络服务、账户、遥测或密钥。`contentHash` 基于原始正文 UTF-8，`requestHash` 只排除顶层 `requestHash`，固定 binding hash 排除所有 hash 字段；测试从完整对象独立复算，验证逻辑不写死预期值等于实际值。D-027 后续将共用 hash 实现放在 `domain/integration_hashing.py`，原 service 模块继续兼容导出，E1 公共入口与算法均未改变。

**影响：** E1 已能在 Continuity Engine 进程内独立验证正式 conformance vector、固定 SubjectBinding fixture 和 D-025 的三个固定 hash，并拒绝未知字段、正文重复、引用不一致、非 UTC 时间、禁止状态写字段、hash 错误和不可解析 `$ref`。这只是机器契约基础，不是 PlatformObservation 摄取适配器或完整连接流程；现有 `APIService.submit_message`、本地 HTTP 和 `UserInteractionService` 均未被复用为第一轮入口。

**未来是否可修改：** 可在双方发布新的版本化契约后增加新的 Schema/Profile；不得静默修改 first-round-v1 的 Schema、URN、hash 算法或固定 fixture。持久化幂等账本、重放和投影数据基础已由 D-027 落地；test-only Adapter 与确定性领域闭环已由 D-028 落地。Vio 或生产入口仍必须单独设计、实现和验收。

## D-027：Engine E2 使用不可变 completed 结果账本与最小状态投影

**决定：** Engine E2 将 v1.1 第 19.6/19.7 节的成功结果和错误 envelope 建模为不可变、严格字段、可恢复对象；stateProjection 只包含固定 schema、binding/revision 元数据、changed、engineUpdateId、最小 snapshot 和 contentHash。`stateHash` 对完整权威 `SubjectState.to_dict()` 做 RFC 8785 → UTF-8 → SHA-256，`contentHash` 对最小 snapshot 做同一算法。E1/E2 复用 `domain/integration_hashing.py` 的唯一实现，原 service 模块保留兼容导出。唯一固定 SubjectBinding fixture/hash 与 completed result ledger 分别保存为带格式版本的本地 UTF-8 JSON，并通过临时文件、flush/fsync 和原子替换写入。

**原因：** 后续 Adapter 必须在跨进程重启后精确重放第一次 operation、response、projection、hash 和完成时间，同时不能把完整 SubjectState、内部隐私或 Event 去重误当成外部结果账本。最小投影与不可变记录能保持 SubjectState 权威只在引擎，并为幂等和投影对账提供稳定依据。

**不变量与边界：** requestId 是永久幂等键，completed 记录不可覆盖；同 requestId/同 hash 可取回原始完整结果，不同 hash 返回内部冲突状态供后续 Adapter 映射为 `IDEMPOTENCY_KEY_REUSED`。同一 subjectId+currentRevision 只能有一份投影内容，非空 engineUpdateId 唯一，多个 changed=false 请求可关联同一 revision 的相同投影。changed=false 保持 revision 且 engineUpdateId=null；changed=true 只推进一次并要求非空 update ID。账本只提供未找到、已完成和 hash 冲突查询，不决定 Schema、Binding、幂等、revision 的正式验证顺序，也不运行领域步骤。

**影响：** E2 新增结果/投影领域模型、确定性结果工厂、Binding 与结果账本仓储及 36 项独立测试；其完工测试基线为 153 项。它没有修改 `APIService.submit_message`、本地 HTTP、`UserInteractionService`、Wake/Perception/Thinking/Action 编排、Event/Mutation/Evolution 或 Vio 文件，也没有在 E2 当时实现 `ContractTestAdapter`、Vio 投影接收器、共享测试或实际连接。后续 D-028 已实现 test-only Adapter 与领域闭环；Vio 和生产连接边界仍未改变。软件版本继续为 `0.1.0`。

**未来是否可修改：** first-round-v1 的外部字段、固定错误、投影语义和 hash 算法只能通过双方版本化契约修订。存储介质未来可替换，但 completed 不可覆盖、跨重启精确重放、最小投影、唯一性和状态权威边界必须保持。

## D-028：Engine E3 使用独立 test-only Adapter、只读外部事实和可恢复领域闭环

**决定：** Engine E3 新增独立、test-only、进程内 `ContractTestAdapter.submit(payload)`，不复用 `APIService.submit_message`、本地 HTTP 或 `UserInteractionService`。入口固定按 Schema/hash → 完整 SubjectBinding → completed/active ledger → expected revision → operation reservation → Wake/Memory/Perception/Thinking/Action → 必要时 Evolution → completed result 的顺序执行。缺少安全可回显的 requestId 属于本地调用错误；机器契约只返回 v1.1 固定的四类错误 envelope。

已验证的 PlatformObservation/message fact 被转换为 `PerceivedPlatformFact`，仅作为 PerceptionContext/PerceptionResult 的只读输入进入 Thinking。平台 `sourceEventId` 只表示外部来源，不成为内部 Event ID、StateMutation 或 Event 历史。确定性 MemoryRetriever、MemoryInfluenceRecorder、ThinkingProvider、ReplyComposer 和 TokenBudgetManager 全部位于引擎进程内，不产生 CapabilityRequest/CapabilityResult。

Thinking 的更新倾向必须先通过真实 ActionService 的权限、风险、资源与 revision 检查。只有批准、无需确认且计划状态为 PLANNED 的 `UPDATE_STATE` 才能产生 `ApprovedStateAction` 授权快照；`ActionEvolutionService` 是该快照进入内部 Event/StateMutation/Evolution 的唯一 E3 接线点。未批准更新、普通回复或无状态变化请求均不创建 Event，revision 保持不变。既有 Wake→Perception→Thinking→Action 编排也改用同一授权快照与服务，公共入口保持兼容。

**原因：** 第一轮必须验证真实 SubjectState、Perception 只读、Action Gate、Evolution 和 revision，而不能让平台事实、确定性模型输出或回复文本绕过主体状态权威。同时，进程故障可能发生在领域完成、Evolution 提交和 completed 结果写入之间，必须以稳定身份和显式阶段恢复，避免重复思考、第二次 Event 或第二次 revision 推进。

**持久化与恢复边界：** E3 在 E2 completed result ledger 之外增加独立、带格式版本、原子 JSON 写入的 operation journal。它保存稳定的 operationId、responseId、内部 eventId、领域 checkpoint、Evolution checkpoint 与完成阶段；同 requestId/hash 重启后继续原 operation，同 requestId/不同 hash 仍返回幂等冲突。若 Evolution 已提交但 checkpoint 未写入，恢复通过稳定内部 eventId 查询现有 StateUpdateRecord，禁止重复创建 Event。每个 JSON 文件各自原子写入；E3 不宣称 result ledger、operation journal 与 SubjectState/Event 仓储之间具有跨文件原子事务。

**影响：** E3 新增 Adapter、bootstrap、确定性替身、ActionEvolutionService、operation checkpoint 模型及 50 项专项测试，并扩展 Perception/Thinking 的外部事实摘要、Action 授权快照和 integration repository。`hello` 保持 revision 0；唯一固定更新夹具 `remember continuity test focus` 通过 Action 后把 `first-round continuity test` 追加到主体关注点并只推进一次 revision。E3 完工测试基线为 203 项；后续 JSONL Runner 与 Vio 测试端已完成 D-030 所记录的 test-only 共享验收，D-031 已将处理链迁入共享核心并建立 Engine 正式回环 HTTP，D-032 补齐 crash recovery，D-033 记录 Vio V3 与 Engine E4 的正式本地回环验收通过。完整生产 Adapter、生产认证/多租户和真实外部能力仍未实现。

**验收状态：** 2026-08-02，项目统筹窗口完成独立验收，确认 `ContractTestAdapter`、Observation/Event 隔离、确定性领域闭环、Action Gate、Evolution、operation journal、中断恢复和跨重启精确重放均符合 E3 边界，50 项专项测试及完整 203 项测试通过，未发现阻塞问题。E3 正式验收通过，但不授权 Vio 侧施工、共享测试或生产连接。

**未来是否可修改：** test-only Adapter 可在双方共享测试验收后被独立的生产 Integration Adapter 取代，但不得把现有调试入口直接升级为生产契约入口。外部事实只读、Action Gate、Event/Evolution 唯一状态落地、稳定幂等恢复、SubjectState 权威和不宣称跨文件事务的边界必须保持；协议字段或错误语义只能通过双方版本化契约修改。

## D-029：共享验收使用 tests 下的显式 JSONL 子进程桥

**决定：** 第一轮双方共享验收准备只在 `tests/shared/` 提供 test-only JSONL Runner。调用方必须显式指定受控临时 `--data-dir`，从 stdin 每行提交一个无包装的完整 `ContinuityInteractionRequest`；Runner 不改写 requestHash，每行只调用现有 `ContractTestAdapter.submit()`，并把其 `to_dict()` 结果以紧凑 UTF-8 JSONL 立即 flush 到 stdout。四类机器契约错误继续处理；空行、非法 UTF-8/JSON、缺少安全 requestId、fixture/存储损坏或 Adapter 非预期异常属于本地故障，不创造第五类错误 envelope。

Runner 的领域装配复用 E3 bootstrap、确定性 Memory/Thinking/Reply/Token 替身、真实 ActionService/Action Gate、ActionEvolutionService 及 JSON SubjectState/Binding/operation/result 仓储。空目录只初始化固定 fixture 一次，非空目录只能完整验证和恢复；部分、损坏或相互矛盾的数据拒绝启动且不得自动修复。新 requestId 的 operationId、responseId、Thinking result ID 和 usage ID 使用 UUID 型工厂避免跨进程冲突；已完成 requestId/hash 仍精确重放第一次完整结果。

**原因：** Vio 共享测试需要一个稳定、本地、无网络的进程边界来验证真实 E3 结果，而现有 `APIService.submit_message`、本地 HTTP 和 `UserInteractionService` 均不是 v1.1 第一轮摄取入口。把桥限制在测试包可避免误升级为生产接口，同时保留 Schema、Binding、ledger、revision、Perception、Action Gate 和 Evolution 的真实边界。

**影响：** Engine E1/E2/E3 的完成基线分别为 `ac61e78`、`d1a96b1`、`c732f35`；Runner 后续以 `7a32a99e60330782c1caf6d6adda5d08d0077a6c` 提交同步，并已由 Vio 测试代码实际启动完成 D-030 的 test-only 共享验收。网络传输、认证、多租户和生产 Integration Adapter 仍未实现。Runner 不注册正式 CLI/HTTP，不监听 Socket，不读取 `.continuity-data` 或生产配置，不调用真实模型、MCP、Tool、设备或外部记忆。

**验收状态：** 2026-08-02，项目统筹窗口完成独立技术验收，确认 Runner、runtime 装配、真实子进程、UTF-8/flush、本地错误、fixture 恢复、损坏拒绝和跨进程 ID 唯一性均符合 D-029，专项 16 项、完整 219 项测试通过，原有 203 项继续通过，未发现阻塞问题。该成果随后提交同步并参加 D-030 的双方 test-only 共享验收；D-029 本身仍不授权网络或生产连接。

**未来是否可修改：** 测试启动器和受控临时目录策略可随双方共享测试版本化调整，但不得改变 v1.1 机器字段、固定 hash、四类错误、Action Gate、精确重放或 SubjectState 权威。生产连接必须另立架构决定和实现，不能直接复用该测试 Runner。

## D-030：第一轮双方 test-only 端到端共享验收通过，不等于生产连接

**决定：** 以 Engine Runner `7a32a99e60330782c1caf6d6adda5d08d0077a6c` 和 Vio `673983901b38127b15f772a8be8507defec7384e`（`test: add Vio Engine shared contract acceptance`）为固定验收基线，正式确认 Vio 与 Continuity Engine 第一轮 test-only 端到端共享验收通过。Vio 的真实持久化 V1 请求经 test-only JSONL Runner 进入真实 Engine E3；Engine 返回的 operation/result/stateProjection 由 Vio V2 严格验证，结果、投影、head 与 receipt 按 Vio 的独立数据库边界持久化。

**验收事实：** A `hello` 为 `changed=false`、revision `0→0`、`engineUpdateId=null`，Vio head 初始化为 0；B `remember continuity test focus` 真实经过 Perception、Thinking、Action Gate 和 Evolution，为 `changed=true`、revision `0→1`，只产生一个非空 `engineUpdateId`，Vio head 推进到 1；C 在 expected revision 1 时为 `changed=false`、revision `1→1`，head 保持 1。最终 Engine revision 为 1 且只有一条 `StateUpdateRecord`；Vio 保存 revision 0/1 两份投影和三条 receipt，legacy SubjectState 与旧 head 未被改写。同进程和双方重启后均精确重放原 operationId、responseId、engineUpdateId、completedAt，不再次运行 Thinking，不创建第二个内部 Event/StateUpdateRecord，也不再次推进 revision。`SCHEMA_INVALID / never`、`SUBJECT_BINDING_MISMATCH / never`、`REVISION_CONFLICT / reassemble`、`IDEMPOTENCY_KEY_REUSED / never` 四类错误全部通过。

**验证结果：** 双方共享端到端 6/6、Vio V1+RFC 8785 17/17、Vio V2 29/29、Vio 后端全量 95/95、Engine Runner 16/16、Engine 全量 219/219。

**原因：** 共享验收需要用双方真实持久化实现验证同一 v1.1 请求、结果、投影、幂等、revision、错误和重启恢复语义，而不能只凭各自单元测试或文档一致性宣称可以共同运行。

**影响：** 第一轮机器契约实现和 test-only 施工阶段完成，证明双方机器契约及领域边界能够通过受控本地子进程共同运行。该结论不把 Runner 升级为正式服务，也不在当时表示 Vio 与 Engine 已建立正式本地服务或网络连接。后续 D-031 已独立完成 Engine 侧正式本地 HTTP 服务，D-032 补齐 crash recovery；D-033 又确认 Vio V3 与 Engine E4 的第一阶段正式本地回环验收通过。再后的 D-034、Vio V4、受控 S4、V5/F1/L1、S4-R 和首次 S4-Live 已完成固定本地 Capability/Conversation 及单次真实供应商验证；生产 Integration Adapter、通用 Provider、MCP/Tool/设备、生产公共链路、认证、多租户或部署仍未实现。

**未来是否可修改：** test-only Runner 和共享验收夹具只能随双方版本化测试契约调整；SubjectState 唯一权威、Observation/Event 隔离、Action Gate、Evolution、幂等精确重放和双方数据库独立边界必须保持。Engine 正式本地服务已经由 D-031 另立决定；生产形态 Integration Adapter 仍需后续决定，不得将本次 test-only 通过自动解释为生产授权。

## D-031：E4 使用唯一交互核心、正式单 active Binding 与串行回环 HTTP

**决定：** Engine E4 将 E3 `ContractTestAdapter` 中唯一的机器验证、领域闭环、Action Gate、Evolution、结果生成和 checkpoint 恢复链抽取为正式 `ContinuityInteractionService`。E3 `ContractTestAdapter` 与 E4 正式 `IntegrationAdapter` 均只委托该服务；`_run_domain`、`_complete_evolution`、`_build_result` 和验证顺序只能存在一份。测试故障注入、trace、clock 和 ID factory 保留为服务级可注入依赖，但正式装配不启用故障注入。

正式运行使用独立 `SubjectBinding` 与单 active 原子 JSON 仓储。Binding 只能经 `python -m continuity_engine.integration_server init` 显式创建或幂等验证；fixture hash 必须按 RFC 8785 复算，cycleId 只用于 Engine 内部关联，不进入 Vio Binding hash。`serve` 不隐式初始化、重置、修复或覆盖状态。损坏、部分初始化、不同 Binding 或不一致跨仓储状态必须拒绝启动。

正式 HTTP/JSON 服务只监听 IPv4 `127.0.0.1`，默认端口 8766，第一阶段使用标准库串行 `HTTPServer`。POST 原样接收 v1.1 请求；success 与四类机器错误均为 HTTP 200 原始 envelope。GET 查询只返回 `completed` 或 `recovery_required`；not_found 为最小 HTTP 404，查询不得运行领域步骤。内部路由使用环境变量 Bearer 令牌、常量时间比较、1 MiB body 上限和严格 Content-Type/Content-Length/UTF-8/JSON 分界；健康接口只返回最小 live/ready。第一阶段明确采用一请求一连接：所有响应设置 `Connection: close` 并停止解析该连接，读取超时默认 10 秒且必须为正有限数；未知方法和可安全响应的解析错误使用固定最小 JSON，部分请求超时与普通断连不得产生默认 HTML、Python traceback 或泄露信息。服务不启用 CORS，不面向 Vio 前端。

第一阶段正式装配使用进程内确定性 Memory/Thinking/Reply/Token Provider，真实 Action Gate、Evolution、SubjectState、AwakeCycle、Thinking 与 operation/result 仓储。确定性 Provider 是 E4 的受限本地实现，不是 GPT、Claude 或其他真实模型。正式装配不得导入 tests、JSONL Runner、`FirstRoundContractBootstrap`、`ContractTestAdapter`、旧调试 HTTP、`APIService.submit_message` 或 `UserInteractionService`。

**原因：** test-only Runner 已证明机器语义能跨仓共同运行，但正式本地服务必须有稳定进程、鉴权、显式初始化、readiness、查询和恢复边界。把领域链集中到一个服务可以防止测试入口与正式入口在 Schema、Binding、revision、Action Gate 或恢复顺序上漂移；单 active Binding 和串行 HTTP 则与当前本地 JSON 仓储的一致性能力匹配。

**影响：** Engine 侧正式本地 HTTP/JSON Adapter 已完成；HTTP/1.1 传输定点补强后，当时专项 67 项、完整 286 项测试通过，原 219 项继续通过。未消费 body 的拒绝关闭当前连接，部分 body 超时后串行服务继续接受健康检查，普通客户端断连不输出 traceback。`changed=false` 仍不创建 Event，`changed=true` 仍只产生一个 Event/StateUpdateRecord 并推进一次 revision。D-032 后续发现并补齐 RESERVED 阶段内部 Wake/Perception/Thinking 子阶段的 durable recovery；当前完整测试基线为 301 项。软件版本保持 `0.1.0`，三份 v1.1 Schema、三个固定 hash、JSONL Runner、旧调试入口和 Vio 仓库均未改变。

该完成状态起初只代表 Engine 侧正式本地服务可启动并通过本地真实 HTTP/子进程测试；后续 D-033 已确认 Vio V3 与 Engine E4 的第一阶段正式本地回环验收通过，D-034、Vio V4、受控 S4、V5/F1/L1、S4-R 和首次 S4-Live 又完成固定本地 Capability/Conversation 链路及单次真实供应商验证。通用 Provider、生产公共 API/前端、生产认证、多租户、TLS、反向代理、云部署和完整生产 Integration Adapter 仍未实现；本决定及后续验收都不自动授权下一阶段施工。

**未来是否可修改：** 传输、认证和存储介质可在双方版本化评审后扩展；当前 127.0.0.1、串行处理和确定性 Provider 可由后续阶段替换。不得改变 SubjectState 唯一权威、Observation/Event 隔离、Action Gate、单一领域处理链、completed 精确重放、revision 只合法推进一次和秘密不进入主体/记忆/普通日志的边界。

## D-032：E4 domain 子阶段必须具有 durable recovery identity 与 checkpoint

**决定：** `ContinuityInteractionService` 在运行具有持久化副作用的 domain 子阶段前，必须先在 Engine operation journal 中保存稳定恢复身份。恢复身份至少覆盖 operation、WakeSession、WakeContext、Perception context/result、ThinkSession、ThinkingResult、Action context、response、内部 Event 和最终 StateUpdateRecord 的关联。Wake、Perception、Thinking 完成后分别保存不可逆 checkpoint；domain checkpoint 保存后不得重新执行 Wake、Perception、Thinking 或 Action。

WakeSession 成功完成时保存恢复所需的结构化 `WakeContext` 快照，ThinkSession 保存完整结构化 `PerceptionResult` 快照；两者不得保存完整模型思维链。恢复时 operation journal 与 AwakeningRepository、ThinkingRepository、SubjectState/update history 和 completed result ledger 共同决定事实：已成功完成的 WakeSession 和 ThinkSession/ThinkingResult 必须加载、验证并复用，不能重新创建或重新调用 ThinkingProvider；running、failed、多个候选、身份冲突或内容矛盾必须失败关闭，不能猜测、覆盖或生成第二份主体经历。`completed → running` 的仓储保护继续保持。

对 E4 旧格式中 `stage=reserved`、`domain=null`，但固定 operationId 对应 WakeSession 和唯一关联 ThinkSession 已成功完成的部分状态，允许按 wakeSessionId、subjectId、providerId、input revision、Observation/fact 引用和结果身份严格回查。只有候选唯一且全部一致时才能复用并补写新的内部 progress；否则拒绝恢复。operation journal 继续兼容读取旧 format v1，并以 format v2 保存 durable domain progress。

ActionRepository 仍为进程内实现，不宣称生产持久化。D-032 的 E4 基线允许从已持久化 Perception/Thinking 结果、稳定 context ID 与时间确定性重建 Action 输入；D-034 的 E5-A durable 恢复进一步要求 Action 完成后先把完整结构化 `ActionExecutionResult` 写入 operation journal，重启后必须直接复用，不能再次调用 ActionService。Evolution 继续使用稳定 Event ID 和 `find_update_by_event_id()`：即使 operation evolution checkpoint 尚未保存，只要合法更新已存在，也只能查回复用，不能创建第二个 Event、StateUpdateRecord 或再次推进 revision。completed result 一旦保存继续逐字段精确重放，第一次 responseId、operationId、engineUpdateId 和 completedAt 不得改变。

**原因：** Vio V3 S3 在 Engine `c5ebbf9b7583f3fb50198a3bf37ea0553edc131f` 上发现：operation 仍为 `reserved/domain=null` 时，WakeSession 和 ThinkSession 可能已经完成持久化；重启后旧实现因缺少 domain 子阶段身份而重跑 Wake，并被正确的“completed WakeSession 不能回到 running”保护拒绝。缺陷不在仓储保护，而在副作用完成与 operation.domain 整体保存之间缺少 durable 子阶段恢复事实。

**影响：** E4 外部 HTTP envelope、v1.1、三份 Schema/hash、SubjectBinding、幂等/revision、Action Gate 和 Vio 均不改变。新增 crash-recovery 专项 15 项覆盖十个故障窗口、旧 E4 部分状态、changed false/true、幂等冲突、损坏状态失败关闭和旧 completed JSON；E4 相关 82 项、Engine 全量 301 项通过。Engine 侧定点修复以 `189441f9bad2a34119b4ef10365a4385ed0949cc` 提交，随后已通过 D-033 记录的双方正式本地回环验收。

**未来是否可修改：** 内部 checkpoint 载体和格式可在保持向后读取与显式迁移的前提下演进；不得放宽已完成会话不可逆、恢复身份稳定、外部事实只读、Action Gate、Evolution 单次提交和 completed 精确重放。若未来引入持久化 ActionRepository 或真实模型，必须新增相同级别的执行状态与未知结果处理，不能以重跑真实外部副作用替代恢复。

## D-033：第一阶段正式本地回环 HTTP/JSON 双方验收通过

**决定：** 以 Continuity Engine `189441f9bad2a34119b4ef10365a4385ed0949cc` 和 Vio `35780da56c72b822fc018702dfe5e90674ab0fcb` 为正式基线，确认 Vio × Continuity Engine 第一阶段正式本地回环 HTTP/JSON 双方验收通过。Vio 的真实持久化请求通过正式 V3 HTTP transport 进入独立运行的 Engine E4，Engine 的 operation、result、stateProjection 再由 Vio 严格验证并持久化。

验收覆盖 S2/S3 15/15，包括 A/B/C revision `0→0→1→1` 主流程、四类正式机器错误、completed、not_found、recovery_required、POST 响应丢失、Engine/Vio 分别或同时重启。requestId、requestHash、operationId 保持稳定；已完成 Wake、Thinking 复用，内部 Event、StateUpdateRecord、revision、Vio 投影和双方账本均未重复。首次 S3 在 Engine `c5ebbf9b7583f3fb50198a3bf37ea0553edc131f` 上发现 `reserved/domain=null` 且 Wake/Thinking 已完成时无法恢复，该历史事实由 D-032 修复记录保留；修复后的原失败场景和全部 S3 均通过。

**原因：** Engine E4 与 Vio V3 分别完成不足以证明双方对正式本地传输、恢复、幂等、投影和崩溃窗口具有同一运行时理解。只有由 Vio 持久化请求经真实 HTTP 进入独立 Engine，并验证双方数据与重启恢复后，才能关闭第一阶段本地连接验收。

**影响：** 第一阶段机器契约、test-only 共享链路和正式本地回环连接均已通过验收；验证结果为 S2+S3 15/15、Vio V1+RFC 8785+V2+V3 64/64、Vio 后端 113/113、Engine crash-recovery 15/15、E4 67/67、全量 301/301。该里程碑不修改 v1.1、SubjectState 权威、Observation/Event 隔离、Action Gate、双方数据库边界或 `0.1.0` 软件版本。后续 D-034、Vio V4、受控 S4、V5/F1/L1、S4-R 和首次 S4-Live 单次真实供应商验收已完成，但仍不表示外网、生产部署、通用 Provider、MCP/Tool/设备或生产公共用户链路已经完成；后续阶段必须另行确定并授权。

**未来是否可修改：** 本次验收基线和历史结果作为工程事实保留。后续可以版本化扩展传输、Provider、认证、多租户和部署，但不得把本地回环验收重新解释为生产授权，也不得削弱 D-025、D-031、D-032 的权威、恢复和安全边界。

## D-034：Capability 由 Engine 决策并持久暂停，由 Vio 执行现实能力后返回结果

**决定：** Engine E5-A 在不修改 `Continuity Integration Contract v1.1` 的前提下建立独立版本化 `continuity-capability/v1` 协议。Continuity Engine 负责判断何时需要外部模型能力、构造不可变 CapabilityRequest、保存 purpose/scope/约束与稳定身份、将原 ThinkSession 推进到 `WAITING_CAPABILITY`，并在 CapabilityResult 返回后完成严格 Schema、hash、subject/binding/request/attempt 关联校验。任何结果必须在 `save_capability_result()` 前完成内容、身份和时间一致性验证：`SUCCEEDED` 的 `responseCandidate` 去除首尾空白后必须非空，`startedAt` 和 `completedAt` 都不得早于已持久化请求的 `createdAt`，且 `completedAt` 不得早于 `startedAt`。验证失败不能写入 attempt、accepted-result checkpoint 或任何 Thinking/Action/Evolution/SubjectState 副作用。Vio 负责现实权限与用户确认、供应商路由、供应商执行、可信使用量和外部秘密；Vio 不拥有 Thinking 决策权，也不能直接写 SubjectState。Vio V4、受控 S4 与首次 S4-Live 已验证这一职责分层；单次现场通过不把该决定扩张为生产授权。

成功的模型输出必须返回同一个 `ContinuityInteractionService`，经结构化结果解释器使原 ThinkSession 按 `WAITING_CAPABILITY → RUNNING → COMPLETED` 恢复，再进入 Action Gate 和 ReplyComposer。外部模型输出、CapabilityResult 或 Vio 结果都不能携带或直接成为 Event/StateMutation；只有后续 Action 合法批准 `UPDATE_STATE`，引擎才能创建内部 Event/StateMutation 并交给 Evolution。E5-A 当前的受约束模型输出只形成回复、不建议状态更新，故成功路径保持 `changed=false`、revision 不变。

Capability 请求、尝试、状态与结果保存在 Engine 自身的 durable capability ledger；operation journal format v3 保存 request/result/thinking checkpoint，并兼容读取 E4 的 v1/v2 operation。重复显式执行既有 `init` 时，初始化器必须读取现有 Binding、SubjectState、AwakeCycle、operation/result/capability 历史并选择与持久化事实相符的内部校验模式；它只能验证或补建允许的空 capability ledger，不得重置、改写或删除既有身份、状态、revision 与结果。`serve` 不得隐式迁移、重置或修复，含 Capability 历史的目录仍不得以 deterministic 模式运行。相同 request/result 必须精确幂等，相同身份但不同内容必须冲突；成功或终态关闭后不得重新执行。崩溃恢复必须复用原 capabilityRequestId、attemptId、ThinkSession、ThinkingResult、responseId 和首次完成结果，不得重复调用 Provider、重复 Thinking 或绕过 completed 精确重放。默认 `deterministic` 模式继续保持 E4 行为，只有显式 `capability` 模式进入暂停—恢复链。

**原因：** 真实模型、Tool、MCP 或设备属于外部能力与现实副作用，不能被当作主体本身，也不能在请求执行期间阻塞或把执行权交给平台。Engine 必须保存“为什么请求能力、等待哪一次结果、结果如何进入原思考”的连续事实；Vio 必须保留现实权限、安全和供应商账本边界。独立协议可避免修改已验收的 v1.1 第一轮交互协议，同时为后续 Vio V4 提供明确、可恢复和可审计的协作边界。

**影响：** Engine E5-A 已实现 CapabilityRequest/CapabilityResult 的领域模型、三份严格 Draft 2020-12 Schema、封闭本地 registry、持久化请求/尝试账本、WAITING_CAPABILITY、提交/查询入口、结构化解释与 durable crash recovery。operation journal 在 Action 完成后保存结构化结果 checkpoint，恢复时复用同一 ActionSession、Decision 和 Plan，不再次运行 Action。本次定点修正关闭了无效成功结果落盘、结果时间早于请求及 Capability 历史目录重复 `init` 失败三个漏洞；专项 54 项、Engine 全量 355 项通过。后续 Vio V4、双方受控 S4 Capability 联验、V5、F1、L1 已完成，Engine S4-R 独立追认结论为 `PASS`；它们验证了 D-034 的现实执行分层、结果回流、恢复与幂等边界，但没有修改本决定或 v1.1。

**S4-Live 验收状态：** 2026-08-14，在不可晋升的独立可销毁测试身份下完成首次真实供应商单次试聊。唯一 Provider execution 与唯一 CapabilityResult 成功，结果返回原 operation/ThinkSession 后由 Engine 形成最终主体表达；`changed=false`、revision `0→0`、`engineUpdateId=null`，没有 Event、StateMutation 或 StateUpdateRecord，也没有重复 execution/result/Message。API Key 未进入 Engine，费用账本为 `not_reported`，沙箱已整根清理。该验收不验证真实模型 `UPDATE_STATE`、通用 Provider、并发压力、长期运行或生产部署。

**未来是否可修改：** capability 协议字段、Provider 类型、超时、重试与输出约束可通过双方版本化评审扩展；后续真实调用、日常使用化与生产能力仍必须另行授权。任何扩展都不得削弱 SubjectState 唯一权威、Engine 的能力请求决定权、Vio 的现实执行与安全边界、外部结果回流 Thinking/Action、Action Gate、幂等终态和跨重启不重复执行原则。

## D-035：P00 接口冻结、运行断开与独立升级基线

**决定：** 以 2026-08-24 确认同步版 v1.1 为 P00—P23 施工流程最高权威，以确认同步版 v6.7 补充长期能力设计细节；从 Engine `7715fb8d35f4b71d94883946e930511b52056c26` 和只读 Vio `729f9e3c7a2dd4da817b9449008d3258f405ac89` 建立 P00 冻结基线。六份现行交互/Capability Schema、D-025 固定 hash 语义及 SubjectState → Event/StateMutation → Evolution 权威路径在 P00 只登记、不修改。

P00 是 Engine/Vio 接口冻结、运行断开和独立升级起点；双方仓库、数据根、测试与进程保持独立，只通过版本化契约和独立验收协作。历史本地 HTTP/JSON 与 Capability 链路继续存在，但不构成任一方存活、启动或施工的前置依赖；第一正式 World Adapter 的生产重连属于 P22。

P10 的唯一主意义是从 P09 已验收的稳定 C1 Continuity Core 创建 Continuity Assistant Git 分支和独立仓库，不是 Engine/Vio 解耦点；P00 不创建该分支或仓库。

恢复边界固定为 P01 仅服务测试、实验和 Research Sandbox，P17 只可复用其设计与验证方式，P20/P21 才承担正式 Subject、Owner Domain 与生产恢复；未具备正式恢复条件时使用 `RECOVERABILITY_NOT_READY`。P17 阶段本身的直接依赖只有 P16 `ACCEPTED`，不依赖后置的 P20/P21；P17 可以施工并验收执行核心、World/Capability Layer、Reality Boundary、幂等、补偿、未知结果处理和能力门禁。P20/P21 只决定依赖正式 Subject、Owner Domain 或生产恢复的高风险现实能力能否开放：在其验收前必须保持 `RECOVERABILITY_NOT_READY / NOT_READY / Disabled` 或正式 Feature Gate 关闭。该开放门不改变 P00—P23 顺序，不授权提前施工 P20/P21，也不得由 P01 测试恢复替代。

现实行动仍只能遵循 `Subject → Thought → Desire → Will → Decision → Structured Action → Capability Resolution → Permission / Resource / Recoverability Check → Reality Boundary → World Adapter → Reality Effect`，Reality Boundary 不得限制或修改主体内部心理活动和长期主体特征。

**原因：** 后续跨度覆盖主体能力、外部世界、恢复、安全与分支产品。若没有唯一阶段底图、冻结接口、职责边界和验收门，不同施工窗口会产生跳阶段、错误实现状态、恢复旁路或主体权威漂移。

**影响：** 新增 P00 四份专责档案，登记 P00—P23 全部阶段、依赖、风险、代码/测试/验收映射和用户授权门。P00 完成本地实现与核查后、用户验收前只能标记 `IMPLEMENTED_NOT_ACCEPTED`；P01—P23 均保持 `NOT_STARTED`，未经独立阶段授权不得进入 P01。

**用户验收状态（2026-08-25）：** 规划监工窗口已完成核查，用户已明确验收 P00，因此 P00 与 P00-01—P00-12 的当前状态为 `ACCEPTED`。该验收没有授权 P01；P01—P23 继续为 `NOT_STARTED`，P01 仍须单独 Stage Brief 和用户明确授权。

**后续授权状态（2026-08-25；2026-08-26 边界校准；2026-08-27 验收）：** 用户在独立 Stage Brief 中另行授权 P01 Engine 侧施工；该后续决定不改变 P00 当时的验收事实。最新规划明确 P01—P21 必须由 Engine 独立施工与验收。用户已于 2026-08-27 正式验收 P01，因此 P01 与 Engine side 均为 `ACCEPTED`、Vio dependency `NONE`，Vio/PWA production integration `DEFERRED_TO_P22`；P02—P23 继续 `NOT_STARTED`。

**未来是否可修改：** 只能由用户通过新的明确决定和版本化规划修改。不得以临时实现、测试便利、平台依赖或进度为由隐式改变阶段顺序、接口、权威、恢复或 Reality Boundary。

## D-036：P01 Snapshot/Rollback 只能作用于登记的 TEST Sandbox

**决定：** P01 测试基础设施位于独立 `continuity_engine.testing` 命名空间，不进入正式 Engine 入口，也不成为 SubjectState、Memory、Evolution 或 Thinking 的运行权威。每个 Sandbox 必须使用独立 disposable subject、Binding、cycle、namespace、branch 和数据根，并持久化 `environment=TEST`、`synthetic=true`、`promotionAllowed=false`。Fixture 的人格、关系与连续性初始化必须经正常 Event/StateMutation/Evolution；正式 `changed=true` 验收必须复用唯一 IntegrationAdapter/ContinuityInteractionService 标准链，test-only 身份适配不得复制领域处理方法或改变生命周期。测试 Memory/Action 只能通过 Sandbox 专用持久化 repository 及现有 ports 存在。

Snapshot 必须以 19/19 逻辑组件和唯一 closed-world physical inventory 形成完整清单；未知、遗漏或重复映射全部失败关闭。Manifest 的完整性由 descriptor/registry 中独立 `snapshotId → manifestHash` 锚点约束，不能通过同时重算内部 hash 绕过。成功证据必须由绑定同一次标准交互 `changed=true`、指定 Snapshot、exact rollback、外部锚点与正式路径隔离的 acceptance receipt 产生；缺失或不一致返回 `ACCEPTANCE_NOT_READY`。

Snapshot 只允许从本 Sandbox 的已知 revision 创建，必须完整覆盖 SubjectState、Event、StateMutation、Evolution、Memory、Learning、relationship、operation/result、capability、resource 以及 Binding、AwakeCycle、subject clock、Fixture 和 test trace。每个组件必须同时记录相对物理位置、规范化内容 hash、文件 hash、revision/count 和 completeness；缺失返回 `SNAPSHOT_INCOMPLETE`，篡改返回 `SNAPSHOT_TAMPERED`。回滚使用同文件系统 staging/backup 原子替换并在安装前后执行逻辑校验；失败必须恢复原 branch，禁止产生混合状态。

Subject clock 是 Sandbox 内持久化、可受控推进的 UTC Frozen Clock；maintenance clock 只决定创建、保留和清理，不进入主体历史。ACTIVE Sandbox 创建时即获得 24 小时 lease；成功测试在有效 receipt 与证据导出后自动清理，失败默认保留 24 小时，显式调试最多 7 天；到期扫描只在 P01 入口或显式维护操作触发，不建设 P11 Scheduler。清理只接受 registry 中的直接 Sandbox 子目录，并拒绝仓库、正式数据、home/父目录、受保护路径、未知/marker 缺失目标、符号链接、junction 和 reparse point。所有 promotion/import-to-formal 路径必须返回 `PROMOTION_FORBIDDEN`。

**原因：** P01 必须能真实验证 `changed=true`、时间和全组件恢复，同时确保测试行为不会污染正式主体或被误当作 P20/P21 生产恢复。只复制目录而不校验领域组件、允许测试身份晋升或让后台清理扫描未知路径都会破坏 Subject Authority 和数据安全边界。

**影响：** Engine side 已完成 test-only 实现与本地专项/完整回归，并由用户于 2026-08-27 正式验收；P01 与 Engine side 均为 `ACCEPTED`，Vio dependency 为 `NONE`，Vio/PWA production integration 后置到 P22。P00 继续 `ACCEPTED`，P02—P23 继续 `NOT_STARTED`。本决定不修改六份冻结 Schema、生产 HTTP/API/Capability/Binding、正式数据、软件版本或 Vio，也不授权 P02。

**未来是否可修改：** P01 若增加新的 test-only 组件，必须先进入完整 manifest 和 Engine 独立恢复验证；不得把 Vio 运行、仓库、数据、网络或凭据变成 P01 依赖。不得把 P01 Snapshot/Rollback 扩张为正式 Subject/Owner Domain 恢复、把测试 Fixture 导入正式历史、提前建设 P11 Scheduler，或绕过 P20/P21。

## D-037：用户正式验收 P01，但不授权 P02、P10、Vio 或 Git 提交

**决定：** 用户于 2026-08-27 在规划监工窗口正式验收 P01。P01、P01 Engine side 及 P01-01—P01-12 当前统一为 `ACCEPTED`；P01 Vio dependency 保持 `NONE`。本次授权只允许把验收决定写入 Engine 工程档案并完成提交前核查，不授权 P02 Stage Brief/施工、P10 Assistant 分支或独立仓库、Vio 配合/测试，也不授权 `git add`、commit、push 或 tag。

**原因：** P01 已形成并闭合独立 TEST Sandbox、Frozen Clock、synthetic Fixture、19/19 closed-world Snapshot/Branch、外部防篡改锚点、原子 rollback、真实标准交互 `changed=true`、acceptance receipt、promotion 禁止、安全 retention/cleanup 和正式目录双算法隔离证据。P01 专项 42 项、相关正式链路 200 项及 Engine 完整 397 项通过，项目内规范 `tree_inventory_hash` 与独立外部聚合 hash 在完整回归前后均保持不变。

**影响：** P00 与 P01 均为 `ACCEPTED`；P02—P23 继续 `NOT_STARTED`。P01 仍只负责测试、实验和 Research Sandbox 的 Snapshot/Rollback，不是正式 Subject、Owner Domain 或生产恢复。P17 可以复用其设计和验证方法，但不能把它作为生产恢复能力；P20/P21 继续负责正式主体和生产恢复。P01—P21 不依赖 Vio，Vio/PWA 第一次生产重连仍固定在 P22；P10 仍只在 P09 已验收的稳定 C1 Core 上实际创建 Continuity Assistant 分支和独立仓库。

**未修改边界：** 本决定不修改 Engine 主体定位、Subject Authority、Engine/Vio 独立边界、Execution Chain、Reality Boundary、六份冻结 Schema、HTTP/API/Capability/Binding 契约、正式数据或 `0.1.0` 软件版本。

**未来是否可修改：** P02 及任何后续阶段必须通过独立 Stage Brief 和用户明确授权；P01 验收不能自动触发代码施工、Vio 重连、Git 提交或推送。

## D-038：P02 Stage Brief、宿主中立模型能力与确认参数

**决定：** 用户于 2026-08-28 单独授权 P02——日常真实模型接入的 Engine 独立模型能力闭环施工。P02 使用宿主中立 `ModelProvider` Port、可替换 Provider/Model Profile、Engine 内确定性 Fake Provider、持久化 execution/usage/test-credit 账本和 E5-A 唯一 durable `model.generate` pause/resume。确认 Profile 元数据为 Alibaba Cloud Model Studio / `qwen-flash-2025-07-28`、单次 1024 synthetic Token、每日 10240 synthetic Token、每个唯一成功 Fake execution 记 1 test-credit、fallback 关闭；这些参数可配置替换，不是永久供应商绑定或真实价格。

**原因：** P02 必须先在 Engine 仓库内证明模型能力的选择、预算、唯一执行、失败/UNKNOWN 查询、崩溃恢复、多轮和原 ThinkSession 回流，而不能把 Provider 候选、Vio 或真实供应商变成新的 Subject Authority。复用 E5-A 可避免第二套 CapabilityRequest/CapabilityResult、ThinkSession、operation journal、completed result 和最终表达状态机。

**影响：** Provider 成功事实只能生成严格验证的 CapabilityResult，并恢复原 ThinkSession，再经过 Thinking、Action 和 ReplyComposer 形成 Engine 最终表达。P02 默认路径保持 `changed=false`、revision `0→0`、`engineUpdateId=null`；模型输出不能直接修改 SubjectState 或创建 Event/StateMutation。P02 Vio dependency = `NONE`。真实 Provider SDK/网络/API Key、可信真实 usage/cost 与 Vio/PWA production integration 全部 `DEFERRED_TO_P22`；冻结 Schema、HTTP/API/Capability/Binding 外部契约和 `vioLedgerEntryId` 不变。

**当前验收状态：** P02、P02 Engine side 与 P02-01—P02-12 的本地实现和证据均为 `IMPLEMENTED_NOT_ACCEPTED`。本决定不授权 P03、P10、Vio、`git add`、commit、push 或 tag；必须等待规划监工窗口核查和用户明确验收。

**返修验证状态（2026-08-28）：** 规划监工发现 UNKNOWN/TIMEOUT 查询恢复、部分每日预算调用前约束和 usage/test-credit 加载期验真三项证据冲突。定点返修后，原 UNKNOWN/TIMEOUT fact 作为不可变历史保留，查询终态以同 attempt 的单调 resolution 追加；`maximumTokens` 在调用前收窄为单次上限与当日剩余额度的较小值；成功 Provider fact 即使因越限不可交付也保留 usage/test-credit 审计，且账本每次加载逐字段绑定唯一成功 fact。决定的 Subject Authority、E5-A 唯一通道、P22 延后边界和验收状态不变。

**未来是否可修改：** P22 可在保持 Subject Authority、E5-A 唯一 durable 通道和冻结契约的前提下提供真实 Provider/World Adapter；Provider、模型、预算和价格策略可经后续授权替换。不得用后续配置变化倒写 P02 的 Fake 证据或自动启动 P03。

## D-039：P02 验收前置的 E4/E5 HTTP 前置拒绝路径统一稳定性返修

**决定：** 用户于 2026-08-29 先授权最小 E5 HTTP 413 稳定性修复；在三轮全量稳定性验证的第三轮又真实出现 401 `WinError 10053` 后，用户明确将授权扩展并锁定为 E4/E5 HTTP 所有“读取正文前拒绝 POST”路径的统一连接收口。覆盖既有 404/405、401、503、415、Transfer-Encoding 400、Content-Length 411/400 与 413；不增加新状态码、路径、JSON 或契约。

**原因：** Windows 在关闭仍有未读入站数据的 TCP 连接时可能发送 RST，使客户端已经进入读取响应阶段后得到 `WinError 10053/10054`。逐状态补丁不能闭合共同根因。统一实现必须先按原路由、认证和请求规则顺序决定拒绝，完整序列化并 flush 响应，再半关闭写方向，并在严格短时窗口内用固定 64 KiB 缓冲丢弃可用入站数据；正文不解析、不保存、不记录、不传入 Integration App。

**认证边界：** 401 仍在认证失败时立即作出，禁止为了连接稳定而在认证前等待完整未授权正文。headers-only 且声明 Content-Length 的未授权客户端可以及时收到 401。缺失/非法长度和 Transfer-Encoding 也不被信任，统一收口有严格总时限和空闲时限，EOF、timeout、reset、abort 均失败关闭且不输出 traceback。

**影响：** 外部 HTTP 状态码、JSON、`Content-Length`、`Cache-Control: no-store`、`Connection: close`、路由顺序、认证优先级、Body 大小限制、E5-A/P02 领域语义、Subject Authority、六份冻结 Schema、正式数据和版本均不变。返修只修改 `integration_http_server.py` 及既有 E4/E5 HTTP 测试；没有新增顶层测试方法，完整测试基线继续为 444。

**授权边界：** 本决定不授权 P03、P10、Vio、真实 Provider、网络、凭据、`git add`、commit、push 或 tag。P02 与 Engine side 完成后仍只能为 `IMPLEMENTED_NOT_ACCEPTED`，必须等待规划监工窗口复核和用户正式验收。

## D-040：用户正式验收 P02，但不授权 P03、P10、Vio 或 Git 操作

**决定：** 用户于 2026-08-29 正式验收 P02。P02、P02 Engine side 与 P02-01—P02-12 当前统一为 `ACCEPTED`；P02 Vio dependency 保持 `NONE`。本次授权只包含 P02 验收状态落盘、工程档案闭合和提交前只读核查，不授权 P03 Stage Brief/施工、P10 Assistant 分支或独立仓库、Vio 访问/配合/测试，也不授权 `git add`、commit、push 或 tag。

**原因：** P02 已完成宿主中立 Provider Port、可替换 Profile、Engine 内 Fake Provider、synthetic usage/test-credit 与预算门、UNKNOWN/TIMEOUT 单调查询解析、加载期账本验真、E5-A 原 ThinkSession 恢复、多轮和崩溃恢复；三项监工阻断、E4/E5 HTTP 前置拒绝 Windows 连接竞态和日期相关证据均已返修。最终证据包括定点矩阵 16/16、P02 专项 47/47、相关链路 143/143及 Engine 全量连续三轮 444/444。

**边界：** P02 验收的是 Engine 独立模型能力闭环，不是日常真实 Provider 或生产 World Adapter 接入。模型候选仍须经 CapabilityResult → 原 ThinkSession → Thinking → Action → ReplyComposer；Subject Authority 不变。P02 Vio dependency = `NONE`；Real Provider/Vio/PWA production integration = `DEFERRED_TO_P22`；P03—P23 保持 `NOT_STARTED`。软件版本继续为 `0.1.0`。

**未修改与停止点：** 本决定不修改生产代码、测试、六份冻结 Schema、`pyproject.toml`、HTTP/API/Capability/Binding 契约、正式数据、Execution Chain、Reality Boundary 或 P00—P23 职责。完成档案与只读检查后停止，等待用户另行确认 Git 操作。

## D-041：P03 Event 客观历史权威、三时间与只读 Timeline

**决定：** 用户在 P02 `ACCEPTED` 后独立授权 P03。Event 继续是 Engine 客观历史事实权威，SubjectState 继续是当前主体状态唯一权威；P03 不建立第二 Event Store。Event 原位增加 occurredAt、observedAt、recordedAt 三时间及明确 time basis，增加分类、来源类别、sourceEventId、correlationId、结构化证据和 caused_by/corrects/revokes 引用。Timeline 只能从 StateUpdateRecord/Event 历史在内存中重建，没有 save、update、delete 或 overwrite 权限。

**时间与身份：** 显式三时间必须带时区、规范化为 UTC 并满足 occurredAt ≤ observedAt ≤ recordedAt。`SubjectStateService.apply_event()` 对一次应用只读取一次权威时钟；显式 Event 还必须满足 recordedAt ≤ appliedAt，未来 recordedAt 失败关闭，同一 appliedAt 传给 Evolution。旧调用只有 occurredAt 时确定性合并缺失时间并标记 `LEGACY_COALESCED`，只缺一个时间时标记 `PARTIALLY_COALESCED`；调用方不能虚假声明 `EXPLICIT`，合并时间也不能伪称独立测量。eventId 是内部不可变身份，sourceEventId/correlationId 不能替代唯一性或绕过 Action Gate。

**追加与幂等：** 相同 eventId 加完全相同 canonical body 是幂等重放，不产生第二 Event、mutation、update 或 revision；不同 body 失败关闭。`apply_event` 先加载当前 state/history 并完成 eventId/canonical identity 判定，只有确认是新 Event 后才读取 applied clock 和执行 future recordedAt 门；因此重启、Frozen Clock 或系统时钟回退不能破坏已提交事件的精确重放，冲突重复也不能被时间错误遮蔽。重放结果显式标记 `idempotent_replay`，并同时返回当前 state 与原历史 update，避免把历史 after_revision 冒充当前 revision。correction/revocation 只能追加新 Event 并引用同 Subject、在写入历史中已经存在的目标；前向、缺失和跨主体引用在保存、加载和 Timeline 重建时均失败关闭，原 Event 永不删除或改写。

**历史分类与 Action Gate：** 新 P03 分类 FACT、OBSERVATION、INTENTION、CORRECTION 和 REVOCATION 无论是否迟到都只能无 mutation 追加，不能直接改变当前 SubjectState。新显式 StateMutation 只属于 STATE_CHANGE；既有 LEGACY/interaction 兼容不扩大也不重写。若历史事实需要影响当前状态，必须由当前合法 Action Gate 形成新的 STATE_CHANGE Event，经 Evolution 推进 revision，并用 caused_by 引用历史事实。PlatformObservation 的 sourceEventId 不会自动成为内部 Event。

**Timeline 语义：** Timeline 按 occurredAt → observedAt → recordedAt → eventId 确定性排序，支持发生时间闭区间、first/last、距离、链和来源/分类/correlation 过滤，默认 50、可配置 20—100。修正/撤销状态只在投影中派生，不回写 Event；revocation 对被指向目标保持终态优先。重启后只能从同一 Event 历史重建。

**边界和状态：** P03 Vio dependency = `NONE`，不访问网络、真实 Provider、凭据或 Vio；六份冻结 Schema、HTTP/API/Capability/Binding 契约、正式数据和软件版本不变。P03、P03 Engine side 与 P03-01—P03-12 当前只能为 `IMPLEMENTED_NOT_ACCEPTED`；本决定不授权 P04、P10、Git 暂存、提交、推送或 tag。

**未来是否可修改：** 只有新的版本化规划和用户决定才能改变 Event/SubjectState/Timeline 权威边界。P04 可消费只读 Timeline，但不得把 DerivedSummary 或 Memory 变成事实权威；P11 Scheduler、P20/P21 恢复和 P22 重连必须按既定阶段另行施工。

## D-042：用户正式验收 P03，但不授权 P04、P10、Vio 或 Git 操作

**决定：** 用户于 2026-08-30 正式验收 P03。P03、P03 Engine side 与 P03-01—P03-12 当前全部为 `ACCEPTED`；P03 Vio dependency 保持 `NONE`。该状态来自用户明确验收决定，不是由本地测试自动推导。

**验收范围：** P03 验收覆盖 Event 三时间、分类/来源/证据、稳定身份、追加式 correction/revocation、精确幂等、恢复时钟无关重放、只读 Timeline、版本化 Fixture、Golden Scenario，以及三项规划监工阻断返修。开工 444、初版 22/469、两项返修 28/475 和恢复返修 31/478 的历史证据均保留。

**权威边界：** Event 继续是客观历史事实权威，SubjectState 继续是当前主体状态唯一权威，Timeline 继续只是 Event/StateUpdateRecord 历史的只读派生投影。P03 不建立第二 Event Store，也不把 Memory、Self-Narrative 或 Timeline 提升为事实权威。

**授权边界：** 本决定只授权 P03 验收状态落盘与提交前最终核查，不授权 P04、P10、Vio、网络、真实 Provider、凭据、`git add`、commit、push 或 tag。P04—P23 保持 `NOT_STARTED`；软件版本继续为 `0.1.0`。

## D-043：P04 Memory Consolidation、DerivedSummary 与证据链边界

**决定：** 用户于 2026-08-30 在 P03 `ACCEPTED` 后独立授权 P04——Memory Consolidation 与 DerivedSummary 的 Engine 独立施工。P04 原位扩展既有 Memory 领域与服务边界，建立正式 Memory 记录、根证据链、单一 MemoryRepository、Consolidation、可解释 activation/温度、追加式纠错传播和可重建 DerivedSummary；不得建立第二 SubjectState、Event Store、Memory Store 或 Summary Authority。

**Authority：** SubjectState 继续是当前主体状态唯一权威，Event/StateUpdateRecord 继续是客观历史事实权威，Memory Store 只权威保存正式 Memory 记录及其证据链，Timeline 继续是 Event 历史只读投影，DerivedSummary 只是 Memory 内部可重建的有损压缩视图。Consolidation、Summary、检索和温度迁移没有 Event、StateMutation、Evolution 或 SubjectState revision 写权限；Summary 不能成为 Event、Raw Source、Confirmed Memory、Self-Narrative 或独立 Learning 证据。

**保留与生命周期：** P04 不自动过期或物理删除 episodic、semantic、relational Memory。HOT/WARM/COLD/ARCHIVED 用于控制活跃度和召回距离；HOT 300、WARM 3000、COLD 30000 只是可配置默认容量。容量达到阈值时由显式调用重算 importance、recent access、relation relevance、emotional weight、unique reinforcement 和 time decay，并优先降温，不能删除最旧数据。P04 不建设后台线程或 P11 Scheduler。

**可见性与隔离：** 正式默认可见性为 `ENGINE_PRIVATE`。本阶段只允许 Engine 内部服务、临时 TEST 数据根和授权只读测试入口访问；不访问或依赖 Vio、网络、真实 Provider、API Key、外部记忆库、MCP、向量服务或正式 `.continuity-data`。这一隔离只属于 P04 施工与测试，不是长期产品禁令：P05 Context Router、P06 Context Composer、P16 外部长记忆/MCP/Skill 和 P22 Provider/Vio/PWA World Adapter 仍按既定顺序开放。

**证据与去重：** 正式 evidence type 固定为 experiential、analytical、external；episodic 只作为 Memory kind。相同 consolidation identity 与相同 canonical 输入精确幂等，不同输入身份冲突；同一底层 root evidence 经 Event、Memory、Summary、检索、重建或重启到达时只贡献一次。只有新的独立根证据可以增加 evidence count/reinforcement。冲突证据在 P07 前失败关闭，不能静默覆盖、投票或裁决。

**纠错、撤销和删除传播：** correction、revocation、deletion 只以追加式 Memory 版本和 lineage 墓碑传播；原 Memory 历史与 Event 均不删除、不原地改写。依赖失效来源的 DerivedSummary 必须追加失效状态，重建只使用当前有效来源。P04 不提供永久删除命令；Intentional Forgetting 属于 P12，生产级永久删除、备份和恢复属于 P20/P21。

**监工返修后的当前影响：** 普通检索只返回 HOT/WARM/COLD，ARCHIVED 仍在单一仓储中供显式 load、维护、审计和授权 trace 使用，不等于删除或失效。MemoryCandidate 的 root/source/kind/evidence/visibility/status/temperature/version 等正式 provenance 由仓储候选密封，调用方 metadata 不能覆盖、伪造或删除。每个 consolidation identity 均在同一 Memory 原子文档保存 canonical input hash/body 与 result Memory identity，重复根 alias 也必须跨重启精确重放，冲突输入失败关闭。DerivedSummary 的 type、scope、来源、根证据、time range、confidence 和生成正文全部参加精确幂等；稳定 type/scope 冲突失败，合法来源或派生变化追加 SUPERSEDED/ACTIVE 版本。lineage 根必须包含来源 Event 的稳定根，并且其余根只能来自目标、合法 replacement 或既定来源链；保存与每次加载都重新验真。以上是 D-043 既有边界的实现闭合，不是 D-044 验收决定。

**operation 精确结果与 alias 来源链补充：** 每个 consolidation operation 还必须在同一原子 Memory 文档中绑定首次 `result_memory_id`、`result_memory_revision` 与 result canonical hash；exact replay 只能从 append-only Memory history 返回该确切版本，不能因后续 reinforcement、terminal propagation 或重启而漂移到 latest。operation canonical input 无论最终是否映射到既有 Memory，都必须独立验证自身 Event/Message/Memory 来源链；source Memory 必须已经存在、同 subject/environment 且不是前向引用，根证据必须从 candidate 自身声明的 source chain 完整追溯。保存与每次加载均重新验证缺失/前向/跨边界来源及 result revision/hash 篡改。这仍属于单一 Memory Authority 内的失败关闭，不新增 D-044 或第二账本。

**状态与授权门：** P04、P04 Engine side 与 P04-01—P04-12 的本地实现和证据完成后只能为 `IMPLEMENTED_NOT_ACCEPTED`，D-044 保留给未来用户正式验收。P04 Vio dependency = `NONE`；P05—P23 保持 `NOT_STARTED`。本决定不授权 P05/P06/P07/P10/P11/P12/P16/P22、Vio、网络、真实 Provider、Git 暂存、提交、推送或 tag，也不修改六份冻结 Schema、HTTP/API/Capability/Binding 契约、`pyproject.toml`、版本或正式数据。

## D-044：用户正式验收 P04 Memory Consolidation 与 DerivedSummary

**决定：** 用户于 2026-09-01 明确正式验收 P04。P04、P04 Engine side 与 P04-01—P04-12 当前全部为 `ACCEPTED`，P04 Vio dependency = `NONE`；该状态来自用户明确验收决定，不是由本地测试自动推导。P00—P03 继续 `ACCEPTED`，P05—P23 继续 `NOT_STARTED`。

**Authority：** Memory Store 是正式 Memory 记录及其证据链的唯一权威。SubjectState 继续是当前主体状态唯一权威，Event/StateUpdateRecord 继续是客观历史事实权威，Timeline 继续是只读 Event 历史投影。DerivedSummary 只是同一 Memory 仓储内可重建的有损压缩视图，不是第二事实、状态、Memory、Summary 或 Learning Authority，也不能创建 Event、StateMutation、Evolution 或推进 SubjectState revision。

**保留、温度与删除：** HOT/WARM/COLD/ARCHIVED 只表达活跃度和召回距离，不表达事实真伪，也不授予删除许可。P04 没有自动过期、物理删除、后台 Scheduler 或永久删除命令；容量维护只通过显式入口重算 activation 并降温。correction、revocation、deletion 继续使用追加式 Memory 版本、lineage 墓碑和依赖摘要失效，原 Event 与 Memory 历史不得删除或原地改写。

**Operation 与 provenance：** consolidation operation 精确绑定 canonical input、首次 result Memory identity、revision 与 canonical hash，跨进程重启和后续 terminal propagation 后仍从同一 append-only Memory history 精确重放首次结果。duplicate-root alias candidate 必须按自身 Event/Message/Memory 来源链验真，不能借用结果 Memory provenance；保存和加载均对缺失、前向、跨 subject/environment、不可追溯根与篡改失败关闭。

**Learning 与 Summary 证据：** Learning 按底层 root evidence identity 去重；同一证据经多个 Memory、Summary、重建、检索或重启到达时只贡献一次。DerivedSummary 自身不构成独立事实或独立 Learning 证据，只有真正独立的根来源可以增加 evidence count 或 reinforcement。

**长期开放边界：** P04 的无 Vio、网络、真实 Provider、凭据、MCP、外部数据库隔离只属于本阶段施工和测试，不是长期产品禁令。后续仍按既定顺序由 P05 开放 Context Router、P06 开放 Context Composer/Authority 分层、P16 开放外部长记忆/知识/MCP/Skill、P22 开放真实 Provider 与 Vio/PWA World Adapter 生产重连。

**授权与停止点：** 本决定只授权 P04 纯档案验收闭合与提交前核查，不授权 P05、P10、Vio、网络、真实 Provider、`git add`、commit、push、merge、rebase、tag 或 release。软件版本继续为 `0.1.0`，六份冻结 Schema、外部 HTTP/API/Capability/Binding 契约、正式 `.continuity-data` 和其他仓库均不得修改。

## D-045：P05 Context Router 职责、预算、权限与只读 Trace 边界

**决定：** 用户于 2026-09-01 在 P04 `ACCEPTED` 后单独授权 P05——Context Router 的 Engine 独立施工。P05 位于结构化 `PerceptionResult` 之后，只负责决定“查哪些逻辑分区、向哪些已授权来源取多少候选”，并产生确定性的 `RoutePlan`、`CandidateManifest` 与 `ContextTrace`。Router 不负责 P06 Context Composer、最终 Thinking 输入、五层 Context Authority 或 token 级裁剪；现有 `ThinkingProvider.think(PerceptionResult, TokenBudget)` 外部兼容接口保持不变。

**Authority 与来源：** SubjectState、Event/StateUpdateRecord、Memory Store、Timeline 和 DerivedSummary 各自既有 Authority 不变。Router 只读消费 SubjectState 分区、P04 Memory/DerivedSummary、P03 Timeline/Event 投影和 Engine 本地授权的版本化候选；“小抽屉”是一次逻辑选择，不复制数据、不建立第二 Store、事实权威、状态权威或 Trace Authority。Attention、Desire、Conflict、Somatic、Expression 等未来分区在来源尚未实现时只能明确标为 UNAVAILABLE/FEATURE_GATED，不得伪造内容。

**权限、预算和确定性：** 默认只允许 routing request、source binding 和 candidate 具有同一 subject/environment 且 visibility 为 `ENGINE_PRIVATE`。权限未知、跨主体/环境、不可见、缺失、撤销、删除、失效、被替代或 version/hash/revision 漂移均 fail closed；候选在保留引用前必须再次验证。Retrieval Budget 与 Storage Budget、未来 P06 Context Budget 分离，候选默认 50、可配置 30—80，并支持每来源上限；Router 禁止在来源失败时全库扫描、扩大范围或绕过权限。排序结合 purpose、relevance、recency、importance、activation，并以稳定 partition/source/candidate/version/hash 打破平局。

**Trace 与副作用边界：** Context Trace 只保存 request/subject/revision/UTC 时间、目的、signal hash、打开/未打开分区、来源范围、计数、权限、预算、版本、排序解释与原因码；不保存原始秘密、凭据、Authorization、完整敏感正文或可复用认证材料。Trace 不反向成为 Router、Thinking、Evolution、Memory 或 SubjectState 的输入。Router 无创建 Event、StateMutation、Evolution、Memory、Summary 或推进 revision 的写入口，普通路由也不写 access/reinforcement/Learning。

**阶段隔离和状态：** P05 Vio dependency = `NONE`。施工与测试只使用 Engine 本地版本化 synthetic TEST/RESEARCH Fixture，不访问 Vio、网络、真实 Provider、API Key、凭据、MCP、外部数据库、向量服务或正式 `.continuity-data`。这一隔离不是长期产品禁令：P06、P16、P22 仍按阶段分别开放 Composer/Authority、外部知识能力和真实 Provider/Vio/PWA production integration。P05、P05 Engine side 与 P05-01—P05-12 完成后最多为 `IMPLEMENTED_NOT_ACCEPTED`；D-046 保留给未来用户正式验收。本决定不授权 P06、P10 或任何 Git 写操作。

**规划监工阻断返修补充：** purpose/signals 必须先确定本轮打开的逻辑分区；固定必需来源必须由显式规则加入。未选分区统一为 `NOT_OPENED` 并记录稳定原因，来源调用次数必须为零，候选自身 relevance 不得反向打开分区。总 Retrieval Budget 必须在任何 adapter 调用前确定性分配，各来源 requested limit 之和不得超过单轮总预算；Trace 分别记录 requested、retrieved/evaluated、retained、rejected，不得以最终 Manifest 数量冒充读取量。Memory/DerivedSummary 使用仓储边界内有界只读查询，Timeline 初读和重验绑定同一查询窗口；任一必需来源被拒绝、失效或失败时，Route Result 必须失败关闭并返回空的不可消费 Manifest。以上属于 D-045 原有 P05 职责的验真和闭合，不创建 D-046，也不改变任何 Authority 或 P06 边界。

## D-046：用户正式验收 P05 Context Router

**决定：** 用户于 2026-09-03 明确正式验收 P05。P05、P05 Engine side 与 P05-01—P05-12 当前全部为 `ACCEPTED`；P00—P04 继续 `ACCEPTED`，P05 Vio dependency = `NONE`，P06—P23 继续 `NOT_STARTED`。`PLANNING_CONFLICT = NONE`，`EVIDENCE_CONFLICT = NONE`。

**验收内容：** Context Router 在结构化 Perception 后先按 purpose/signals 选择逻辑分区，再于 adapter 读取前确定性分配总 Retrieval Budget；未选来源零读取。Memory/DerivedSummary 通过 P04 单一仓储的有界只读查询端口取候选，Timeline 初读和重验绑定相同的近期相关稳定窗口。Candidate Manifest 与 Context Trace 只保存稳定引用、版本/hash、排序和 requested/retrieved/evaluated/retained/rejected 证据；必需来源被拒绝、失效或失败时 Manifest 为空。Router 不创建或修改 SubjectState、Event、Memory、DerivedSummary、Timeline、Thinking、Evolution 或任何权威 Store。

**证据与历史：** 当前证据为 P05 专项 39/39、直接相关链路 125/125、P01—P05 综合矩阵 244/244、Engine 全量连续三轮 548/548，以及档案后终局 P05/全量复跑。初版 35/122/240/544、四项监工阻断反例、定点返修和施工时 `IMPLEMENTED_NOT_ACCEPTED` 状态继续作为真实历史保留；本决定的 `ACCEPTED` 来自用户明确验收，不由测试自动推导。

**长期开放与授权边界：** P05 的 Vio/网络/Provider/正式数据隔离只属于本阶段施工和测试，不是长期禁止 Memory 参与 Thinking/Context。P06、P16、P22 仍按既定阶段分别开放 Context Composer/Authority、外部长记忆/知识/MCP/Skill 与真实 Provider/Vio/PWA production integration。本决定不授权 P06、P10、Vio、真实 Provider、网络、凭据或任何 Git 暂存、提交、推送、合并、rebase、标签或发布操作；软件版本保持 `0.1.0`。

## D-047：P06 Context Composer、可信 Authority 分层与独立 Context Budget

**决定：** 用户于 2026-09-03 在 P05 `ACCEPTED` 后独立授权 P06——Context Composer 与 Authority 分层的 Engine 独立施工。P06 只消费 P05 已完成且可消费的 RoutePlan、Candidate Manifest 与 Context Trace，通过 Manifest 中的稳定引用精确解析材料，执行验证、去重、确定性排序和独立 Context Budget，并产生 Thinking-ready 的 `ComposedContextSnapshot` 与不含正文的 `CompositionTrace`。P06 不重新选择分区、不扩大检索、不建立 Context Store，也不修改 P05 的 purpose、分区、Retrieval Budget、Manifest、Trace 或失败关闭语义。

**Authority：** Context Authority 固定为 `confirmed_state`、`confirmed_memory`、`derived_summary`、`retrieved_candidate`、`raw_source` 五类。Authority 由 Composer 配置的可信 resolver/source binding 决定，候选自报的 `authority_label` 不可信，不能提权。SubjectState 仍是当前主体状态唯一权威，Event/StateUpdateRecord 仍是客观历史事实权威，Memory Store 仍是正式 Memory 记录与证据链权威，Timeline 仍是只读投影，DerivedSummary 仍是可重建有损视图。Authority 不是简单数值覆盖规则；raw source 与既有 State/Memory 不一致时双方及 provenance 都保留，P06 不替 P07 裁决，也不产生 Event、StateMutation、Evolution 或 revision。

**精确解析与失败关闭：** Composer 只允许 resolver 按 Manifest 既有 source/stable ID 精确加载，并逐项验证 subject、environment、source、version/revision、content hash、可见性与当前状态。Manifest 外材料不得读取；Route Result 为 REJECTED、INCOMPLETE 或 FEATURE_GATED 时不可消费且 resolver 读取次数为零。必需材料失效或漂移时返回不可消费结果；可选材料可排除，但必须以稳定原因写入 Trace。材料正文始终按 data 处理，不能成为系统指令或改变 Authority/策略。

**Context Budget、去重与可追溯性：** Context Budget 与 Storage Budget、P05 Retrieval Budget、Thinking/Provider TokenBudget 分离，默认核心片段 10（可配置 6—15），无 P05 hint 时本地 token cap 默认 2048。token/cost 由可替换的确定性 estimator 计算，先验证和精确去重，再裁剪。confirmed_state 中受保护的 identity/continuity/relationship 必须优先保留；预算不足以容纳必需上下文时返回 `INSUFFICIENT_CONTEXT_BUDGET`，不得静默删除。Summary 与 raw source 不因正文相似被合并；精确重复只保留一次且 provenance 不丢失。Snapshot/Trace 保存稳定引用、hash、Authority、预算与原因码，不保存凭据、完整敏感正文或 Provider hidden Chain-of-Thought。

**阶段隔离与状态：** P06 只形成结构化 Thinking-ready snapshot，不修改 `ThinkingProvider.think(PerceptionResult, TokenBudget)` 兼容接口，不接入 P02/E5-A、生产 Thinking 或真实 Provider；P09 才负责正式 C1 运行链接线。P06 Vio dependency = `NONE`，不访问 Vio、网络、凭据、MCP、外部数据库或正式 `.continuity-data`；该隔离不是长期产品禁令，P09/P16/P22 仍按阶段开放核心接线、外部知识能力和真实 Provider/Vio/PWA production integration。P06、P06 Engine side 与 P06-01—P06-12 完成后只能为 `IMPLEMENTED_NOT_ACCEPTED`；D-048 保留给未来用户正式验收。本决定不授权 P07、P09、P10 或任何 Git 写操作。

**规划监工验收阻断返修补充：** `CompositionTrace.missing_count` 只统计 Manifest candidate 的解析缺失，`upstream_notice_count` 只统计 P05 分区 notice；二者不互相充数，candidate 覆盖公式固定为 `resolved_count + missing_count == candidate_count`。Composer 自己记录 exact resolver 总读取次数及按 source ID 的稳定分解，Feature Gate 关闭、非 COMPLETE Route 和未引用来源均为零；该审计进入规范序列化、hash 和篡改校验。内置 State、Memory、DerivedSummary、Timeline 与 local/test resolver 分别保留 `SOURCE_MISSING`、subject/environment mismatch、status/visibility、version/hash drift、content/provenance invalid 等稳定原因，required/optional 只决定结果是否可消费，不覆盖真实失败原因。以上属于 D-047 原有 Trace、精确解析和失败关闭职责的验真，不创建 D-048，不改变 P05、Authority、Thinking/E5-A 或阶段边界。

## D-048：用户正式验收 P06 Context Composer 与 Authority 分层

**决定：** 用户于 2026-09-03 明确正式验收 P06。P06、P06 Engine side 与 P06-01—P06-12 当前全部为 `ACCEPTED`；P00—P05 继续 `ACCEPTED`，P06 Vio dependency = `NONE`，P07—P23 继续 `NOT_STARTED`。`PLANNING_CONFLICT = NONE`，`EVIDENCE_CONFLICT = NONE`。该状态来自用户明确验收决定，不是由测试自动推导。

**Authority 与消费边界：** P06 只消费 P05 `COMPLETE` Route Result 中可消费 Manifest 的精确引用；Authority 只能由 `TrustedContextResolverBinding` 密封为 confirmed_state、confirmed_memory、derived_summary、retrieved_candidate、raw_source，候选自报标签和正文不能提权或改变策略。Composer 不重新检索、不扩大分区、不建立 Context Store，也不创建或修改 SubjectState、Event、Memory、DerivedSummary、Timeline、Evolution 或 revision；所有 fragment 的 `direct_state_write_allowed` 均为 false。

**预算、失败关闭与 Trace：** P06 Context Budget 与 Storage Budget、P05 Retrieval Budget、Thinking/Provider TokenBudget 相互独立；受保护 identity/continuity/relationship 不能被普通裁剪删除，required 材料失效或预算不足时不产生可消费 Snapshot。CompositionTrace 将 Manifest candidate missing、P05 upstream notice 与 Composer 自有 resolver reads 分开计数；验收 Golden 为 candidate/resolved/missing/upstream notice `18/18/0/5`、decision MISSING `0`、resolver read `18` 且按 source 稳定分解。State、Memory、DerivedSummary、Timeline、local/test resolver 保留互不混淆的 missing、subject/environment、status/visibility、version/hash、content/provenance 稳定原因。

**证据与历史：** 用户验收依据包括规划监工独立 P06 38/38、Engine 全量 586/586，以及 Authority 密封、Manifest-only 精确解析、独立 Context Budget、失败关闭、无生产 Thinking 接线和底层零写入复核。初版 33/72/160/277/581、监工反例、两项阻断、返修后 10/38/77/165/282/586 和施工时 `IN_PROGRESS`/`IMPLEMENTED_NOT_ACCEPTED` 状态全部作为历史保留，不删除或倒写。

**长期开放与授权边界：** P06 的 Engine 本地、无 Vio/网络/Provider 隔离只属于本阶段施工和测试，不是长期产品禁令。P09、P16、P22 仍按既定顺序分别开放正式 Thinking 运行链接线、外部知识能力及真实 Provider/Vio/PWA production integration。本决定不授权 P07、P09、P10、Vio、网络、真实 Provider、凭据、MCP、外部数据库或任何 Git 暂存、提交、推送、合并、rebase、标签或发布操作；软件版本保持 `0.1.0`。

## D-049：P07 三类矛盾检测、证据隔离与非权威审计边界

**决定：** 用户于 2026-09-03 在 P06 `ACCEPTED` 后独立授权 P07——Epistemic / Evidential / Cognitive Contradiction Detector 的 Engine 独立施工。P07 只消费完整、可消费且完整性校验通过的 P06 `ContextCompositionResult`，不重新执行 P05 路由、不重新检索、不读取 Manifest 外材料。P07 只检测 `EPISTEMIC`、`EVIDENTIAL`、`COGNITIVE` 三类矛盾；Ambivalence、爱与恨、靠近与逃离等 Psychological Conflict 明确排除，继续属于 P14/P15。

**Authority 与 claim 封印：** SubjectState 继续是当前主体状态唯一权威，Event 是客观历史事实权威，Evolution 是合法状态变化过程；Memory、DerivedSummary、Timeline 与 P06 Authority 均保持既有边界。P07 通过宿主中立 `StructuredClaimResolver` 接收 test-only/未来可信结构化语义投影，但 claim 的 fragment、subject/environment、Authority、source identity/version/hash、provenance roots 和时间只能由 P06 fragment 密封，正文、自报标签和 conflict marker 不能提权或替代真实 claim/evidence 校验。无法形成可信结构化 claim 的材料只记为 `UNASSESSED`；Psychological claim 只记为 `NOT_APPLICABLE`。

**检测、隔离与零写入：** 同一 proposition/scope 内不兼容的结构化 claim 才能形成 case；不同 proposition、subject/environment、time scope、精确重复及明确同源版本 supersession 不误报。发现矛盾后不删除证据、不按 recency/confidence/Authority 静默选 winner，而以 contested、isolated、downgraded 或 retained-disputed disposition、verification task 和稳定 reason code 显式表达。所有 case/result 固定 `direct_state_write_allowed=false`、`evolution_commit_allowed=false`；P07 不创建或修改 SubjectState、Event、Evolution、Memory、DerivedSummary、Timeline、P06 snapshot 或 revision，P09 才负责未来正式运行链接线。

**生命周期与持久化：** P07 case 支持 `OPEN`、`ISOLATED`、`PENDING_VERIFICATION`、`RESOLVED`、`REOPENED`、`SUPERSEDED` 的受约束追加式审计。解决只接受同 subject/environment、明确 basis/reference、exact source hashes 且 `verified=true` 的结构化依据；仅凭较新、分数更高或 Authority 更大不能解决。新独立冲突证据可 reopen，旧 claim、隔离、核实和 resolution 全部保留。`JsonContradictionRepository` 只是 TEST/RESEARCH/Engine 本地的非权威、可验证审计组件，以 canonical JSON/hash、原子替换、单调 revision、精确 replay、重启恢复和加载期 transition/边界验真提供证据，不形成第二事实、状态、Memory、Timeline 或 Context Store。

**隔离、状态与授权门：** P07 Vio dependency = `NONE`；施工与测试不访问 Vio、网络、真实 Provider、凭据、MCP、外部数据库或正式 `.continuity-data`。这一隔离只属于 P07 施工和测试，不是长期产品禁令。P07、P07 Engine side 与 P07-01—P07-12 完成后最多为 `IMPLEMENTED_NOT_ACCEPTED`；D-050 保留给未来用户正式验收，本轮不得创建或使用。P08—P23 保持 `NOT_STARTED`；本决定不授权 P08/P09/P10、Vio、真实系统或任何 Git 暂存、提交、推送、合并、rebase、标签或发布操作。

**规划监工阻断返修补充：** 独立复核在首轮 35/35 后证明公共 `verified=True` 可自证 resolution、COMPLETE result 可拼装跨主体 case、repository 会明文保存 canonical value、旧版本可自报 supersede 新版本。上述四项使当时 `EVIDENCE_CONFLICT = PRESENT`，不删除首轮通过历史。P07 阶段内返修固定：五类 resolution basis 只能经可信 `ResolutionEvidenceVerifier` 密封；`reopen()`/`supersede()` 不接受裸 hash；Result 必须封印 case/trace 的 subject、environment、snapshot 和 detector version；持久化只保存 `canonical_value_hash`；supersession 只能由可信 source-version binding 证明严格新版本替代旧版本。返修不改变 Authority、阶段职责或 D-050 验收门。

**2026-09-04 第二轮监工阻断补充：** 46/632 通过后仍发现 resolution 审计可跨 subject/environment、合法更正证据可解决无关命题两项缺陷；两个正式回归在修复前均失败，当时 `EVIDENCE_CONFLICT = PRESENT`。本次把可信证据适用的 proposition/scope 与预登记来源 identity/version/hash/provenance/claim 值 hash 独立绑定，禁止由当前待解决 case 反向生成“可信”适用范围。验证输出绑定 target case ID、原 revision/hash、snapshot、detector version、完整 claim hashes、适用 claim hashes 和单一 allowed action；audit 的 action/reason/source hashes/time 必须与其一致。domain、service、repository append/from_dict/load/detect replay 共用语义门，format v3 仓储每次重新调用可信 verifier 验证持久化证明，而非仅信任自洽 hash。resolve/reopen/supersede 和五类 basis 正向链继续保留。旧 v2 非权威测试审计缺少目标封印时拒绝，不静默升级；正式数据不迁移。D-049 仅补充 P07 阶段内决定，不创建 D-050，不改变 Authority 或授权后续阶段。

## D-050：用户正式验收 P07 Contradiction Detector

**决定日期与授权来源：** 2026-09-04，用户明确表示“我正式验收 P07”。规划监工已独立确认第二轮两个阻断反例均被拒绝，合法生命周期及重启重放通过；独立 P07 56/56、Engine 全量 642/642 PASS。本决定登记用户正式验收，不以测试通过自动代替用户决定。

**正式状态：** P00—P07 = `ACCEPTED`；P07 Engine side = `ACCEPTED`；P07-01—P07-12 = `ACCEPTED`（12/12）；P07 Vio dependency = `NONE`；P08—P23 = `NOT_STARTED`；`PLANNING_CONFLICT = NONE`；`EVIDENCE_CONFLICT = NONE`。D-049 保留开工及两轮返修决定；D-050 本次正式创建并使用。

**验收边界：** P07 只消费封印通过的 P06 COMPLETE Context，经可信结构化 claim 绑定检测 EPISTEMIC、EVIDENTIAL、COGNITIVE；Psychological Conflict 明确排除，不按 Authority、confidence 或 recency 选择 winner。SubjectState、Event、Evolution、Memory Store、DerivedSummary、Timeline 和 P06 Authority 不变。P07 非权威 append-only repository 只保存密封 value hash 与必要来源/审计引用；不建立第二事实、状态、Memory、Timeline 或 Context Authority。全部输出继续固定 `direct_state_write_allowed=false`、`evolution_commit_allowed=false`。

**两轮阻断验收依据：** 五类 resolution basis 必须经可信 verifier 验真；证明绑定适用 proposition/scope、目标来源及具体 case/revision/hash/snapshot/action。audit 与所属 case 的 subject/environment、action、reason、source hashes、时间和证据必须一致，仓储追加、反序列化及重启重放均重新验真。reopen/supersede 不能依赖裸 hash；COMPLETE Result 不能跨边界拼装；canonical 明文不落盘；supersession 必须由可信版本绑定证明方向。合法五类 basis 与 resolve/reopen/supersede 不受影响。

**历史保留：** 开工 586/586、初版 35/112/200/317/621、首轮四项失败与返修 46/123/211/328/632、第二轮两项失败与返修 56/133/221/338/642，以及无效测试入口、系统中断和当时的 `IN_PROGRESS`/`IMPLEMENTED_NOT_ACCEPTED`/`EVIDENCE_CONFLICT=PRESENT` 均作为历史保留。本决定只取代现行待验收状态，不倒写旧记录。

**本次允许与禁止：** 仅更新验收档案并进行只读一致性和回归核查，不修改运行代码、测试、Schema、HTTP/API/Capability/Binding 契约、正式数据、`pyproject.toml` 或软件版本 `0.1.0`。不授权 P08/P09/P10、Vio、网络、真实 Provider、凭据、MCP、外部数据库或任何 Git 暂存、提交、推送、合并、rebase、标签和发布操作。现有未提交成果由用户自行决定提交和推送。

**长期开放边界：** P07 的 Engine 本地隔离是本阶段施工/测试边界，不是长期产品禁令；P09/P16/P22 仍按既定顺序开放正式运行链接线、外部知识能力与真实 Provider/Vio/PWA production integration。

## D-051：P08 Direct/Optional Planner 与 E5-A 内部有限泛化

**授权：** 2026-09-04，用户授权 P08；开工核查发现现有 E5-A 仅接受 model.generate/Thinking 恢复，先停止并报告规划边界。用户明确选择方案 A，补充授权最小内部类型、恢复归属和同一持久化通道的类型分派；此前阻断已由本决定解决，不代表实现风险自动消除。

**身份映射：** 一次可信 Thinking Need/Decision 绑定 P06 COMPLETE snapshot、subject/environment、来源及稳定 decision identity。简单 Direct Action 不创建 Goal/Plan；复杂行动生成确定性 plan/step 结构。每个 Direct Action 或 Plan Step 对应唯一 operation、capability request 和恢复目标；父 decision/plan identity 不替代步骤唯一性。attempt/result 仍保存在 E5-A 同一个 capability ledger，计划状态只能由该账本投影，计划不成为第二份执行事实。内部请求使用明确非模型类型，外部 Schema/validator/HTTP 仍只允许原模型契约。

**兼容与安全：** 旧模型记录按原格式加载和重放，不迁移正式数据、不改变身份/hash/计费；model.generate 继续恢复原 ThinkSession，不进入 Planner。非模型结果只恢复对应 Action/Step，经相同 Capability、Permission/Confirmation、Resource、Recoverability、Reality Boundary 门后才能进入本地 Fake Adapter。UNKNOWN 先查可验证回执，无结论则保持待核实；不宣称任意生产 Adapter exactly-once。复用既有 Action 权限/风险/资源检查与唯一 Evolution Authority，不增加第二执行请求账本。

**阶段门：** P00—P07 = ACCEPTED；P08 / Engine side / P08-01—P08-12 开工为 IN_PROGRESS，完成后最多 IMPLEMENTED_NOT_ACCEPTED。P08 Vio dependency = NONE；P09—P23 = NOT_STARTED。D-052 保留用户正式验收，不创建。禁止真实网络、Vio、Provider、凭据、MCP、外部数据库、生产 Adapter、P09 正式接线、P17 完整执行引擎以及所有 Git 写操作。SubjectState/Event/Memory/Timeline/Evolution Authority、六份冻结 Schema、外部契约及 0.1.0 版本不变。

**阶段内返修与收口补充：** 两个新增正式回归先后证明待恢复 Adapter/policy 未绑定和步骤间 Context 失效未重验，均保留失败事实。请求现密封 Adapter ID、能力版本/权限/成本/原子性 canonical hash；每次新执行前重新验证 Context。初版 32、第一次修复 33 与最终 36 项专项历史保留；307 直接链路、374 综合、连续三轮 678 全量通过。本地 Fake 原子回执的幂等结论不得扩大成生产 exactly-once。P08 / Engine side / P08-01—P08-12 现为 IMPLEMENTED_NOT_ACCEPTED，两个 conflict 为 NONE；D-052 未创建，等待规划监工及用户验收，不授权任何 Git 写操作。

**2026-09-04 监工验收阻断返修补充：** 36/678 之后独立发现“仅 Engine ledger 自洽伪造成功放行后续 contact”和“Context 失效挡住原已发生效果归账”。先以两个正式失败回归记录，当时 `EVIDENCE_CONFLICT = PRESENT`，不倒写此前通过结论。本次仍在方案 A 有限授权内，仅调整 P08 service 与 E5-A 内部 coordination 的现有类型分派：每次消费带 receipt 的历史、重启终态重放及追加结果前，均经原绑定 Adapter query 独立核实；存储 hash 不充当真实执行证明。原输入封印和恢复归属始终验证，当前 Context 失效时仅可查询/归档原请求事实，不可创建新请求、重试或放行后续执行。每次新执行仍通过当前 Context、Permission/Confirmation/Resource/Recoverability/Reality Boundary；不添加第二账本、不变更六份 Schema/外部接口或旧模型路径、不引入生产验签/Adapter/P09/P17。D-052 未创建、未使用，P08 始终 `IMPLEMENTED_NOT_ACCEPTED`，验收仍由用户决定。

**本次返修实证：** 新增定点 10/10、P08 46/46、旧 E5-A/P02 101/101、直接链路 317/317、综合 384/384、三轮全量 688/688 均通过。两项原反例分别变为 REJECTED/effect=0，以及合法归账 COMPLETED/query=2/execute=0/effect=credit=1；后续动作仍受当前全部门禁。两项证据闭合后 PLANNING_CONFLICT/EVIDENCE_CONFLICT 均 NONE；不将本次决定解释为用户已验收 P08。

**2026-09-04 第二轮无回执终态返修补充：** 用户要求继续 P08，原有限授权不变。原 46/688 之后两个监工反例再次证明仅在 receipt 非空时验真不足：无回执 FAILED_TERMINAL 可掩盖 Fake 成功；自洽 hash 的 EXPIRED 可冒充此前未执行。本轮先固化 2/2 FAIL（0.467 秒），当时 EVIDENCE_CONFLICT=PRESENT。内部结果只支持 PROPOSED/UNKNOWN/SUCCEEDED/FAILED_TERMINAL/EXPIRED；成功、执行失败必须有绑定原请求的 receipt 并由可信 Adapter query 核实；EXPIRED 是本地停止决定，只有明确类型化 NOT_EXECUTED 查询才能消费。UNKNOWN、查询异常或任意字符串不构成未执行证明；不支持的取消/重试状态拒绝，不建设新系统。

**统一消费与历史：** 领域构造/反序列化和同一 InternalActionAttempt 的保存重验等价结构约束；协调器追加、历史重载消费、终态重放与依赖放行统一验证可信事实。choice 到期或 Context 当前失效不能抹掉此前成功；原 UNKNOWN 的真实 receipt 仍可归账。已存停止决定与真实执行冲突则明确拒绝，不覆盖任一历史。原身份/主体/环境/snapshot/step/Adapter/policy、当前新执行门和 model.generate 恢复路径均不变。仅修改三个 P08/内部协调运行文件及既有恢复测试，不新增账本、存储格式或外部 Schema，不变更 Authority；D-052 未创建、不使用。最终证据见本轮施工日志与 P08 测试索引，仍需监工和用户验收。

**第二轮返修实证：** 本轮 12/58/101/329/396 均 PASS，三轮全量均 700/700 PASS（136.457、137.235、138.780 秒）。两条原反例分别在领域门拒绝假失败后恢复真实 COMPLETED，以及 query 发现停止决定与真实成功冲突而拒绝消费；effect/credit=1 均保留且无重复执行，合法成功/失败/零效果过期可重启精确重放。静态与冻结边界通过后两个 conflict 闭合为 NONE；这不是正式验收决定，P08 与十二项仍 IMPLEMENTED_NOT_ACCEPTED。

## D-052：用户正式验收 P08 及方案 A 内部有限泛化

**日期与授权来源：** 2026-09-04，用户明确“我正式验收 P08，包括方案 A 授权范围内的 E5-A 内部 durable pause/resume 最小泛化，以及两轮监工阻断返修”。本决定只登记用户验收及纯档案闭合，不由测试自动推导验收，也不增加新施工授权。

**独立验收依据（用户提供的监工复核）：** 四项历次反例 4/4 PASS、0.990 秒；P08 专项 58/58 PASS、22.534 秒；Engine 全量 700/700 PASS、141.583 秒。冻结 Schema、外部接口、pyproject/0.1.0 和正式数据未改变。本窗口本轮档案后的实际复跑与上述独立证据分别记录，不相互冒充。

**正式状态：** P00—P08 = ACCEPTED；P08 Engine side = ACCEPTED；P08-01—P08-12 = ACCEPTED（12/12）；P08 Vio dependency = NONE；P09—P23 = NOT_STARTED；PLANNING_CONFLICT=NONE、EVIDENCE_CONFLICT=NONE。D-051 原开工、用户选择 A 及两轮返修记录完整保留；D-052 本次创建并使用，取代现行未验收状态，不倒写历史。

**有限范围与 Authority：** 简单 Natural/Direct Action 无需 Goal/Plan；复杂行动由 Optional Planner 规划，model.generate 仍走原 Thinking/ThinkSession 路径。非模型 Action/Step 仅在同一 E5-A 内部通道中分派，身份、持久化、恢复和幂等不形成第二请求账本，不冒充 model.generate，不扩展六份冻结外部 Schema 或 HTTP/API/Capability/Binding 契约。SubjectState、Event、Memory、Timeline、Evolution Authority 不变，P08 结果无直接状态写权或 Evolution commit 权。

**两轮返修验收边界：** 历史执行成功/失败必须经原绑定可信 Adapter query 核实 receipt；EXPIRED 只是本地停止决定，须明确 NOT_EXECUTED，不能以 receipt=None、reason、时间或自洽 hash 掩盖真实执行。UNKNOWN/查询异常不等于未执行；停止历史与真实事实冲突拒绝消费而不覆盖。当前 Context/choice 失效不阻止原请求真实回执归账，但所有新请求、新执行、受控 retry 和后续步骤仍受现有 Context、Capability、Permission/Confirmation、Resource、Recoverability、Reality Boundary 约束。

**不扩大授权：** 验收仅覆盖已验证的 Engine 本地 TEST Fake/幂等回执能力，不代表任意生产 Adapter exactly-once，不授权生产 Adapter、生产验签、P09 开工、P17 Execution Engine、Scheduler 或真实系统接入。P08 本地隔离不是长期产品禁令；后续仍按 P09/P16/P22 的既定授权和阶段顺序开放，不因此提前施工。

**本轮纯档案与历史：** 不修改运行代码、测试、Schema、外部契约、正式数据、pyproject 或软件版本 0.1.0。完整保留开工 642、初版 36/678、首轮返修 46/688、第二轮 58/700、所有失败回归、辅助测试错误、当时的 IN_PROGRESS/IMPLEMENTED_NOT_ACCEPTED/EVIDENCE_CONFLICT=PRESENT 和既有终局结果。未执行 Git 暂存/提交/推送/合并/rebase/tag/release，Git 操作等待用户另行决定。
