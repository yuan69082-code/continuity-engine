# 本轮测试进程观察

2026-09-25。固定源码的完整回归 `full-final-01` 于 `03:43:03 UTC` 通过本目录 `run.py` 启动，执行会话 ID `14909`，进程标准输出和标准错误只写入该标签各自日志。启动记录中的源码指纹为 `sha256:188796ffbc704795291bba912f668f3d415c5173328de307ca9a7e2115d3afd1`。

执行期间只读查询 `Get-CimInstance Win32_Process` 返回“拒绝访问”，故不能据该辅助命令断定进程身份或退出状态；本次未清理、终止或重启该测试。原会话的持续返回和 stdout 增长是当时仍在运行的证据。终局只以完整 `full-final-01.json`、退出码、unittest 汇总和运行前后源码身份计数；如会话中断而没有完成文件，只保留未确认状态，不能写 PASS。

终局：原会话 14909 实际完成，`full-final-01.json` 记退出码 0，unittest 汇总 1736 项中 1 个既有 SKIP、0 FAIL/ERROR；后续 `Get-Process python` 未列出本轮 Python 进程。没有强制清理或留下本轮后台测试宿主。此前 CIM 拒绝访问仍作为辅助观察限制留档。
