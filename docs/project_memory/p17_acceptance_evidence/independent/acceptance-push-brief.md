# P17 用户正式验收与 Git 收尾授权

日期：2026-09-10。用户在独立复核通过后明确回复“验收并 push”。本指令授权 Engine 完成 P17 初版及 R1/R2 返修的正式验收归档、精确暂存、普通提交及推送；不是继续修改行为实现或 P18 开工授权。由用户转发，不由规划侧跨任务派单。

执行仓库：`C:/Users/Administrator/Documents/continuity-engine`。

独立复核材料目录：`C:/Users/Administrator/Documents/Codex/2026-08-24/https-github-com-yuan69082-code-continuity/reviews/p17-independent-review-20260909`。

## 1. 验收依据与当前现场

先完整读取该目录的 `repair-review-report.md`、`repair-identity-check.json`，以及 Engine 当前返修报告和精确待提交清单。归档所需独立报告、原始运行、探针及身份核对副本时，记录来源和 hash，保持规划侧原件不变。独立新增四项只是复核探针，不改写 Engine 正式测试总数。

已独立复核：P17 专项 74 PASS（包含新增正式 12 项），101.169 秒；原七项探针与额外四项共 11 PASS，9.246 秒；身份/保护/历史/范围检查 24/24 通过。施工方全量 1360 项：1359 PASS、1 既有 Windows symlink 1314 SKIP、0 FAIL/ERROR、1171.401 秒，当前源码身份与原始记录已核对；这是引用结果，不是监工重新跑全量。

独立复核基线：main，HEAD/本地 origin/main 为 `0c440b0476b07723abafe93777fe895b64fd8d0e`，本地 ahead/behind 0/0，暂存区为空；源码/测试/资源 253 个，当前 P17 成果 196 个路径，另有 32 个排除项。先重新核对当前现场、来源和远端目标。不要因旧报告写着“等待验收/不得 push”忽略本次明确授权，也不要退回 P16 收尾。

如果出现新的源码/测试漂移、不明改动、暂存冲突、远端分歧或新的阻断，保留现场并报告，不覆盖、不强推、不自行改行为代码。若本项已经完成，只核实并报告，不重复验收或制造重复提交。

## 2. 正式验收归档

确认当前代码仍为已独立复核版本后，登记 P17 用户正式验收决定 D-071；先检查编号尚未被其他决定使用，若冲突则停止说明，不覆盖原决定。

将 P17、Engine side、十二项矩阵及现行 P00—P17 状态同步为 ACCEPTED。关闭本次 R1/R2 独立复核已确认的阻断；只有当前没有其他未解决冲突时，现行 PLANNING_CONFLICT/EVIDENCE_CONFLICT 才标为 NONE。保留历史 PRESENT、首次 FAIL/ERROR、SKIP 和旧快照，不把历史改成全部成功。

同步用户验收记录、决策/状态/矩阵/测试入口、归档导航及精确文件清单。明确本轮验收是已实现的本地 TEST 范围：生产 Adapter、凭据、恢复与相关生产能力仍 NOT_READY；不把现实边界扩大成心理内容筛查。P18—P23 保持 NOT_STARTED。

## 3. 只做必要的收尾检查

不得修改已验证行为源码、原测试断言、冻结六份 Schema、外部契约、63 项保护文件、三份规划原件、正式七文件、pyproject/版本 0.1.0 或 32 个排除项。不改 Assistant。

核对当前 253 个源码/测试/资源文件与已验证身份，核对保护项、正式数据树、独立证据原件/副本、文档链接、敏感内容和 Git 差异。只新增验收文档及证据副本时引用已验证行为结果，不机械重跑全量；如发现必须修改行为代码的新问题，本次收尾暂停，等待用户确认。

新写可编辑文件的格式问题正常处理。原始失败日志中的行尾空格、旧 EOF 空行等历史格式告警原样保留并逐项列明，不能为消除告警覆盖证据，也不能统一忽略新增问题。若发现疑似真实凭据或敏感信息泄露，停止相关发布并说明，不上传或自动改写原证据。

## 4. 精确提交与普通 push

以原 196 个 P17 路径为已核对起点，加上本轮必要验收归档文件，生成最终逐文件提交清单与排除清单。最终数量按实际报告，不能固定为 196，也不能混入其他阶段或不明文件。

仅按最终清单显式暂存；禁止 `git add .`、`git add -A` 或将整个目录无差别暂存。32 个排除项（31 个 P10 脚本与 1 份旧 P14 接续报告）始终不加入 Git、原样保留。

提交前核对暂存路径与清单完全相等、每项来源合理、保护项未进入不应有的改动。完成一次普通提交，建议说明：

`feat: implement and accept P17 execution and world boundaries`

普通推送到已核实的现有 Engine `origin/main`。不改变远端地址/可见性，不创建新仓库，不 force push、amend、reset、rebase、merge、切换分支或创建 tag/release。若远端分歧则停止报告；若仅遇临时传输错误，可核查远端状态后对同一提交作有限重试，不能反复制造新提交。

推送后实际查询远端 main，核对本地 HEAD、本地 origin/main、远端 SHA 一致，本地 ahead/behind 0/0；暂存区及已跟踪工作区干净，剩余未跟踪仅既有排除项（如出现其他文件须逐项说明）。最终 SHA 和投递结果在交付消息或仓库外证据记录，不为把提交自身 SHA 写进同一提交而重复 amend/commit。

## 5. 如实交付并停止

交付 D-071/验收入口、最终 SHA、直接父提交、提交说明、实际文件数和精确清单、排除项、三方 SHA 核对、完整 Git 状态、引用/新运行的区别以及历史格式告警。

不为这次收尾新建 CI workflow 或绕过权限。没有 workflow、没有运行或查询被拒绝时，如实写“未取得可确认的远端 CI 结果”；普通 push 成功不等于 CI PASS。

完成 P17 验收与普通 push 后停止，等待用户另行确认 P18。不要启动常驻服务、后台轮询、生产外部操作或下一阶段施工。
