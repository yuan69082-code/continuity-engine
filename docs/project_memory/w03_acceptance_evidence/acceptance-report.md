# W03 长期认识与未完事项正式验收（D-083）

2026-09-26，用户正式验收 W03 的 N03、N04、N06（包括 N06 三处定点返修），并授权本轮精确提交及向现有 Engine `origin/main` 普通推送。D-082 仍是开工决定；W03 = `ACCEPTED`。W02/D-081、P00—P18 历史验收不变；W04/W05/P19 及后续阶段没有因本决定开工。此报告不预填 Git 推送或远端 CI 结果。

## 依据、已交付范围与边界

现行依据是[规划版本索引](../现行规划版本索引.md)中的总施工 v1.5、最终新增 v1.5、长期能力 v6.9，以及 W03/N03/N04/N06、T04/T07/T08/T09/T12 和适用 T18。规划窗口独立**只读**核对源码、原始输出、文件身份及保护范围，没有独立运行 Engine 测试。用户以该复核和施工方同版实跑为依据验收；这不保证不存在其他缺陷。

- N03：旧 Event/Memory 可在原 Learning/Evolution 链形成有独立根、反证、更正和撤销理由的对象认识；同名不同归属分开，历史依据可追溯。TEST 注解不证明真实自由语言的通用理解质量。
- N04：启用必要核心门禁时，正常 C1 回应在 Thinking 前经原 Router/Composer 获得当前身份、关系及版本化保护片段；缺失、过期或预算不足明确拒绝。模型权重没有被修改，完整常驻联动留待后续。
- N06：原 SubjectState/Action Gate/Evolution 管未完事项；来源、等待条件、完成事实和公平就绪排序可只读核对。三处返修使终态不占当前 64 项、旧字符串仅按显式版本绑定迁移、当前 Memory/Timeline/W02-C 材料及权限变化阻断继续就绪。没有第二队列或请求账本，生产 Adapter 的 exactly-once 不在验收范围。

[逐项验收矩阵](acceptance-matrix.md)保持 `Planning Item → Code Change → Test → Acceptance Result` 对应。[W03 初版交付](../w03_evidence/final-report.md)、[N06 返修报告](../w03_n06_repair_evidence/report.md)、[原始测试索引](../w03_n06_repair_evidence/test-index.md)及同标签 stdout/stderr/JSON 保留发生时的状态，不倒改旧结论。

## 同一源码版本的测试依据

304 份源码、正式测试及资源的 SHA-256 集合指纹为 `sha256:d6a98e0ea091984c235cc16cd03c249f05b2c56e00ff2a423b9f95f774a23ecf`。施工方实跑：W03 专项 32/32 PASS（155.062 秒）；受影响兼容 176/176 PASS（350.243 秒）；完整回归 1768 项为 1767 PASS、1 个既有 Windows 1314 SKIP、0 FAIL/ERROR（1883.314 秒）。三组前后源码指纹均相同、集合有交叠，不相加。本次纯验收归档未重新运行上述测试，也无远端 CI PASS 证据。

初版 W03 全量的 W02 `RECALL_TIMEOUT` ERROR、后来中途停止的运行、正式修前反例及辅助夹具错误均保留。旧 HEAD 和当时 W03 工作版在同条件下各一次触发原 1000 毫秒回忆时限（约 1063/1049 毫秒）；单次对照不能唯一归因或证明风险消失。早期四份外部资料同时进入的负载没有在最终版重跑，**不宣布四份负载问题已解决**。同名旧事项无可靠身份时不凭标题自动合并，需明确版本绑定。历史 F1/H1/F2 根因仍为 `UNKNOWN`；既有 SKIP 不是 PASS。

用户确认关闭本次已复核的 W03/N06 已知验收阻断；这不关闭 W02 时限风险、不改写任何历史失败或 F1/H1/F2。生产资料服务、真实设备/凭据、P19 页面和完整常驻联动仍按既定后续阶段 `NOT_READY`。W03 的验收不开放正式主体数据或生产能力。

## 本次归档与提交边界

只新增或同步验收文档，运行源码及正式测试仍与上述指纹一致。[最终审计](final.audit.json)与[精确逐路径清单](final.pending-files.md)记录本次验收增量、每份成果 hash、63 项保护、三份现行规划、正式七文件与原 57 项排除材料。排除材料不暂存、不清理。提交、普通推送和实际远端结果须在执行后另行核对，不以本地跟踪分支代替实际远端。

首次验收清单辅助脚本在两份自引用输出尚未生成时因 `KeyError` 退出；[原 stdout](audit-helper-before-01.stdout.log)与[原 stderr](audit-helper-before-01.stderr.log)已保留。修正的是清单工具对未生成自引用路径的处理，未触及运行源码、正式测试或测试结果；随后终局审计通过。
