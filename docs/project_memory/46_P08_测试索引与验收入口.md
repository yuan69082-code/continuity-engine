# P08 测试索引与验收入口

当前 P08 / Engine side / 十二项均 `ACCEPTED`，用户于 2026-09-04 正式验收（D-052）。所有测试均本地 TEST Fixture；不扩大方案 A，不代表生产 exactly-once，不授权 P09/P17/生产 Adapter 或 Git 操作。以下初版、两轮返修及其当时状态保留为历史；现行验收依据见第 10 节。

## 1. 专项入口

| 文件 | 项数 | 覆盖 |
|---|---:|---|
| tests/test_p08_action_planning.py | 23 | Direct/Planner、两类 trigger、输入封印、统一执行门、预算、篡改、Golden/底层零写入 |
| tests/test_p08_action_recovery.py | 35 | 原 13 项 + 首轮返修 10 项 + 无回执终态返修 12 项：执行事实/停止决定、统一验真、stale Context 归账与新执行分离 |

```powershell
$env:PYTHONPATH = "src"
$env:PYTHONDONTWRITEBYTECODE = "1"
python -m unittest tests.test_p08_action_planning tests.test_p08_action_recovery -q
python -m unittest tests.test_action tests.test_integration_persistence tests.test_perception tests.test_memory_service tests.test_event_persistence tests.test_evolution tests.test_p03_event_semantics tests.test_p03_timeline tests.test_p04_memory_consolidation tests.test_p04_memory_repository tests.test_thinking tests.test_awakening tests.test_p05_context_router tests.test_p05_context_routing tests.test_p06_context_composition tests.test_p06_context_composer tests.test_p07_contradiction tests.test_p07_contradiction_detector tests.test_p08_action_planning tests.test_p08_action_recovery -q
python -m unittest tests.test_p01_sandbox tests.test_e5_capability_flow tests.test_e5_capability_http tests.test_e5_capability_recovery tests.test_p02_model_capability tests.test_p02_model_recovery tests.test_p03_event_semantics tests.test_p03_timeline tests.test_p04_memory_consolidation tests.test_p04_memory_repository tests.test_p05_context_router tests.test_p05_context_routing tests.test_p06_context_composition tests.test_p06_context_composer tests.test_p07_contradiction tests.test_p07_contradiction_detector tests.test_p08_action_planning tests.test_p08_action_recovery -q
python -m unittest discover -s tests -q
```

## 2. 初版收口历史验证（返修前）

- 专项 36/36 PASS，11.590 秒。
- 直接相关链路 307/307 PASS，31.112 秒。
- P01—P08 综合矩阵 374/374 PASS，74.506 秒。
- 开工 642/642、103.184 秒属于修改前历史；P07 原验收及两轮返修证据保留。

代码收口后连续三轮 Engine 全量（同一命令顺序执行，不并发）：

| 轮次 | 实际结果 | unittest 耗时 |
|---|---|---:|
| 1 | 678/678 PASS | 116.450 秒 |
| 2 | 678/678 PASS | 116.219 秒 |
| 3 | 678/678 PASS | 117.417 秒 |

163 个 Python AST 全通过；51 份 Markdown、171 个本地链接、尾随空白/围栏检查零失败；敏感值、P08 禁止依赖、后续阶段代码混入零命中。正式数据指纹和 7 文件、六份 Schema、pyproject SHA/0.1.0 不变。P08 新缓存和 Sandbox 残留为零，已有其他阶段缓存未删除。档案后的专项与全量终局只读复跑将单独在最终报告给出，不把此前三轮当作新结果。

## 3. Golden 与恢复证明

`p08-direct-planner-golden-v1` 复用 P06 COMPLETE Context，以可信 TEST Thinking producer 形成 INFORMATION_NEED：lookup → contact。两个稳定步骤和两次唯一 execute，仅 contact 产生 1 synthetic effect/credit；重复提交仍是相同 result hash。Direct 六种行动在 Planner 关闭时成立，plan=None。源 SubjectState/Event/Memory/Timeline 树 hash 与 P06 result hash 前后相同。

crash matrix 包括 requests 保存后、Adapter 前、Fake effect/receipt 后但 Engine result 前、result 保存后。各路径重启使用原 operation/request/step，成功 attempt 每请求只有一个。UNKNOWN 保留历史并先 query；NOT_EXECUTED 必须受控 retry；跨步骤回执、篡改结果、确认错绑与配置漂移拒绝。测试读取落盘 receipt 与 capability ledger 的计数，不仅依赖内存 calls。

## 4. 真实失败与最小返修

初版 32/32 PASS（10.724 秒）。追加 Adapter 漂移反例后 1/1 FAIL（0.220 秒，未抛 CapabilityConflictError）；绑定 Adapter ID/能力配置 canonical hash 后 33/33 PASS（10.959 秒）。追加步骤间 Context 失效反例后 1/1 FAIL（0.269 秒，未抛 CapabilityValidationError）；每次新执行前重验后 36/36 PASS。两条失败历史不倒写、不删除断言。

## 5. 安全停止点

不修改六份 Schema、HTTP/API/Binding、正式 .continuity-data 或 pyproject/0.1.0；不接 Vio、外部网络、Provider、凭据、MCP、外部数据库或生产 Adapter。P09—P23 不开始，不执行 Git 写操作。最后验收由规划监工及用户决定，不由测试自动升级为 ACCEPTED。

## 6. 2026-09-04 首轮两项监工阻断返修：历史定点与兼容入口

前述 36/307/374/678 为本次监工反例之前的历史结果。监工两项反例先固化为 2/2 FAIL（0.487 秒）；最小修复后 2/2 PASS（0.460 秒）。新增 10 项顶层回归的完整入口如下；不以更改旧预期隐藏失败。

```powershell
$env:PYTHONPATH = "src"
$env:PYTHONDONTWRITEBYTECODE = "1"
$targets = @(
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_forged_persisted_success_cannot_release_dependent_action',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_stale_context_recovers_already_executed_receipt_without_new_authorization',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_historical_receipt_requires_independent_query_on_load_replay_and_append',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_rehashed_persisted_receipt_field_drift_is_not_execution_proof',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_stale_context_terminal_replay_preserves_exact_history_without_clock_or_new_gates',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_stale_context_archives_first_effect_but_blocks_unexecuted_plan_step',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_stale_context_unknown_remains_query_only_even_with_retry',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_stale_context_not_executed_retry_and_new_request_are_blocked',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_stale_recovery_keeps_choice_snapshot_producer_adapter_and_policy_binding',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_receipt_recovery_does_not_authorize_next_step_through_other_gates'
)
python -m unittest $targets -q
python -m unittest tests.test_e5_capability_flow tests.test_e5_capability_http tests.test_e5_capability_recovery tests.test_p02_model_capability tests.test_p02_model_recovery -q
```

| 本轮实际回归 | 项数 | 耗时 | 结果 |
|---|---:|---:|---|
| 新增定点矩阵 | 10 | 3.140 秒 | PASS |
| P08 专项（23 planning + 23 recovery） | 46 | 13.115 秒 | PASS |
| 旧 E5-A/P02 兼容 | 101 | 20.053 秒 | PASS |
| 直接相关链路（第 1 节原命令） | 317 | 39.625 秒 | PASS |
| P01—P08 综合（第 1 节原命令） | 384 | 86.714 秒 | PASS |

伪造成功的修复前后：`COMPLETED / effect=1` → `REJECTED / effect=0 / execute=0`，原始伪造 ledger 不被修写。Context 失效恢复：`CONTEXT_STALE_OR_UNAUTHORIZED / query=0 / effect=1` → `COMPLETED / query=2 / execute=0 / effect=credit=1`。新增/重算 hash 的回执字段漂移、缺失、UNKNOWN、查询异常都不能作为终态或依赖依据。合法终态即使当前门禁失效也可经可信 query 原样重放，不读新时钟，ledger bytes 不变。

新执行与事实恢复分离：原 request/choice/snapshot/step/Adapter/policy 和 trusted producer 校验仍强制；Context 失效时只有原请求可查询/归账。后续步骤、NOT_EXECUTED retry 和新请求继续受当前 Context 门限制；Permission、Confirmation、Resource、Recoverability、Reality Boundary 的五类拒绝均只归档第一步既有 receipt，第二步 execute=0。UNKNOWN 重复恢复/显式 retry 仍无 execute、无新 effect/credit。

## 7. 首轮返修后三轮全量与静态闭合（历史）

运行 `python -m unittest discover -s tests -q`，同一工作区顺序连续执行：

| 轮次 | 实际结果 | unittest 耗时 |
|---|---|---:|
| 1 | 688/688 PASS | 130.617 秒 |
| 2 | 688/688 PASS | 131.293 秒 |
| 3 | 688/688 PASS | 134.722 秒 |

正式数据测试前后均为 `sha256:f9c710c6085e430a251f74463d57133f8c5a4497fe8ffa2ccb96be2db1037bb2`、7 文件；逐物理文件 SHA-256 亦完全一致。六份 Schema 与 pyproject hash/0.1.0 不变，外部 interfaces diff=0。163 个 Python AST、51 份 Markdown、174 个本地链接通过；尾随空白、敏感值、P08 禁止依赖、P09/P17 运行混入、新缓存/Sandbox 残留零命中。148 个旧阶段缓存的最新时间仍为 2026-09-03 22:42:27，保留未清理，不伪称工作区从无缓存。`git diff --check` exit=0，仅 44 条 LF→CRLF 配置提示，暂存区为空。

本次两项证据闭合后 `PLANNING_CONFLICT = NONE`、`EVIDENCE_CONFLICT = NONE`。P08 / Engine side / 12 子项仍为 `IMPLEMENTED_NOT_ACCEPTED`，D-052 未创建。全部档案完成后的 P08 专项及全量终局复跑仅登记在最终报告，不把上述三轮重复计为新证据；不再修改代码或测试，不执行 Git 写操作。

## 8. 第二轮无回执终态返修：现行定点矩阵

前述 36/678、46/688 与各自失败记录保持为历史。本轮新增 12 项顶层测试；原 2 条监工反例先实测 2/2 FAIL、0.467 秒，修复后 2/2 PASS、0.495 秒。无回执 FAILED_TERMINAL 在领域构造拒绝，Engine UNKNOWN 保持，重启查询真实成功后 COMPLETED/effect=credit=1；重算 hash 的 EXPIRED 在重载消费 query=1 后被拒绝，原 ledger 与 Fake receipt bytes 均不变。追加补测时曾因断言落错测试方法出现 NameError（12 项、4.194 秒、1 ERROR），纠正位置后保留断言；没有运行代码回归被隐藏。

```powershell
$env:PYTHONPATH = "src"
$env:PYTHONDONTWRITEBYTECODE = "1"
$targets = @(
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_receiptless_terminal_failure_cannot_mask_lost_success_on_append',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_rehashed_expired_terminal_cannot_hide_independent_success',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_internal_result_status_evidence_matrix_and_roundtrip',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_invalid_terminal_shapes_rehashed_reload_fails_closed',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_legal_success_and_failure_receipts_restart_and_duplicate_exactly',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_legal_expiry_requires_not_executed_and_replays_without_clock',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_expired_choice_and_stale_context_still_recover_actual_success',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_expired_unknown_invalid_query_and_query_exception_do_not_prove_nonexecution',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_expiry_append_checks_typed_nonexecution_not_caller_assertion',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_persisted_expiry_cannot_replay_when_nonexecution_becomes_unverifiable_or_conflicted',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_bypassed_domain_terminal_is_rechecked_at_coordinator_and_storage_append',
    'tests.test_p08_action_recovery.P08ActionRecoveryTests.test_expired_dependency_with_real_lookup_success_is_rejected_before_contact'
)
python -m unittest $targets -q
```

| 当前实际回归 | 项数 | unittest 耗时 | 结果 |
|---|---:|---:|---|
| 无回执终态定点矩阵 | 12 | 4.114 秒 | PASS |
| P08（23 planning + 35 recovery） | 58 | 22.424 秒 | PASS |
| 旧 E5-A/P02 兼容（第 6 节命令） | 101 | 23.558 秒 | PASS |
| 直接相关链路（第 1 节命令） | 329 | 43.852 秒 | PASS |
| P01—P08 综合（第 1 节命令） | 396 | 89.791 秒 | PASS |

**证据覆盖：**

- `internal_result_status_evidence_matrix_and_roundtrip`、`invalid_terminal_shapes_rehashed_reload_fails_closed`、`bypassed_domain_terminal_is_rechecked_at_coordinator_and_storage_append` 覆盖构造、反序列化、重算 hash 重载、绕过 frozen 对象后的协调器/仓储追加；取消、FAILED_RETRYABLE、缺失 receipt、错 status/reason/time/gates 和空 receipt 对象均拒绝。
- `legal_success_and_failure_receipts_restart_and_duplicate_exactly` 验证成功与失败均有唯一可信 receipt、跨重启/重复结果保持原 hash 和 ledger bytes，不增加 execute/effect/credit。
- `legal_expiry_requires_not_executed_and_replays_without_clock` 验证合法零效果过期、重复终态不读 clock；两次恢复/重复追加累计 6 次明确 NOT_EXECUTED 查询，execute/effect/credit 均为 0。
- `expired_choice_and_stale_context_still_recover_actual_success` 验证 choice/Context 都失效且所有新执行门拒绝时，原 UNKNOWN 仍恢复真实成功，不再 execute/收费。
- `expired_unknown_invalid_query_and_query_exception_do_not_prove_nonexecution`、`expiry_append_checks_typed_nonexecution_not_caller_assertion`、`persisted_expiry_cannot_replay_when_nonexecution_becomes_unverifiable_or_conflicted` 覆盖 UNKNOWN/None/普通同名字符串/查询异常、已有真实回执及重启后事实冲突；不得追加未经证实的过期或覆盖原记录。
- `expired_dependency_with_real_lookup_success_is_rejected_before_contact` 验证伪造过期的 lookup 历史拒绝消费，contact 无 execute/effect；与上一轮伪造成功、stale Context 后续步骤门禁一起保留。

完整状态/证据规则见 [恢复语义](45_P08_DirectPlanner执行恢复语义.md)。本轮无新增账本、存储字段或版本；真实执行依据仍为原宿主中立 Adapter query，只证明已验证的本地 Fake 能力。

## 9. 第二轮返修三轮全量与终局入口（验收前历史）

同一工作区、同一环境，顺序执行 `python -m unittest discover -s tests -q`，任一失败即退出循环；本轮实际结果：

| 轮次 | 结果 | unittest 耗时 |
|---|---|---:|
| 1 | 700/700 PASS | 136.457 秒 |
| 2 | 700/700 PASS | 137.235 秒 |
| 3 | 700/700 PASS | 138.780 秒 |

163 个 Python AST、51 份 Markdown、175 个本地链接通过；尾随空白、秘密值、P08 禁止依赖、后续阶段运行文件混入、新缓存/Sandbox 残留零命中。对全部 10 个已变更运行文件另做 AST import/凭据读取扫描亦零命中。保留 148 个旧阶段缓存，最新时间 2026-09-03 22:42:27；不清理用户现场。

正式数据 tree hash 测试前后 `sha256:f9c710c6085e430a251f74463d57133f8c5a4497fe8ffa2ccb96be2db1037bb2`、7 文件逐项 SHA-256 一致；六份 Schema 与 pyproject SHA/0.1.0 不变，外部 interfaces diff=0。`git diff --check` exit=0，44 条 LF→CRLF 仅为配置提示，暂存区为空。PLANNING_CONFLICT=NONE、EVIDENCE_CONFLICT=NONE；P08 未用户验收、D-052 未创建。

全部档案写完后再次实际执行第 1 节 P08 专项命令及 `python -m unittest discover -s tests -q`，之后不再修改仓库文件。该终局结果仅在最终回复登记，不把本节三轮或监工的旧 688 结果冒充新证据。停止等待规划监工独立复核，不提交、不进入 P09/P17。

## 10. 用户正式验收与接续收尾

用户于 2026-09-04 正式验收 P08，既有 D-052 覆盖 Natural/Direct Action、Optional Planner、方案 A 的 E5-A 内部有限泛化及两轮阻断返修。P00—P08、P08 Engine side 与 P08-01—P08-12 全部 `ACCEPTED`；P08 Vio dependency = `NONE`；P09—P23 = `NOT_STARTED`；PLANNING_CONFLICT/EVIDENCE_CONFLICT 均为 `NONE`。D-052 未重复创建，不新增决定编号。

以下是用户交接的已完成历史证据，不是接续任务本次新跑结果：

| 证据来源 | 实际结果 | unittest 耗时 |
|---|---|---:|
| 规划监工最后一轮独立四项反例 | 4/4 PASS | 0.990 秒 |
| 同轮监工独立 P08 专项 | 58/58 PASS | 22.534 秒 |
| 同轮监工独立 Engine 全量 | 700/700 PASS | 141.583 秒 |
| 引擎第二轮返修档案后终局 P08 | 58/58 PASS | 22.713 秒 |
| 同次引擎终局 Engine 全量 | 700/700 PASS | 136.560 秒 |

监工独立静态复核为 163 个 Python 文件、51 份 Markdown、175 个本地链接通过，227 个纳入核对的仓库文件前后内容一致；冻结文件与正式数据不变。第 2、4、6—9 节的开工 642、初版 36/678、首轮 46/688、第二轮 58/700，以及所有失败、辅助测试错误和当时阶段状态完整保留。

接续任务只补验收档案。旧任务的 remote compact task 断流是 Codex 上下文压缩/响应传输中断，不是项目测试失败；正式验收决定已登记与档案收尾完成是两件事。第 44 号矩阵十二项已同步，未完成/待确认清单、施工日志、修订记录、CHANGELOG 与工程总档案已补齐现行入口。

本次接续复跑在全部档案修改和静态检查完成后，按第 1 节环境设置，依次实际执行 P08 专项与 Engine 全量命令，不并发、不修代码或测试；若有失败则保留首次输出并停止。测试只使用现有隔离 Fixture，旧 HTTP 测试仅本机 loopback。真实结果、项数、耗时和最终冻结/正式数据/Git 复核在本次最终报告单独登记，不把上述历史结果当作本次通过，也不在测试结束后继续修改仓库档案。
