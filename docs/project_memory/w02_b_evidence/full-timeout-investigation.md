# 首轮全量 T04 超时及局部修补

首轮 [full-final-01](full-final-01.json) 完整结束：1693项，1691 PASS、1既有Win1314 SKIP、0 FAIL、1 ERROR；退出1，2182.850秒。出错为T04候选偏好测试，原因链 `RECALL_TIMEOUT`；运行前后均冻结版本01，未发生源码漂移。原始JSON/stdout/stderr及清理前TEST文件hash原样保留，不能把前面47/66/509通过说成这轮全量通过。

## 事实与定位

同一原始消息流程的单次定向诊断 [full-timeout-profile-01](full-timeout-profile-01.json) 保存分阶段耗时和读取次数。第三轮回忆prepare约0.966秒，整轮53次 `load_operation`；一次调用会重新解析并校验整个原operation文档。其间历史resolver先读取一次、随后父实现又做原始及返回前读取；候选阶段已经从原仓储得到合法记录，却仍逐条再次完整加载同一日志。没有证据将此次归因于旧F1/H1/F2或操作系统故障。

这次诊断本身通过，但不能关闭全量错误；它具体定位了冗余读取和接近1000毫秒边界的开销，而不是“反复重跑到绿”。原全量没有更细的每次读取计时，不能伪造当时分项耗时。

## 修改与拒绝边界

只再调整本批已有 `input_context_source.py` 与 `associative_recall_service.py`：历史来源复用本次原始material进行可用性检查，仍独立重读授权后的材料，并在返回前再次核对根状态。候选授权使用同一批原仓储完整核验记录；**所有授权回调之后，再从原仓储完整读取一次并比较原记录**，任一参与记录变化立即拒绝。没有跨请求缓存，没有跳过原解析/完整性校验，没有另一套事实源。

公共当前输入仍有原授权前/后两次读取；只改变内部helper组织，不改旧权限、生命周期、计费、重试、PAUSE/STOP或时间预算。新增两项正式测试验证：授权过程中根撤销不能返回；候选授权期间原日志记录变化不能进入模型或产生新效果/扣费。原有T04及其他测试断言不改。

修后定向诊断第三轮约0.730秒，整轮40次load；这只是当次测量，非生产性能保证。原1000ms上限原样保留。`timeout-correction-01` 的辅助测试构造错误、`consistency-diagnostic-01` 确认类型信息均保留；将字符串时间戳按原契约解析后，[timeout-correction-02](timeout-correction-02.json) 3/3 PASS，16.204秒。

## 最终覆盖要求

版本02新增两测试，原1646身份/字节不变，合计发现1695项；289份源码/测试/资源指纹 `sha256:e87ceea10e6c00ff6c5d3e94c6e13f764c82228681f2029dcc648d3cb512a33a`，见[frozen-source-02](frozen-source-02.json)。必须用02的专项/A兼容/公共兼容/全量实际结果完成交付；01通过组只作历史。若新全量再失败，保存现场后定位，不能自动重跑或放宽原时限。

本项仍待独立复核，EVIDENCE_CONFLICT不自行关闭。历史F1/H1/F2 UNKNOWN不变。

## 第二轮完整全量：补修仍不足

full-final-02完整结束：1695项，1689 PASS、1 SKIP、1 FAIL、4 ERROR；2569.981秒，退出1。运行前后源码一致，原始记录不改写。

四个ERROR是T03、T04、负面反例和新增根场景的RECALL_TIMEOUT；一项FAIL是读取中变更测试在到达故障注入点前就超时，不能伪报为通过。它们不是历史F1/H1/F2。

full-timeout-details-01为一次预定cProfile定位，未放宽时限；测量本身有额外成本，第二次准备在约1.465秒超时。11次完整operation文档加载/解析约1.006秒，主要成本在嵌套Context绑定hash与反序列化；这些是本次测量，不能伪造为全量当时的逐调用计时。

在已列明的json_integration_repository.py与本批Recall服务中增加仅本次准备启用的解析复用：每次仍读取原权威文件，只复用字节完全相同且已完整校验的文档；返回独立深拷贝。字节变化、删除、损坏仍走当前真实读取和原校验。权限与根来源检查不缓存，不建立索引/人物库/请求账本，不改变原格式；旧调用未进入scope时保持原加载行为。前版小优化结果保留，不宣称已经解决该轮失败。

## 专项03与输入投影补修

w02-b-final-03：55项=54PASS/1ERROR，157.611秒，新增根/原候选场景仍超时。A03、兼容03、全量03未启动。原断言和1000ms保留。单次dependency-profile-01同流程诊断通过（第三轮约0.549秒），不能证明专项失败消失。发现input绑定明确排除continuity_context却先完整to_dict再丢弃；按原绑定语义先投影，不改变保存在原仓储的完整Context。input-projection-correction-01共13PASS、约0.902秒测量，不能写成更快。

最终调整让仅在原字节校验scope中的输入读取返回独立轻量投影，而不是复制所有旧Context后再丢弃；整份原文档仍先完整验证，字节变化重新验证，原权限/根复核继续。scope外读法保持，返回副本隔离。新正式对照证明另外一个原请求损坏也会被发现，修改返回副本不改变原文件。input-projection-correction-02共15PASS（14正式、1诊断），第三轮约0.585秒。均为一次预定实验的观察，不承诺生产性能；最终04仍须专项/兼容/完整回归，不以这些诊断代替。

## 最终版本04实际覆盖

最终57项正式专项PASS、66项A兼容PASS、509项受影响兼容PASS；全量1703项=1702PASS/1既有Windows1314SKIP/0FAIL/ERROR，3191.570秒、退出0。四组前后源码均与frozen-source-04一致；集合交叠不相加。源码指纹`sha256:c3e9649dbf8590a202e5fc82de79bdc6f641fbb41bd4355efd912eaa62c62aed`。这些是施工方实跑，不是独立复核或用户验收；保留全部前版失败、诊断局限，等待独立复核。原始命令与输出见对应04标签及最终索引。
