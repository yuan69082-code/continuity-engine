<!-- P13_ACCEPTED_START -->
> P13 用户正式验收（2026-09-08，D-063）：P00—P13 ACCEPTED；P13 / Engine side / P13-01—P13-12 ACCEPTED；P13 Vio dependency=NONE；P14—P23 NOT_STARTED。现行 PLANNING_CONFLICT=NONE、EVIDENCE_CONFLICT=NONE，仅表示本轮已发现的 R1/R2 阻断经独立复核闭合并由用户确认验收。
>
> 监工独立实跑：原七项 7 PASS（3.832 秒）、P13 三模块 59 PASS（55.614 秒）、新增相邻检查 5 PASS（3.070 秒），均无 SKIP/FAIL/ERROR；59项含七项正式化版本，不相加为互斥覆盖数。全量仅核验引用施工1070项：1069 PASS、1既有 Windows symlink 1314 SKIP、0 FAIL/ERROR；stderr 621.987秒、结构化621.988秒是同一轮。本次纯档案归档未重跑测试。
>
> [验收依据、未提交版本身份及审计入口](P13_用户正式验收_20260908.md)。当前 HEAD 仍为 P12 checkpoint，P13 已验收源码由212文件 hash 标识、尚未提交；验收不等于提交或生产能力开放。下方历次施工/送审状态、PRESENT、FAIL/ERROR/SKIP、辅助错误、审批拒绝和 P09 segment 10 UNKNOWN 均为原样保留的历史。Git 操作及 P14 均须用户另行授权。
<!-- P13_ACCEPTED_END -->

# P13 测试索引与独立复核入口

当前为施工验证记录，P13 未经用户验收。范围见 [Stage Brief](63_P13_ExpressionPolicy架构边界.md)，实现/恢复边界见 [65](65_P13_表达绑定持久化与恢复语义.md)，逐项见 [矩阵](64_P13_规划施工测试验收矩阵.md)。D-061 已登记 P12 验收；D-062 仅是 P13 开工。

## 可复现入口

在 Engine 仓库执行，所有 Fixture 自动建立独立 Temp 数据根：

```powershell
$env:PYTHONPATH = 'src'
$env:PYTHONUTF8 = '1'
$env:PYTHONDONTWRITEBYTECODE = '1'
python -m unittest tests.test_p13_expression_policy tests.test_p13_expression_recovery -v
python -m continuity_engine.testing.p13_expression_fixture
python -m unittest discover -s tests -q
```

当前宿主实际 Python 为 `E:\Adobe\python.exe`。如需逐项身份和 stdout/stderr 长期保存，用 `p13_evidence/run_tests.py` 外层配合 `p13_evidence/run_suite.py`，阶段参数 `p13`、`compat` 或 `full`，每次必须提供全新证据标签；标签重复会拒绝，不覆盖首次结果。正式数据、仓库根、保护重叠路径不是可用 Fixture 根。

```powershell
python docs/project_memory/p13_evidence/run_tests.py review-p13-unique docs/project_memory/p13_evidence/run_suite.py p13 review-p13-unique
```

`archive_p12.py`、`implement_wiring.py` 是本轮已执行的一次性编辑过程记录，不是回归入口，不要在复核时再次执行。`capture_baseline.py` / `read_plans.py` 的已有输出保持原始证据。

## 测试归属

开工 [身份/hash](p13_evidence/before.json) 核实已验收 P12 的 205 个源码/测试与 1011 个测试身份一致，无导入错误。历史全量 1011 项：1010 PASS、1 个既有 Windows symlink 1314 SKIP、0 FAIL/ERROR（unittest 521.632 秒 / 结构化 521.633 秒）。这是核验引用，不是本轮全量执行。历史监工 9/9、7/7、117/117 PASS 的来源在 [P12 验收入口](P12_用户正式验收_20260908.md)。

本轮结果以 `p13_evidence/*.tests.json`、相应 `.stdout.log` / `.stderr.log` 和命令记录为准；终局表在验证完成后追加。Engine 没有 Actions workflow，不宣称远程 CI。

## 首次失败及辅助错误

- [初次红测](p13_evidence/initial-red.stderr.log)：现有 Core 不接受 expression_policy；七模式测试辅助误用了不存在的 ThinkingDepth.SHALLOW。Ran 2、8 ERROR，其中七个子用例是辅助字段错误，不是 Engine 缺陷。
- [纠正辅助字段后的红测](p13_evidence/initial-red-corrected.stderr.log)：Ran 2、8 ERROR；缺失配置入口及七种 expression_mode 输入为新增行为未实现的真实结果。完成内部可选字段后 [2 项通过](p13_evidence/input-green.stderr.log)。
- [第一次扩展测试导入错误](p13_evidence/policy-first.stderr.log)、[第二次错误导入路径](p13_evidence/policy-import-fixed.stderr.log)：SandboxOperationError 的实际位置是 testing.models；这两次各1 loader ERROR，不是模块运行通过。
- [修正导入后的辅助 state 访问错误](p13_evidence/policy-models-import-fixed.stderr.log)：使用了不存在的 SandboxRuntime.state；新增 Fixture state 只读属性后 [12 项通过](p13_evidence/policy-state-fixture-fixed.stderr.log)。
- [恢复首轮](p13_evidence/recovery-first.stderr.log)：14 项中1 ERROR。测试误用 FACT 携带状态 mutations，被原 Event 边界正确拒绝；改成已有显式 TEST STATE_CHANGE seed。Expression 本身不写 SubjectState。
- [恢复扩展](p13_evidence/recovery-extended.stderr.log)：19 项中2 ERROR，分别为 TEST 构造的拒绝 Action 缺少理由、capability_result_payload 导入位置错误。改用正常 Action Gate 的撤权输入及原 tests.test_e5_capability_flow 辅助函数，保留原输出。
- [Action 绑定反例辅助错误](p13_evidence/binding-red.stderr.log)：ActionExecutionResult.decision 是只读投影，不能作为 dataclass 构造字段；更正为改变合成 session identity。
- [真正的 Action 绑定遗漏](p13_evidence/binding-red-corrected.stderr.log)：1 FAIL，初版表达服务接受了另一 ActionSession。修复为逐项比较 operation 保存的原 Action 与传入 Action，拒绝且零持久变化/Port 调用。
- [真正的 Router request 绑定遗漏](p13_evidence/request-binding-red.stderr.log)：1 FAIL，初版服务可以把原 Router Context 绑定到另一个请求。修复为核对原 request、ThinkSession/result ID、subject、revision 及 Action 原件，拒绝且零写入/调用。
- [37 项稳定专项](p13_evidence/p13-stable.stderr.log)：37 PASS、0 SKIP/FAIL/ERROR，32.387 秒。这关闭上述 P13 内部实现缺口，不代表用户验收。

全部旧 P00—P12 失败/ERROR/SKIP、自动审批拒绝及 P09 segment 10 stderr 缺失、根因 UNKNOWN 均保留；后续 PASS 不覆盖原记录。

## 后续首次失败与局部收口

- [沉默预算反例](p13_evidence/silence-budget-red.stderr.log)：1 ERROR；未输出草稿长度被错误用于限制 SILENCE。只对 SUBJECT_EXPRESSION 应用可见正文预算，空表达仍核验当前绑定，无 Port 调用。随后专项 38 项全部通过。
- [Golden 首次错误](p13_evidence/golden-final.stderr.log)：新增 CLI 给每个模式额外增加子目录，使旧 Windows 临时文件路径达到 263 字符。[路径诊断](p13_evidence/golden-layout-diagnosis.json) 与 [修复后输出](p13_evidence/golden-layout-fixed.stdout.log) 保留。仅移除冗余目录层；P01 自身仍创建独立 sandbox identity，没有修改原 Awakening/存储实现。

## 本轮终局实际结果

| 执行项 | 实际结果 | 耗时 | 证据 |
|---|---|---|---|
| P13 专项 | 38 项：38 PASS、0 SKIP/FAIL/ERROR | 32.894 秒 | [逐项结果](p13_evidence/p13-stable-final.tests.json) / [stderr](p13_evidence/p13-stable-final.stderr.log) |
| E5-A/P02、Thinking/Action、P08/P09/P12、前 P12 修复直接兼容 | 445 项：444 PASS、1 既有 SKIP、0 FAIL/ERROR | 451.420 秒 | [身份与结果](p13_evidence/compat-final.tests.json) |
| 当前最终源码全量，仅一轮 | 1049 项：1048 PASS、1 既有 SKIP、0 FAIL/ERROR | 596.249 秒 | [逐项结果及源码 hash](p13_evidence/full-final.tests.json) / [stdout](p13_evidence/full-final.stdout.log) / [stderr](p13_evidence/full-final.stderr.log) |
| Golden CLI | 7 模式及重放断言完成，退出码 0；不重复计作 unittest 项数 | 外层 4.867 秒 | [命令记录](p13_evidence/golden-layout-fixed.json) |

专项之后仅修改了 Golden CLI 的目录布局，已经实际运行该命令验证；兼容之后的沉默预算调整只作用于新增 P13 纯物化代码，随后专项验证。最终全量包含这些最终源码、全部原兼容项和全部 38 项新回归，源码 hash 与收口现场一致。全量后只调整档案，不机械再跑三轮。

SKIP 唯一身份仍为 `test_p08_fixture_paths.P08FixturePathTests.test_symlink_component_rejected_without_writes`，原因 `OS does not grant symlink creation: 1314`。没有新 SKIP；不计作 PASS。原 1011 项身份全部保留，旧测试文件未修改。

## 终局审计与待复核

[只读审计](p13_evidence/final-audit.audit.json) · [精确未提交清单](p13_evidence/final-audit.pending-files.md)。包括六 Schema/25 冻结边界、正式七文件及树指纹、三规划源、版本 0.1.0、31 个 P10 脚本、源码/测试身份、静态/链接/敏感模式/差异和历史内容保护。

P00—P12 ACCEPTED；P13 / Engine side / 十二项 IMPLEMENTED_NOT_ACCEPTED；P14—P23 NOT_STARTED。当前 PLANNING_CONFLICT=NONE、EVIDENCE_CONFLICT=NONE 仅表示本地已知失败均闭合，独立复核仍待进行。请重点核对七模式的上游决定绑定、当前消费与原事实恢复分离、原日志和最终源码身份。未执行任何 Git 写操作，不修改 Assistant，不进入 P14。

## R1/R2 当前复核入口

初版下方38/1049证据全部为历史，不能覆盖独立新反例。当前独立原件、修复前后七项、正式相邻测试、失败辅助历史和运行命令统一见新返修入口。 本轮 P13 专项 59 PASS、0 SKIP/FAIL/ERROR，79.325 秒；直接兼容 445 项：444 PASS、1 既有 SKIP，492.606 秒；最终全量 1070 项：1069 PASS、1 既有 SKIP、0 FAIL/ERROR，621.988 秒。 [逐项证据](P13_独立复核返修_R1-R2.md)。当前 EVIDENCE_CONFLICT=PRESENT，待规划侧复核；未创建 P13 验收决定。

## 2026-09-08 P13 用户正式验收补记（D-063）

现行验收入口转到D-063。独立7/59/5项均PASS；本次只核验引用1070项施工全量，不重跑或把重叠组相加。历次红测、辅助错误和单一既有SKIP保留。 [正式验收与证据](P13_用户正式验收_20260908.md)。本次无 Git 写操作，不修改 Assistant，不进入 P14。
