# P18 当前有效续接：已授权统一有限尝试

用户已确认两个存储文件范围及完整操作绑定 WinError5 的有限尝试行为变化；不重复申请。仅修改 json_repository.py、json_runtime_repository.py 与必要 P18 测试、证据、档案。旧等待授权记录是历史。

P18 IMPLEMENTED_NOT_ACCEPTED / EVIDENCE_CONFLICT=PRESENT。无 D-073、无 Git 写、无 P19。有限预算属于同一次存档，不属于引擎寿命；不改生产政策、ACL、只读属性、权限、计费或状态格式。

## 实现前验证矩阵

| 组 | 实验 | 必须保持 |
|---|---|---|
| A | 原正常进程2项、冻结时钟读者反例2项、独立持续走时2项 | 原断言和25秒；无新聊天仍推进；子进程明确STOP清理 |
| B | 读者仍持有/分类前释放/长占用；无绑定错误；完整绑定持续WinError5；真实权限拒绝和只读 | 当前允许尝试不代表历史共享；固定上限；原生裁决；无授权公共旧路径一次失败 |
| C | 临时/目标内容和身份改变、版本冲突、保存成功返回丢失 | 原记录完整；停止旧提交；原事实恢复不重复revision；主错误与清理错误保留 |
| D | 重试中PAUSE/STOP/撤权/控制竞争；耗尽后宿主、控制和其他合法任务 | 保留原事务和身份；不误报成功；局部等待不变成整个宿主停机 |
| E | 有实际TEST效果的原事实恢复；R1/R2、P18、公共兼容、最终一次全量 | 不重调用、不重派、不重扣；原1480身份/断言保留 |

原始反例、旧规则观察和独立原件保持不变。新增测试表达已批准的规则。历史F1/H1/F2分别保留缺失证据，不自动唯一归因或关闭。

当前：准备固定现场；新运行使用本目录唯一标签，不启动重复全量。若测试失败先保留，按原遗漏/新回归/辅助问题/未确定分类。

## 2026-09-14 实际进度（替代上段准备状态）

已修改两个获准存储文件；新增 tests/test_p18_storage_retry.py、test_p18_storage_runtime.py，共27项。原1480测试身份与断言文件保持。

最终身份在 frozen-source-02.json：269文件、1507测试；sourceHash=sha256:31ebcd2eb210304a98dae87a836ff2bed9581bc41d26216dc5dc75e4eaa4bb7e。frozen-source.json 是前一个中间版本，不能冒充最终版本。

boundaries-final-04：27/27 PASS。当前 validate_final.py 已启动且串行执行原进程4项、走时2项、旧独立8项、原生拒绝4项、P18、兼容；它发现任何非零立即停止。恢复时先检查实际进程和已完成记录，不重执行整套脚本。全量尚未启动，必须等前组完成后单独用唯一 full-final-01 标签启动一次。

首次失败：boundaries-after-01 三项新TEST长路径261字符导致唤醒临时文件创建失败，短根原断言验证通过；new-guards-before-01 的临时文件初始绑定/复制线程context/异常链三项失败与 expired-context-before-01 过期context错误，均保留后作两文件内补修。auxiliary-placement-01 是新增测试类插入位置的语法辅助错误，已留证纠正。不能把这些写成历史F1/H1根因。

## 最终全量进行中

前序全部完成：originals-final-03 4/4、advancing-final-02 2/2、prior-boundaries-final-01 8/8、native-denial-final-02 4/4、P18 147/147（215.150秒）、公共兼容605/605（892.095秒）。源码均匹配frozen-source-02。

full-final-01 已单独启动，唯一标签存在STARTED记录；本次工具会话56035，但若中断恢复必须重新检查真实进程和结果，不能凭旧会话号重开全量。源码和测试在整个全量期间固定。先等真实汇总；若失败保留现场，不自动再跑。

全量成功后执行本目录 finalize.py，根据真实JSON生成报告和文档（不预填数字）；然后保存实际进程观察为process-observation-final.json，执行audit.py生成精确清单及审计。若有检查问题，保留失败证据后仅在获准档案/工具范围内处理；不修改冻结源码来迁就收尾。当前尚未执行这两份收尾脚本。

## 最终交付进度

新增正式27项，原1480项身份及断言保留。P18 147 PASS；公共兼容 605 PASS；本轮最终全量 1507项=1506 PASS、1既有Windows 1314 SKIP、0 FAIL/ERROR，1319.841秒，exit 0。各组重叠，不相加。
最终结果已完成，标签见selected-runs.json；源码仍frozen-source-02。不要再次跑全量。审计后交独立复核，历史UNKNOWN及EVIDENCE_CONFLICT保留。

终局审计02为最终入口（final.audit-02.json / final.pending-files-02.md）。首版审计后仅补充文档说明：首次失败分类、一个中间运行期间新测试文件加入、原始日志格式告警；运行源码/测试和全部最终结果均未变。不需重复全量。
